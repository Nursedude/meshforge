# Review provenance ledger — which commits have been through a review pass

> **Why this file exists**: scoping "what's unreviewed" cost a full
> exploration pass in the 2026-07-05 QA session because review provenance
> lived only in commit bodies. One row per review pass, updated when a
> pass closes. A commit range NOT in this table has had **no** adversarial
> review — that absence is the signal this ledger makes legible.
> Convention: record the RANGE + scope paths + fix commits + where the
> residual/refuted notes live. Never delete rows; supersede them.

| Date | Scope (range + paths) | Mechanism | Fix commits | Residuals / refuted notes |
|------|----------------------|-----------|-------------|---------------------------|
| 2026-09-01 | **FRONTIER PASS (Fable 5.1) — closes the QUEUED field-eComm + duty-cycle DESIGN row below (not a code review).** Scope: the three ⚖️ decisions in `.claude/plans/field_ecomm_and_dutycycle_fleet.md` (detached = zero fleet deps; rejoin one-way valve; DORMANT as declared posture) against the storm-prep + Lala forensics docs, `fleet_roles.yaml` (`field-node`), `fleet_offline_check.sh` (`via`/UNOBSERVABLE leg), `role_expected_active`/`watchdog_retarget` (declaration re-read), `fleet_truth` cell states, `virtual_fleet.py` chaos drills, and lehua's bring-up (GPS/RTC facts). | Adversarial DESIGN review, inline: every decision attacked with a concrete failure timeline from this fleet's measured history (Lala page storm replayed against today's instruments; moc4's 8-day clock; kiai's path-vs-box page; the two-preset split; the tunnel-is-inbound contradiction), then written as position + invariants + virtual-fleet drills. Key rulings: the kit is a STANDALONE-offering box while detached (`field` role, enrollment conditional on posture); GPS on the MeshAdv Mini is the kit's stratum-0 and the fleet's first owned stratum-1; the tunnel is a DOOR for the operator not a PATH for organs — the kit pushes evidence, nothing pulls, ordering key `boot_id+uptime`; the kit carries evidence not messages across the boundary (message continuity = a second kit tier, deliberately); DORMANT = one operator-values file mirrored pre-storm, four per-box states (`active/shed/dormant/detached`), MANDATORY capped expiry, posture drift a finding both ways, clock-gated expiry that HOLDS when the consumer's clock is unconfirmed, a declare-time validator refusing a mesh-less posture, automatic entry from battery metrics explicitly OUT of this arc, and a closed-enum consumer list that must fail a test until each reads posture (the bulk of the build). | this commit (seed section `⚖️ Frontier design pass — DECIDED 2026-09-01` appended: positions, drills V1–V8 + one real-fleet drill, priority order against the Starlink cutover clock, three operator-only open questions). No code. | **Nothing verified live — a design pass.** Residuals: (i) three OPEN QUESTIONS only the operator can settle (kit tier, whether the truth-spool pull survives, automatic posture entry); (ii) the build is day-work behind the existing gates and the vfleet needs a `field` node type first; (iii) the posture-consumer list was compiled by reading the probes named, not by grepping every freshness judgment in the tree — the seed-coverage test proposed in the position is what makes that list complete, and it does not exist yet. |
| 2026-09-01 | **FRONTIER PASS (Fable 5.1) — closes the QUEUED mesh-oracle policy-split row below (item (b) of deferred `mesh-oracle-followups`).** Scope: `src/oracle/responder.py` `_allowed`/`from_env`, the five leg builders, the RNS leg in `bridge_rns_events_mixin.py`, `_rns_bridge_xform.py` M→R/R→M formatting + `peer_gateway_destinations`, the LIVE drop-ins and audit logs on moc + moc3, MA's drop-in on meshanchor-server. | Frontier inline adversarial DESIGN pass; every claim drilled against ground truth read live this session, not from the queue text. **Transitivity (sub-q 1) — CONFIRMED, and it had already fired 74 times.** moc bridges every non-consumed Meshtastic text to RNS as RAW content (`content = msg.content`, attribution only in `title`/`fields`) fanned to `default_lxmf_destination`, which on moc includes moc3 (`f68c2f56`). moc3's RNS leg calls `handle(source_hash, raw)` with NO tagged-text guard, so under `*` it answered moc's gateway hash `3dfbdb5d` 74/118 times (06-22→08-13; 62 in June, 10 July, 2 Aug). Exact two-log witness: moc declined `help` from `!896b1917` at 07:05:34Z 08-13 (`cooldown`), moc3 answered `help` from `3dfbdb5d` at 07:05:36Z, and moc's `messages.db` holds that reply as RNS-inbound at 07:05:36. Composition: moc's node allowlist + cooldown bypassed for its whole segment; ALL moc-side askers collapse into ONE moc3 cooldown bucket; moc3's log cannot name the originator (it is in `lxmf_fields.meshforge_from_id`, unread); the reply re-broadcasts on moc's RF via moc's R→M leg (BELIEVED from code — `should_bridge`→queue→`[RNS:f68c]` label; no sibling capture DB holds the RF copy and moc's journal starts 08-30). Net effect of moc's 'strict' allowlist was only to choose WHICH box answers. This is cross-mesh answering, which the operator locked OUT 2026-06-22 (memory `project_mesh_oracle_channel_whitelist_2026_06_22`: per-mesh LOCAL answer, no cross-mesh querying). **Channel gate (sub-q 2) — it IS the authorization primitive on Meshtastic, and the stronger one.** A decoded packet under a channel name means the local radio decrypted it with that channel's PSK = the sender holds the key; the `from` id is unauthenticated and spoofable (responder's own comment) — so a 3-node allowlist with no channel gate on a default-PSK channel authenticates nothing, while `CHANNELS=meshforge` (private PSK) is real key-possession auth. On RNS the LXMF source hash is signed, so THERE an allowlist means something — and that is exactly the leg moc3 left at `*`. The split is INVERTED, not merely inconsistent: each box is strict where identity is weak and open where it is strong. **Verdict: DRIFT, not intent.** moc3's `*` predates both moc's oracle (06-24, operator: 'own nodes only') and the 06-22 no-cross-mesh lock; it was RNS-leg bring-up convenience ('verify end-to-end') never revisited, and the 08-29 drill + today's re-drill (`5f1e34a7`, minted, allowlisted nowhere, 10 answers) show it reaches any announced identity — the fleet RNS links to an external transport node (VolcanoAI + moc `TCPClientInterface`) and moc3 carries an RNode LoRa interface, so 'announced' includes strangers. Three real unknown RNS askers are in the log (`0f648f68` 06-22 = the validator's ephemeral id; `f40f6142` ×3 on 07-20, unidentified). **Position for ratification** (written into deferred `mesh-oracle-followups.check`): (1) replace moc3's `*` with the same two client hashes moc trusts (`f0a6899b`, `3ac3d1e3`) — never a gateway hash; (2) moc gains `MESHFORGE_ORACLE_CHANNELS=<private channel>` OR the operator accepts documented spoofability on LongFast; (3) the structural fix that holds under EITHER ratification is landed here: a peer gateway is never an oracle principal. | this commit (peer-gateway guard on the RNS leg: `decline(reason="peer_gateway_relay")` before the allow logic, keyed on `rns.peer_gateway_destinations` — one constant, hfm #5; relayed text bridges on as chat = 'cmd crosses, ack doesn't'; audit records gain `channel`; `ORACLE_DECLINE_REASONS` closed vocabulary test-pinned to the delivery probe's decline bucket so the new reason can't land in 'benign'; `docs/ENVIRONMENT_VARIABLES.md`; eval case `evals/local_brain/oracle_peer_gateway_relay_2026_09_01.jsonl`). VERIFIED this turn: 594 passed in the 6 oracle/bridge suites (rc 0), lint rc 0, guards 65 passed, sabotage drill (guard reverted) fails exactly the new relay test. | **NOT applied live**: the drop-in edits — a trust-boundary change the queue entry reserved for operator ratification; the guard itself activates on moc3's next gateway restart (deploy is manual). **Residuals**: (i) MA twin — `responder.py` is a DIVERGED twin and MA's RNS leg is fail-closed live (no `MESHANCHOR_ORACLE_RNS_ALLOWLIST`), so the port is day-work, not urgent; (ii) `_on_lxmf_receive` stores bytes titles/content as reprs (`[b'MeshForge Gateway'] b'dude-AI…'` in moc's messages.db) — legibility bug, day-work; (iii) the RF re-broadcast leg stays BELIEVED — a live drill from a non-allowlisted LongFast node would settle it, deliberately not fired (airtime on the operator's channel); (iv) moc3's log shows `from=''` ×2 answered via channel gate — empty sender folds to key `!`, harmless but ugly; (v) the MQTT/tap legs answer as channel BROADCAST, so on a default-PSK channel a spoofed query = an RF amplification of ~1:1, bounded by cooldown per (spoofable) sender — accepted tradeoff already in the 07-05 row, restated here so it is not rediscovered. |
| 2026-08-31 | **FRONTIER PASS (Fable 5) — closes the QUEUED rotate_session_notes row below.** `scripts/rotate_session_notes.sh` + `tests/test_rotate_session_notes.py` @ `5d83306f` (working tree byte-identical, verified by empty diff). | Frontier inline adversarial pass, single-context, per the brief's non-doctrine frame (bash/parser/filesystem correctness). Every candidate DRILLED input→output in a scratchpad sandbox before being called a defect; live notes never touched. Steer 1 partially REFUTED by drill: CRLF, first-line-`## ` (pre_lines=0) and bare-final-heading all rotate clean; the two steer-1 shapes that were real: no-trailing-newline and tab-in-heading. Steer 2 CONFIRMED by drill (touched 2026H1 captured 2026H2's content). Steer 3 verified held (mid-pipeline sed loss is caught by the conservation equation — existing fault-injection test). Steer 4 verified benign (byte-mode `grep -cxF` consistent on both sides; display `substr` may split a multibyte char, cosmetic). Steer 5: malformed STICKY_RE dies fail-closed rc 2 before any write (drilled); over-broad regex is visible as all-KEEP-STICKY in the printed plan — accepted for a human-invoked tool. Steer 6 (prune): glob/stem-eviction/backward-clock attacks re-derived — no new defect beyond the existing pins. **Double tap 2026-09-01 (operator doctrine), prune path LIVE against sandbox copies of the real files** (37KB notes + 346KB archive, real emoji headings; live notes never touched, originals checksum-verified): run 1 — full sweep rc 0, byte math re-derived from disk (37193−7342=29851; 346585+7449=354034), both buckets pruned independently to the exact rank-based survivor sets (11+8 oldest deleted), 4 planted foreign files untouched, both own backups md5-identical to pre-apply originals, zero temp litter. Run 1's own catch: the own backup ranked 4th, so rank alone saved it — the identity guard never decided. Run 2 forced that shape (6 future-dated 2099 seeds, --keep-backups 2, own backup rank 7 of 7): identity guard fired as the SOLE protection, own backup survived md5-intact while 4 future-dated files pruned — the stale-clock scenario proven live, not just in the fixture test. No code change needed. | `7086ec4f` — 4 CONFIRMED, fixed, all one class (verification keyed to a representation instead of the thing): **(1) no trailing final newline + last section rotated** → correct rotation failed by 1 B (awk counts every line length+1); cure: conservation measured from disk both sides (stayed+arrived==original, banner measured separately). **(2) duplicate headings inside the NOTES** → both-rotate failed the per-row +1 check, kept+rotated failed "still in notes"; cure: per-unique-heading ledger (archive +k, notes −k). **(3) tab inside a heading** → plan's awk `$6` truncated it, every verification grep missed; cure: heading = tail of the TSV row (mawk-safe, no `{n}` intervals — fleet awk is mawk 1.3.4). **(4) `trap - EXIT` after the mv's** leaked `.add` temp beside the archive + pre-counts in TMPDIR every clean apply; cure: trap stays armed. Plus steer-2 fix: archive chosen by lexically-last NAME, never mtime. Plus fail-closed hardening: empty (errored) verify-phase grep count → −1, never "no mismatch". Tests 33→41; all 6 new/changed guards mutation-drilled red; suite `41 passed` rc 0; shellcheck rc 0; lint rc 0. | Residuals, unfixed and named: 4+-backtick/`~~~` fence conventions can mis-toggle (two ``` sample lines inside a ````-fence expose interior `## ` as boundaries — wrong PLACEMENT, bytes still conserved; odd imbalance still refuses); crash between the two `mv`s duplicates (never loses) content, absorbed next run; invariant 3 is really verify-after-commit + backup + loud restore, not files-untouched-on-failure; `find -name` prune glob would over-match a stem carrying glob metachars (hostname-derived stems don't); test smell: `test_keeps_newest_n`'s `any("202" in n)` is tautological over its 2020 seeds (own-backup survival is properly pinned elsewhere). Steer 8 design verdict: size-triggered human-invoked rotation with sticky-correction stands; no change recommended. |
| 2026-08-30 | **FRONTIER PASS (Fable 5) — closes the Pri-1 worklist row below (queued `baf27057`).** The 2026-08-30 dude-claw arc: MF `978883ea..b9d6751c` (PSK bridge `claw_copy_fleet_channel.py` + `claw_set_fleet_channel.py` + their security tests; `claw_telemetry.py::_parse_direct`/`last_hops`) + fork `wireclaw-dudeclaw` `4f1172d..2d21e0d` (`tools.cpp` reboot refusal + mesh_set_channel persist; `lora_ears.cpp` hop-awareness). Do-not-relitigate steers honored: config-rollback DO-NOT-BUILD and digipeater NO untouched. | Frontier inline adversarial pass, single-context. Every candidate defect DRILLED before being called one (mocked-main drills on the bridge; planted-violation drills on the test pins; the pre-fix file fails all three new pins). Static attacks backed by arithmetic (reply-buffer budget computed vs the 768/512-byte caps) and by re-deriving the Meshtastic flags layout against the fork's captured `0x62 -> limit 2, start 3, hops 1` example (derivation CORRECT for hop_start>0). Fork checks run live against source: `reboot` absent from `AGENT_TOOL_ALLOWLIST`, deferred-reboot consumer exists (`main.cpp:1871`), all four refusal branches fail CLOSED. | `ee7b8007` (all 3 confirmed defects + 5 main()-level pins, revert-drilled; suite 11018 passed rc 0, lint rc 0) | **3 CONFIRMED, fixed (all MF-side, all in the empirical-warrant's predicted class — degraded state mapped to valid-looking output): (1) persist-FAILED read as success** — `--persist` + firmware `[persist FAILED]` exited 0 printing "(RAM-only)"; older-firmware silent-ignore had the same hole; both now rc 1, and `claw_set_fleet_channel` (persist defaults ON) gets the same gate + rc 1 on unparseable replies. **(2) dark-claw UNKNOWN branch was DEAD CODE** — `request()` raises `NatsError`, never returns None, so the rc-2 UNKNOWN path never ran; a dark claw exited 1 as a bare ERROR (UNKNOWN conflated with proven failure). **(3) the argv security pin had a hex hole + a tautology** — firmware takes "hex or base64", the test checked only `B64_32` (planted 64-hex key passed 10/10), and `"--psk" not in joined or "--psk-stdin" in joined` could never fail. Also fixed: the "captured verbatim" reply-shape test wrote `(persisted)`; firmware has emitted `[persisted]` since fork `eaa7489`. **QUEUED — firmware (needs a flash cycle, do NOT hot-fix from a fleet box; implementable specs + drills queued 2026-08-30 in the fork's `FORK.md` §"Queued for the NEXT flash cycle" — that section is the single home, this row is the pointer): (F1) reply truncation yields CONFIDENT WRONG dB** — `lora_stats` with the full 12-id watch list hits 771 chars worst-case vs the ~743-byte NATS reply budget (`reply[768]` minus wrapper; agent path caps at 511 — clips from 8 ids), and `jsonEscape` truncates silently mid-token, so a clipped `@-104` parses host-side as a VALID −1 dBm; undetectable by the parser — firmware should emit a terminal sentinel the host requires (absence = mark watch/direct suspect), or emit `direct=` before `watch=`. Fleet currently at 2-3 watch ids, so not live TODAY; arms as the list grows. **(F2) (0,0) false-direct**: an originator that never sets hop_start (pre-2.3 Meshtastic) relayed down to hop_limit 0 arrives flags=0x00 → hops=0 → the RELAY's RSSI recorded as a DIRECT link — exactly the wrong-siting failure; all-modern fleet channel makes it low-odds now, watching any third-party/public node makes it real; conservative option (require hop_start>0 for the direct claim) trades away legit hop-limit-0 directs — operator's call. **(F3) `tool_reboot` guards `wifi_ssid` only** — a config whose `wifi_pass` KEY (the firmware's actual name; this row first said `wifi_password`) was clobbered/lost passes the guard and strands (empty password is legit on open APs, so check key PRESENCE, not emptiness); 1400-byte config buffer truncation refuses fail-SAFE (verified direction). **NOTES**: reboot refusal path REMAINS undrilled (portal-only — unchanged from the 08-30 row above); static review found no fail-open leg. Token drift: `claw_metrics_push` honors `MINI_DUDEAI_NATS_TOKEN`, neither channel tool does — the day the claw bus gets a token, the security-path tools break while metrics keep working. ~~UNKNOWN: whether the claw↔bus NATS TCP leg crosses AREDN RF~~ **RESOLVED 2026-08-30 (same day, traced live): NO AREDN RF hop exists in any claw↔bus path.** Topology as measured: bus = moc2:4222 (wired, site LAN `.41`); claws 01+03 sit in `10.120.250.192/28` behind node WH6GXZ-6-VOLCANO-HI-HAP (br-wan `.250`), claw-02 in `10.143.126.64/28` behind WH6GXZ-6-VOLCANO-QTH-HAP (br-wan `.27`) — matched live `ss` peers (two conns from `.250`, one from `.27`) to each claw's own `device_info` IP. Both nodes hold their OWN MACs in moc2's ARP (L2-attached WAN legs, never routed via another node), both report **zero RF mesh links** (`link_info` empty; their mesh fabric is WireGuard — `wgc0`/`wgs0` up), and the single wireless hop (claw→node LAN AP, DudeNET/DudeNET2) is **WPA2-PSK** — all three claws carry a set `wifi_pass` (15/20 chars, presence checked via a masked filter ON the bus host; values never left it) and are connected through it. So the PSK's set-time transit is: WPA2 air + NAT + wired/site-LAN copper — encrypted or physical end to end. **Standing caveats**: (1) this is TODAY's shape — if a claw ever re-homes to a node whose backhaul is an RF mesh link, the exposure returns; re-derive with the same two reads (claw `device_info` IP → owning node's `sysinfo.json?link_info=1`). (2) The protection is link-layer only: NATS is cleartext+tokenless, and `tool_file_read` serves `/config.json` UNREDACTED over the bus (`wifi_pass`, `nats_token`, `lora_tx_psk`) — the on-device LLM agent is allowlist-excluded from it but any bus/LAN client is not; deliberate bus-trust design, now stated with the boundary verified rather than assumed. |
| 2026-08-13 | **FRONTIER PASS (Fable 5) — closes the QUEUED row below.** Pri-1 design (partial blindness vs the 3-word disposition vocabulary) + MF `765337d3^..9cbf2bb4` + MA `7b43b46b^..f1d82ed4`; seam reviewed: `note_disposition` → watchdog.json coverage → mini `_coverage_blind_extractor` → `detector_blind_any` | Frontier inline deep review, single-context (VolcanoAI no-fan-out convention); all 6 steers walked; steer-2 sibling sweep run BEFORE designing (all multi-subject clean-noting probes enumerated); live checks this pass: `TZ=UTC0 systemd-analyze calendar` omits the `(in UTC)` line entirely (the finding), `journalctl --since -216000s` accepted rc 0, malformed `--since` rc 1 with empty stdout (indistinguishable from no-match — unreachable today, windows are generated), `USER_UNIT=` journal lines carry ONLY `SYSLOG_IDENTIFIER=systemd` (verified on a real timer unit), all 9 fleet boxes `Pacific/Honolulu` (timedatectl, per box) | this session's fix commit | **Pri-1 ANSWERED — the vocabulary stays three words; the SCOPE moves from prose to structure.** The sweep settled the design before any option was weighed: the scalar disposition forces every N-subject probe to pick a collapse, and sibling probes had picked OPPOSITE wrong ones — `user_timer_unit_failing` partial→`clean` (blindness laundered into health, the queued question) while `cron_verdict_stale` partial→`indeterminate` (the reboot_unjudged branch discarded the true negative on every judged cron — steer 1's trap, already shipped in a sibling). A 4th enum state was REJECTED: it can't say HOW MUCH was judged, has no honest worst-wins rank (partial vs inert aren't ordered), and costs a closed-enum sweep of every consumer (hfm #7). SHIPPED instead: optional validated `coverage={"judged": n, "enrolled": m}` on the disposition entry (bool-excluded, judged≤enrolled, malformed dropped never raised, worst-wins keeps entries wholesale); `user_timer` + `cron_verdict` pass it; mini's `_coverage_blind_extractor` seeds the SAME `detector_blind` kind from `clean` + judged<enrolled (full blindness keeps its branch, one condition per class, inert exempt) — so a permanently half-blind box now escalates differently from a fully-observed healthy one WITHOUT discarding true negatives, ratio damping stays in the rule (annotate-only, 24h grace) per the ratified (b) contract, zero new files/probes/enum members. `cron_verdict` keeps `indeterminate` when the judged subset is EMPTY (clean over nothing is the other laundering). Taxonomy row (steer 3) checked: (a)(b)(d) never posed partial-vs-total; the (b) contract ("probe-side semantics fix, never a rule whitelist") is exactly what shipped. **Steer-4/5 attacks on `timer_cadence` — 3 CONFIRMED fixed** (all latent, none live on this HST fleet): (C2) a UTC-timezone box loses EVERY OnCalendar cadence silently — `systemd-analyze` under `TZ=UTC` prints no `(in UTC):` line, the regex parsed nothing, rc 0, None memoised, no warning (standalone-offering real; cure: force `TZ=UTC` in the subprocess + match the trailing `UTC` stamp, mixed-shape double-count pinned); (C3) a TRANSIENT analyze failure (timeout/OSError/missing binary) was memoised as the spec's answer for the process lifetime while the memo's docstring promised staleness was impossible — transient now raises internal `_AnalyzeUnavailable`, converted to un-memoised None (deterministic rc≠0 rejection still memoised; trade documented: a persistently wedged analyze now costs its timeout per spec per tick, loud, instead of silent permanent flat-floor); (C4) `RandomizedDelaySec` took the FIRST parseable line, systemd honours the LAST assignment → `reversed()`. Multipliers (steer 4) HELD: 2.5 guarantees ≥2 firings for any cadence, wrong-multiplier direction degrades to unjudged (the outcome gate is load-bearing — it also absorbs drop-in `.d/` schedule edits the file-text read can't see, in the safe direction); the `fails+oks` outcome proxy HELD under live attack. **REFUTED**: job-stdout contaminating the Failed/Finished patterns (`USER_UNIT=` carries only manager lines — verified live, so no false-page/false-recovery leg); seconds-form window rejection; `SELF_HTTP_PORT` dual-module-instance via `src.` imports (docstrings only); memo staleness on unit EDIT (spec-text keying holds). **MA range (steer 6) — fail-OPEN RATIFIED**: registration is structurally wired at both bind sites before `serve_forever` + pinned by `TestSelfPortRegistrationIsWired`, and the unregistered case is precisely the cross-process caller (TUI `collect_fleet_rollup`) where the fetch is legitimate on a genuine co-install; on the MA-owns-:5000 box that cross-process fetch now merges NOTHING (the server's own guard strips the MF blocks from the body it serves) — bounded, self-healing, one wasted localhost fetch per rollup. BrokenPipe port faithful to the MF twin (narrow tuple, debug witness, narrowness pinned); both BaseException-sentinel egress tests sound (the AssertionError-swallowed drill note is the right lesson). 9cbf2bb4/710c78ce reviewed clean. **RESIDUALS/QUEUED**: **MA cadence port gap** — MA's `check_user_timer_unit_failing` still judges against the FLAT 3h window AND counts a unit judged from a SINGLE witnessed outcome (one in-window failure reads healthy via `len(fails)<min_failures`); its `HealthResult` vocabulary can't carry dispositions, so this is a shape-tier port needing its own drill cycle, not a byte-port — queue for the next MA session (MA-only standalone boxes are the exposure; meshanchor-server itself is covered by MF's watchdog). Sibling adoption queued: `tracer_peer_unreachable` (partially-unparseable rows / vanished peers fold into clean, no witness) and `fleet_box_unreachable` (malformed state rows silently skipped → clean) should pass `coverage=` when they skip subjects. fleet.html renders partial coverage only via the reason prose (cell stays green; mini is the consumer of record — acceptable, noted). Recency headroom is 0.5·cadence: a job that burns >half its cadence before failing can flap under the recency gate (pre-existing shape, unchanged). Malformed `--since` rc 1 = observed-empty in the journal helpers (unreachable today; would surface as all-unjudged indeterminate, not clean). MA header-write path (`send_response`/`end_headers`) still unguarded against hang-ups (twin-consistent with MF; payload write dominates the storm); MA port-0 ephemeral bind would register 0 and disarm the guard (test-only shape). **Double-tap addendum (same day)**: C3+C4 re-proven live against REAL systemd-analyze (PATH-broken → None + the loud warning, PATH-restored same process → 87000.0 healed = not memoised; last RandomizedDelaySec honoured); live MF-mini engine exercised on VolcanoAI (restart → 30s ticks, journal 0 errors, correctly emits nothing on a 6/6 box). ⚠️ STRUCTURAL GAP the tap surfaced (pre-existing, not introduced): the fleet's ONLY shortfall box (meshanchor-server, 2/4) runs MeshAnchor's mini, which BY DESIGN reads MA's SQLite fleet-watchdog and never MF's watchdog.json — so MF-watchdog blindness (full OR partial) has NO local mini consumer on the dual-stack box, and never did; today it surfaces only in the fleet-wide coverage map. Fold into the queued MA mini/port arc: a dual-stack box needs a consumer for the co-installed MF coverage block. The cron_verdict clean+coverage flip has no live exerciser today (fleet sweep: no box carries an unjudged @reboot beside judged crons) — test-pinned only. |
| 2026-08-13 | **QUEUED — NOT YET REVIEWED. Frontier worklist Pri-1 (design question) + the day's unreviewed ranges.** MF `765337d3^..9cbf2bb4` (`fb7920d5` cadence-aware user-timer window + new `src/utils/timer_cadence.py`, `710c78ce` map/counters test pins, `9cbf2bb4` /fleet/slo no-egress invariant) and MA `7b43b46b^..f1d82ed4` (`563e5d21` BrokenPipe guards, `0f8419bb` self-port recursion guard + `map_data_service` port registration, `f1d82ed4` end-to-end self-dial pin). Design question centres on `src/utils/watchdog_probes_user.py::probe_user_timer_unit_failing` + `watchdog_probe_core.note_disposition` consumers. | **NONE — this row records an ABSENCE.** Build-time work only: each change was drilled in the failing direction (planted violations, red-first, restored green) and verified live on the affected boxes, but **no adversarial review pass has run on any of it**. Drills prove a guard is wired to its function; they do not attack the design. Author-written checks encode only the narrowness the author already thought of, so the fresh-eyes pass is still owed (feedback_review_your_own_fixes; authorial distance). | none yet — queue only | **THE QUEUED QUESTION (Pri-1): a partially-blind probe still reports `clean`, and the blindness survives only as PROSE.** Measured live on meshanchor-server 2026-08-13 after the cadence fix: `{'disp': 'clean', 'reason': '2 of 4 enrolled timer(s) judged; no failing job. Unjudged: meshanchor-map-restart.service (no journal lines at all in 216000s); meshanchor-mini-dudeai-dream.service (…217500s)'}`. That is a strict improvement on the morning's affirmative `clean` over all four — but the disposition consumers (mini's `detector_blind_any` seed, `_BLIND_DISPOSITIONS`) read the DISPOSITION, not the reason string, so a box that is permanently 50% blind escalates identically to a fully-observed healthy one. By this tree's own rule — *a long-running `detector_blind` is a finding, not furniture* (2026-08-05) — that is arguably the same laundering one level up. **Steers for the pass**: (1) it is NOT a simple "flip to indeterminate at N%" — some timers WERE judged, and discarding a true negative to express partial blindness trades one laundering for another; consider whether the vocabulary needs a fourth state, a coverage FIELD on the disposition, or an escalation keyed on the ratio, and note the footprint cost of each (no new machinery to watch machinery). (2) **Sweep for siblings**: any probe that judges N subjects and collapses them into ONE disposition has this shape — the "N of M judged" idiom is now in this module and the delivery/queue file-fallback legs; enumerate before designing. (3) Check whether the second-brain taxonomy (row 2026-07-26, questions (a)(b)(d)) already answers partial-vs-total blindness, or deliberately left it open. (4) Attack the cadence design itself: `LOOKBACK_CADENCE_MULT=2.5` / `RECENCY_CADENCE_MULT=1.5` are asserted, not derived, and the outcome gate uses `fails+oks` as a proxy for "firings witnessed" — a `Type=simple` triggered unit logs `Started`, not `Finished`, so its firings are uncountable and it would sit unjudged forever (no such unit on the fleet today — verified — so the caveat is theoretical and documented, not live). (5) `timer_cadence` shells `systemd-analyze` per distinct spec and memoises on spec TEXT; attack the memo's staleness window and the `None`-means-fall-back-to-flat-floor path. (6) MA's `SELF_HTTP_PORT` is a process global written at bind time by a single writer — attack multi-bind / test-order / import-shape assumptions, and whether fail-OPEN on an unregistered port is the right default. |
| 2026-08-12 | **re-review of fix commit `a5d5c377` itself** (feedback_review_your_own_fixes — closes the QUEUED item in the row below): the 19-file fix diff, esp. the exithandler guard, the presence gate, the wedge warning, the warmstart predicate | /code-review high, FRESH-EYES fork (not the agent that authored the fixes — authorial distance is the point): 8 finder angles → 16 candidates → 12 CONFIRMED + 3 PLAUSIBLE + 1 REFUTED; 10 reported, all 10 applied after independent re-verification in the main session (unit files, RNS fork source, fleet_roles.yaml, watcher source all read live) | this commit | **The headline pair proved the a5d5c377 guard wrong twice over**: instance-scoped `_transport_voided` vs a process-global hazard — it missed every fresh-bridge restart (daemon restart_service, gateway_cli) AND broke the one same-instance path (`commands/gateway.py restart()` latched RNS permanently dead while reporting success). **Cure DIFFERS from the reviewer's suggestion** (process-global witness in rns_init): the teardown call is REMOVED outright — verified `Reticulum.__init__` registers `atexit(Reticulum.exit_handler)` (fork line 391) so process-exit cleanup is RNS's own, a shared-instance client skips `persist_data()` anyway, and the call had NEVER successfully run (misspelled since inception) — months of production prove "stop leaves Transport alive". Pin flipped to assert ABSENCE via AST (first cut grepped text and failed on its own explanatory comment — the assert-on-prose trap, third instance, caught by the run). Also fixed: default wedge list now intersects role-derived expected-active (was standing noise + false-positive-re-arming remediation on every gateway-only box; warning now fires only on explicit overrides, with `.service`-normalized membership); presence gate True family's bare else closed (unrecognized status → indeterminate, never inert); warmstart↔watcher axes 4-5 (raw strptime, `environ.get(key,default)` + loud empty-path) + a MECHANIZED watcher source pin (skips in MA — absent by design); 9 warmstart tests unpinned from the operator's real ledger; prose-bucket sentence pinned against the pathspec (harness check #11). Dropped by the reviewer's cap (acknowledged, not fixed): uncached `_names_called` (~0.19s), contract-glob scope gap (no escapee today), `[^2]*` regex brittleness (fails loud), falsy `id: 0` display collapse (pre-existing). |
| 2026-08-12 | **the queued 08-12 inert/indeterminate arc** — MF `1f30cc8e^..9840d006` (8 commits): honest_status skew leg, warmstart deferred-backlog line, watchdog absent→inert conversions (9 probe modules), gateway exithandler fix, rns_api_surface gate. All 7 queued steers walked. | /code-review (Fable 5 forked agent): 8 finder angles → ~17 candidates → 12 verified live → 10 CONFIRMED (facts checked against unit files, watcher source, runner gating, repo layouts this turn); same-agent fix pass, then MF+MA lint/suites/parity re-run (exit 0 quoted per batch) | `a5d5c377` (MF, 19 files, +532/−163; MA warmstart byte-port staged separately) | **Steers 1, 2, 4 each confirmed real**: (1) wedge_check ⊄ expected_active had no owner → runner warning; (2) `nomadnet-silence-watch-user.service` IS a resident scripts/ daemon → scripts+requirements.txt join the CODE pathspec (trade-off accepted: scripts-only commits now mark resident units behind — the honest direction); (4) `void_queues()` was never read → exithandler now guarded: `_connect_rns` refuses loudly after a void instead of "connecting" a dead singleton (GUARD, not cure — a true fix touches the `open_reticulum` chokepoint, deliberately not attempted). Steer 6's five "false positives" re-derived clean by the AST gate itself. Also: warmstart predicate drift vs deferred_work_watch (3 axes, honest_failure_modes #5), mixed-type-id sort crash, EACCES-reads-as-absent, ambient-systemd test verdict, ~10-copy presence-gate prologue → shared `note_unit_presence_gate()` + contract test, AST-walk 4× redundancy. DROPPED after verify: `from RNS import` gate bypass (no such import in src/ today), `pid <= 1` nits (pre-existing minor), test-side pathset hardcode (subsumed — now pinned against the script). **QUEUED**: re-review of fix commit `a5d5c377` itself (feedback_review_your_own_fixes; also the leg-3 advisory's >800-line window) + the MA shape-tier port pass (in flight this session). |
| 2026-08-09 | **self-review of THIS session's own probe work** (operator: "double tap — done is not done if it's half wired and not tested; be adversarial about yourself and your helpers") — `12440f48..9a193604`: src/utils/watchdog_probes_drift.py, watchdog_probes_gateway.py, watchdog_probe_core.py, new watchdog_probes_rns_env.py, scripts/lint.py MF027 | Opus-class single-context adversarial self-pass — NOT a frontier fan-out. Method: attack my own new helpers with fixtures, then count real (unmocked) test coverage per helper | `f5e04286` (this push) | **CONFIRMED + FIXED 5, all in code I wrote hours earlier.** (1) The MF027 widening from `c1e23a93` was defeated by ONE fixture — a `_note_*` helper noting on one branch and silent on the other passes it and still goes dark; my 4 "narrowness" tests missed exactly that case. REVERTED; the real fix was shape, not scope — `_classify_dups_unreachable` RETURNS (disposition, reason) and the handler makes the literal call, so the strict gate passes both probes with no relaxation. (2) FIXED TEMP NAME `f"{cp}.tmp"` in the consumer-path cache, violating honest_failure_modes #8 in the same session I quoted that file repeatedly; `utils.paths.atomic_write_text` already existed — reinvented badly, now reused. (3) `_service_interpreter`: 7 branches, ZERO tests, including an `#!/usr/bin/env` branch written from memory and never executed — 8 tests added. (4) `_ask_interpreter_site_dirs`: 8 test references, EVERY ONE a mock — the real subprocess path untested, verbatim the 2026-07-25 failure cited earlier the same day; 4 tests now drive it unmocked via sys.executable. (5) `_read_pkg_versions_for_user.last_source` function-attribute side channel — stale across calls by construction, no test; returns a tuple now. **Repeat pattern named:** TWO tests this session asserted on my own prose rather than the code (the #69 Reticulum guard, then the temp-name guard grepping my comment about not using a fixed temp name) — both now assert on behaviour. **Residual:** MF027 still cannot see through ANY indirection (documented, deliberate — flow analysis is over-engineering for a 20-line AST check); the probe-side classifier shape is the answer instead. |
| 2026-07-24 | **dude-claw + fleet-monitor** — the recent multi-claw work as a diff: `3d21f683` (multi-claw fleet_truth + fleet.html), `c17c611c`+`44b2e067` (witness fan-out / spool tri-state), `d85f1323`, `8052c44f` (claw-02 instance, multi-claw NOC, battery soak), `756e9f2b`+`78ee633f` (host-probe witness failover), `17611b07` (claw@ deploy wiring). Surface: src/utils/fleet_truth.py, web/fleet.html, web/node_map.html, src/utils/map_federation.py, src/mini_dudeai/rollup.py, scripts/promote_seed_rules.py, scripts/battery_soak.py | /code-review (adversarial, forked agent) → 8 findings, ALL confirmed against the code by hand before fixing; the fixed panel re-rendered headlessly against a new-shape truth doc; probe changes live-run against moc2's real tick files + soak config | `41f34b4b` (8 findings, +14 tests) + MA twin `3790ebd7`; `4c840cb5` (battery intelligence follow-on, +50 tests) | ONE class throughout: a MISSING/UNOBSERVABLE claw rendered as a present/healthy one. Fixed: `claw_count` counted absent claws ("2 claws healthy" for one claw); fleet.html invented a claw row on every unreachable/`accepted_blind` box (3 phantom rows live); FAILED chip styled identically to DARK; node_map + `_fetch_peer_claw` + rollup all primary-only (dead dudeclaw-02 invisible on 3 surfaces); promote_seed_rules reported a half-applied promotion as a total no-op; battery_soak mislabelled pre-tagging rows "from other devices". **DOCUMENTED not changed** (operator agreed, same session): the claw cell's FAILED path is unreachable against the real producer (every not-ok reason matches `_UNOBSERVABLE_MARKERS` → DARK, and DARK claw doesn't taint) — flipping it would paint the fleet red during the deliberate claw-02 discharge; corrected the false docstring + UI footnote instead and pointed at `probe_claw_device_dark`, which does page. That thread became the follow-on: `utils/claw_battery.py` (level+trend+intent, SSOT for the 3.4/3.5 dual-hardcode) so a deliberate discharge, an unasked-for drain, and a crash-with-healthy-pack stop being the same page. |
| 2026-07-23 | `src/gateway/*` (~30k lines, ~50 files) — honest-failure-modes lens across 4 clusters: claim-producers, node-state/models, queue/routing, bridges/transforms | 4 parallel code-reviewers + per-finding adversarial verify against the consumer (frontier tier) | `e9f6ccd1` — 4 CONFIRMED fixed: message_queue `get_stats.failed`, node_models `from_meshtastic` lastHeard, bridge_health meshcore KeyError, node_tracker name-provenance; `tests/test_gateway_honest_failure_fixes_2026_07_23.py` (12). MA twin port `241ec894`. | Full finding inventory (4 fixed + 12 queued ranked + twin note + clean bill) in `.claude/audits/gateway_review_2026_07_23.md`. Pri-1 closed follow-on (row below). |
| 2026-07-23 | follow-on from the row above — `gateway_heartbeat.py` (Pri-1 finding) | fix from the review's own inventory (no new review pass) | `6afe194c` (MF) + `3f4a60cb` (MA twin) — monotonic peer-liveness; `tests/test_gateway_heartbeat.py::TestClockStepDoesNotSplitBrain` (3) + 5 existing tests moved to the monotonic field | Closes the queued Pri-1 (wall-clock peer-liveness → split-brain). Remaining queued findings (2–12) still in `gateway_review_2026_07_23.md`. |
| 2026-07-01 | transport-truth arc (gateway cid/dedup/true-origin files: base_handler, canonical_message, mesh_bridge, mqtt_bridge_handler, rns_bridge, _rns_bridge_xform, meshtastic_handler, config) | /code-review xhigh ×2 (second pass re-reviewed the first's fixes) | `2038a2db`, `a3b93007` | deferred_work ledger `transport-truth-arc` keys `review_fixes`/`review2_fixes_2026_07_01`; PSK-canary loss class, claim/confirm TOCTOU, save()-bakes-defaults, true_origin default decision (soak review 2026-07-07), relay_fields; refuted: seen-on-RF founding trade |
| 2026-07-04 | `474d630a..HEAD` — dudeclaw/oracle arc (src/mini_dudeai/*, launcher_tui/handlers/{mini_dudeai,offline_oracle}.py, claw scripts) | xhigh 10-finder + adversarial verify; fresh-eyes re-review of the fix commit | `9fd98ae0` (13+3), `2b47bb0c` (5) | memory `project_dudeclaw_oracle_review_2026_07_04`; refuted-by-design: `_probe_tier` no-localhost default, sequential tier GETs; residual latents: brief.py witness path, unconditional capability-index check, single-banner ERROR, CADENCE_VERDICT_NAME test-pin |
| 2026-07-05 | `adf4ac0c~1..cd500d8c` — kilo arc K0/K0.1/K1 + W5.1 (src/kilo/*, scripts/claw_metrics_push.py, mini_dudeai/claw_telemetry.py, monitoring/_mqtt_message_decoder.py + mqtt_subscriber.py, utils/db_inventory.py) | xhigh 10-finder + 8 verifier batches + sweep (41 verdicts) | this session's fix commit (see git log `fix: QA session 2026-07-05`) | Deferred with rationale: kilo/status.py move (await probe consumer), matrix totals SQL aggregate, single-pass status scan, packet-id UNIQUE semantics (witness added), full monotonic anchoring (witness added), repo-wide node_num_to_id sweep (~20 copies — follow-up arc), ghost-row migration (retention ages; map restart on moc instead). Kept-by-design: stray-glob fail-loud |
| 2026-07-05 | `2e57672d~1..5901e603` + `c88451f6` — TUI fleet_provision handlers + handler_registry guard | xhigh 7-finder + 3 verifier batches | this session's TUI fix commit | ~24 confirmed fixed (apply-path TOCTOU + warn-visibility + clobber-guard + registry dup-tag/orphan). Deferred: mqtt-leg reply-on-wrong-channel (needs radio query the leg avoids). All pinned by +32 tests |
| 2026-07-05 | `e2de394f~1..511c549b -- src/oracle/ src/gateway/` + `8bfa4f3e` + `47ec5ba5` — gateway RF oracle leg + resourcepath + id-less dedup | xhigh 6-finder + 3 verifier batches (27 distinct, 25 CONFIRMED) | this session's oracle fix commit | Contained correctness+security FIXED (paths.py rpc_key 0600/symlink-guard LIVE; MeshCore DM-or-drop + consume-invariant; PhoneAPI consume flag; is_query 'help' tightening; responder monotonic-cooldown + spoofable-id cap + node_num_to_id + answer_all-in-list + loop-guard pin; snapshot TTL cache; 3rd RNS-configdir writer + widened guard; dead-code + env-docs). DEFERRED → deferred_work `oracle-leg-hardening-arc-2026-07-05` (dedup_key-timestamp SOAK-COUPLED for 07-07 reviewer; _healthy liveness heartbeat; wrong-channel reply; 5x/6x/resolver refactors; 4 documented design tradeoffs). Pinned by +~20 tests |
| 2026-07-05 | **MAPS DOMAIN** (first-ever pass) — `fe864a20..2ae9eaca` in meshforge (src/utils/map_*.py, _map_*.py, node_history.py, prometheus_exporter.py, map_metrics.py) + meshforge-maps branch `qa/maps-audit-2026-07-05` (mqtt_subscriber, meshtastic_collector, map_server). Grafana/Prometheus/cloud-push scripts + the mini/oracle/kilo integration seam reviewed inline. | 4 parallel xhigh code-reviewer agents (map-core / HTTP-surface / federation+prometheus / meshforge-maps-app) → 44 findings; top ones self-verified; **self-re-review of the fix commits** (feedback_review_your_own_fixes) → caught **2 fix-introduced defects** (F1 `os`-unimported → atomic _save_cache NameError-swallowed → cache never written; F2 ISO-string last_heard → `str<=0` TypeError → healthy RNS nodes DROPPED), both fixed `b9c3f860` + `_coerce_epoch` + `import os`, neither had gone live (deployed via pull, restart held). **DEPLOY+VERIFY 2026-07-06**: pushed `fe864a20..b9c3f860`, fleet 5/5 rolling-restarted, CI green, full suite 8177 passed exit 0, watchdog 5/5 clean. | meshforge: `1cec1199` (HTTP security), `6c4fcb14` (collector correctness), `d29a684e` (federation/prometheus), `2ae9eaca` (meshtasticd #75-leak candidate), `b9c3f860` (re-review: 2 fix-introduced defects); meshforge-maps: **PR #90 merged `0bf63a2`** (was branch `1dedef2`, rolled out to moc/moc1/moc2) | FIXED ~32 across 6 commits (+49 tests): CORS subdomain-bypass, unauth RF-transmit/fleet-logs/run-test → LAN-trust gate, limit/window clamps, open-redirect, weather-lock, path-traversal; _timed_collect source-isolation, record_observations coord-coercion, CLI numeric-id, _total_nodes_seen reset, future-lastHeard clamp, RNS last_heard+coord-validation, atomic cache; federation node-cap + backoff-fields, _is_node_online future-clamp, MF008 systemctl, prometheus label-bucketing/zero-out + POST-cap; meshtasticd lock-contention→skip-CLI; meshforge-maps broker OOM-cap + numeric-id + status redaction. **DEFERRED → deferred_work `maps-audit-hardening-2026-07-05`**: A1-F1 VACUUM-under-lock (needs node_history locking-model redesign + concurrency harness — partial fix would worsen the wedge), /api/status live-meshtasticd caching (F8, cross-cuts oracle/mini/federation seam), 200-empty→503 status-code honesty (API contract), MQTT-before-bind cold-start, /tmp world-writable RNS cache path, peer-claw XSS (needs frontend render check), prometheus per-scrape collector reuse; meshforge-maps F2/F3/F4/F7/F8 (node-id-at-ingestion, health-staleness, api_key live-refresh, XFF depth, eviction-flood). |
| 2026-07-19 | **FLEET-TRUTH HONESTY INVARIANTS** (the queued frontier pass for the NOC arc) — `7085f4e4..e8255ccb` surface: src/utils/fleet_truth.py (builder), both repos' fleet_truth_collector.py, web/fleet.html, _map_fleet_endpoints/_map_fleet truth routes, the Phase-0 disposition recorder + ALL 43 probes' instrumentation (9 watchdog_probes_* modules), src/gateway/delivery_counters.py health block | Frontier self-pass (Fable 5) on the invariant core + **6 refutation-framed verifier agents** (5 over the probe instrumentation, 1 fresh-eyes renderer/collector attacker) + 3 fixer agents with the confirmed findings as specs; fixes verified by full suites both repos (MF 8821 / MA 5779, exit 0), lint 0, parity in sync | this commit (MF) + MA `c16a240e` | **CONFIRMED+FIXED ~27** (+~63 tests): BUILDER — stale watchdog signals presented as active-red (frozen-block signals now dark); stale mini active_rules presented as live escalations; `_ci_cell` invented FAILED from in_progress + let None-state read healthy (observed-states-only now); dead `/fleet/slo` leg never tainted (services → core-observability); counts/tiles were reachability-only so a faulted box tiled green (box_state worst-of roll-up). COLLECTORS — wall-clock TTL (NTP backstep froze the doc as fresh → monotonic); unbounded r.read() under the fan-out lock (drip/huge peer → 8 MB + 10s bounded read); HTTPException escaped to worker-error with peer-controlled bytes in the doc; IP-shaped fleet_hosts/fleet.json entries became served aliases (masked + ip_literal amber); MF endpoint self-fetch hardwired :5000 (→ live server_port); fanout.membership fleet-of-one transparency. PROBE DISPOSITIONS — false-clean leaks all sharing one root shape (un-noted read failure before a shared clean exit): spaced-instance `ss` needle blindness in the collision probe (the #69/#82 class — now uses rns_init's truncation-aware twin parser), empty/unrecognized rnstatus → interface-table clean, /proc/net/tcp unreadable → phoneapi clean + wedge streak-reset, tracer all-rows-unparseable → clean, cron verdict-log unreadable + @reboot-no-verdict → clean, dep_install_fragmented partial enumeration → clean, inherited-app dropped scan roots, router-scout skipped mirrors, garbled calibration ledger → clean; inert-vs-indeterminate splits (FileNotFoundError=inert, other OSError=indeterminate) across cron/fleet_box/host_frozen/ntfy×2/mini×2/role_drift/mqtt_root/dup-transport/synth+resource-canary; http_local HTTPError-vs-comment inversion (5xx now clean-for-this-class per docstring); delivery_counters snapshot-failure served stale-healthy preflight → `db_unobservable` marker + canary probe indeterminate (the #63 organ). MF025 split: probe_tracer_peer_unreachable → new watchdog_probes_tracer.py. **REFUTED (survived attack)**: renderer XSS (esc() coverage complete), default-dark stClass at every render site, cache serving partial builds, worker-error IP leakage from registry targets, worst-wins merge wrongly resolving double-notes, escalation-key mismatch. **RESIDUAL/ACCEPTED**: MA `non_self_peers` bare-hostname drop + IP-self double-count (MA row, deferred redesign); fan-out lock held across (now-bounded) fan-out — single-flight redesign deferred; slo cells can't express benign absence (name-gated core-taint is the middle; absent-aware slo cells deferred); phoneapi-wedge gateway gate + nomadnet not-live + file-absent inert conflations accepted with honest reasons where absence is positively observed; http_local permanently-erroring service reads clean for the *unresponsive* class by design; stale-panel disclosure = banner + opacity dim only. |
| 2026-07-13 | **perf-arc self-review (pre-Opus-handoff)** — MF `bd911479..8a6d61c1` (TUI deferred-imports + lazy package inits + MF025 + install_noc reorder) + the MA twin range | 6 parallel finder agents (line-by-line / removed-behavior / cross-file / reuse+simplification / efficiency+altitude / conventions), ~30 candidates, inline verification | MF `7282dad4` + MA `12539da2` (11 findings fixed both repos: unguarded deferred bridge/transport imports, per-tick failed safe_import in the topology monitor, lazy-map submodule names, NoneType-not-callable dialogs, silent toggle, post-commit ImportError misreport, save-swallow witness, MF025 cwd anchor, module-count budget, dead imports) | REFUTED-by-policy: handlers/gateway.py eager first-party import (CLAUDE.md direct-import rule); MF025 shrink-lock (test_baseline_entries_still_needed owns it); install_noc all-or-nothing ordering (intended). RESIDUALS (queued in project_opus_handoff_2026_07_13): lazy-__getattr__ factory dedup (3 copies), pre-existing first-party safe_imports in dashboard/metrics handlers, manifest-lazy handler registry (the deeper altitude fix that would obsolete per-file deferrals), messaging repeated `_messaging()` lookups |
| 2026-07-06 | **meshing-around BOT (fork repo)** — full external-data surface (mesh_bot.py dispatch + modules/{locationdata,space,wx_meteo,rss,dxspot,wiki,llm,globalalert}.py) at fork base `fde22f7` v1.9.9.8 (= upstream HEAD minus 2 cosmetic space.py commits) | 1 xhigh data-source agent + inline live verification (bot-box logs mined; tide/NINA/hamtest reproduced live) | none yet (audit-only; fixes queued in roadmap) | `.claude/research/meshing_around_bot_audit_2026_07_06.md` — 8 finding groups; wxa/wxalert single-char mangle + NINA NXDOMAIN **filed upstream SpudGunMan#324/#325**; hamtest data restored on bot box; fork-first fix roadmap (dispatch guard, tuple guard, UA+timeouts, honest failures, TTL cache). Fork deploy to bot box still ledger-gated (`meshing-around-fork-deploy`) |
| 2026-07-16 | **meshing_around_meshforge CLIENT repo — FIRST-EVER adversarial pass** (Pri-1). Whole-repo (~50 first-party py, ~23k LOC) across 4 surfaces: mesh-facing parse (mesh_client.py, core/{mqtt_client,meshtastic_api,callbacks,models,maps_client}.py), crypto (core/mesh_crypto.py), subprocess/system installer (setup/{system_maintenance,pi_utils,cli_utils,whiptail,alert_configurators}.py, configure_bot.py, scripts/apply_meshforge_patches.py, setup_headless.sh), TUI+config (tui/{app,whiptail_tui,helpers}.py, core/{config,global_config}.py, setup/config_schema.py). Distinct repo from the 2026-07-06 BOT-fork rows. | 4 parallel code-reviewer agents (one per surface) → ~35 findings; the 5 fixed were self-verified against source line-by-line (crypto derivation checked vs the Meshtastic default-PSK constant; desc-crash + User=root + rmtree confirmed by reading the call sites & signatures). Suite 1021→1028 green (system python3, exit 0). | client `4fc5eea` (5 confirmed defects, +9 tests) | **FIXED 5 CONFIRMED**: C1 non-spec `_derive_key` (SHA-256→AES-256 all PSKs; couldn't decrypt real default/128-bit /e/ — roundtrip tests passed on self-consistent wrong derivation) → rewrote to spec + spec-vector tests; C2 `set_key` silent-base64-truncation → `validate=True`; S1 modular `run_command` missing `desc=` → TypeErr crashed the update/patch-reapply path; S2 bot service `User=root` under sudo → prefer `SUDO_USER`; S3 `rmtree` on unvalidated root path → tested `_is_safe_rmtree_target`. **OPEN CONFIRMED**: S4 systemd-unit f-string injection/unquoted-path. **OPEN PLAUSIBLE** (not line-verified — triage before acting; full list in client `docs/reviews/2026-07-16-first-adversarial-pass.md`): TUI Rich-markup render-DoS from hostile node names (highest-value unfixed), config-editor materializes template defaults (#62 class), crypto multi-key first-parse-wins false-accept, terminal-escape injection via logger, get_nodes off-lock RuntimeError kills geojson thread, unbounded `_decrypt_warn_last`, AttributeError-not-counted, apt/dpkg/git-branch/stash honest-failure set. ⚠️ crypto fix FIELD-TESTED same session — VERIFIED wire-interoperable: fixed `_derive_key` decodes firmware-format default-channel AES-128 ciphertext (16-byte `d4f1bb3a…01`, nonce id‖from‖0) through the module's own `process_encrypted_packet` 3/3, old SHA-256/AES-256 derivation 0/3; envelope parse confirmed on 60 real `mqtt.meshtastic.org` ServiceEnvelopes. Residual: no physical-radio-over-RF default packet was available (public broker republishes decoded; local mesh runs a custom channel only) — result at client `a791448`. /json path unaffected. |
| 2026-07-06 | **meshing-around FORK FIX PASS** — fork branch `audit-fixes-2026-07-06` (`a15ca63..d9cac65`, 6 commits): mesh_bot.py dispatch+wxalert+mwx, modules/{locationdata,space,globalalert,wx_meteo,dxspot,llm,settings}.py, new modules/fetch_cache.py + tests/ (74 pytest, upstream had none) | roadmap items 1–5 implemented, then **8-angle self-review** (3 correctness + reuse/simplify/efficiency/altitude/conventions finder agents) → ~24 candidates → inline verify vs quoted code → 11 fixed in `d9cac65` | fork `7c48261` (dispatch guard), `07f14b3` (#324 tuple), `d2a266d` (UA+timeouts), `ef746cb` (honest errors), `996d17a` (ttl_cache), `d9cac65` (review fixes) | Re-review caught **3 fix-introduced defects** (NO_ALERTS cached 90s = stale all-clear on safety cmds; tide predictions-[] IndexError past my own guard; str/float cache-key split) + 8 pre-existing (volcano ignore-list continue-wrong-loop, drap UnboundLocalError, quake no-magnitude IndexError, hfcond unguarded, satpass network→usage-hint, IPAWS main-feed parse, UK fetchers failure→NO_ALERTS, llm.py functions-post no timeout). DEFERRED w/ rationale: guarded-fetch chokepoint + UA consolidation (upstream-PR-sized), single-flight (documented limitation), dead `location=` lines (upstream noise). Deploy+merge COMPLETE 2026-07-06 same day: zero-delta repoint verified (clean start + live canary ACK 10:43:51 HST over RF), then branch ff-merged (`d9cac65`) + redeployed to the production bot (clean start, 0 tracebacks) |
| 2026-07-09 | `9bbe2374..69845f84` — the unreviewed 07-06/07 range: Gateway Wizard (`gateway_wizard.py` + `guided_flow.py`), MF024 version guard (`version_consistency_check.py` + lint wiring), the three 07-06 deferred-maps fix commits (`bb7fea71` prometheus, `fd7fe952` node_history snapshot, `a13af553` /tmp→~/.cache move), template guard `69845f84`; PLUS audit angles over dudeclaw/oracle/mini diagnostics + plan-test-deploy-verify discipline (operator-requested) | /code-review high: 8 finder angles + 1 audit angle (52 candidates → 28 deduped → 24 verified in 5 batches; 8 REFUTED) → 10 findings (8 CONFIRMED, 2 PLAUSIBLE) → same-session fix pass | this session's fix commit (wizard role-gate + operator-confirmed verify + derived RX probe + preflight SSOT `collect_results` + rc-aware radio verify + decline≠failed + dry-run gate; guided_flow resume-honesty + stale-verify clear + chown state + clear_state witness; node_tracker atomic cache + chown + PermissionError witness; cron_verdict per-name retention (the #78 false-"never" root cause, live-verified 23.5h < 24h cadence); vcc CHANGELOG consumer + unreadable≠absent + single README read; lint MF024 `__file__`-rooted; exporter failure-TTL + `_cap_recent` witness) | **This row is also the recorded re-review of `bb7fea71`/`fd7fe952`/`a13af553`** (they had none — convention 3 violation found by the audit angle; the pass found 3 real defects in them, all fixed here). **Fix-pass re-review (convention 3) caught 3 fix-introduced defects, all fixed pre-commit**: `_v_radio` assumed symbolic region names but `meshtastic --get lora` prints enum INTEGERS (`lora.region: 1`) → permanent false-negative + resume loop (HIGH; now decodes numerics via protobufs, honest not-checked fallback); the radio tests pinned that wrong CLI shape (masking it); the no-prompt role-gate test was vacuous (FakeDialog.yesno defaulted True — now pins yesno_calls==0). REFUTED (don't re-flag): template `mqtt_region=""` is the documented-correct #34 default (RX subscribes both shapes); no root-hosted UnifiedNodeTracker exists (split-home cache divergence not reachable — latent only if daemon.py ever unit-hosted as root); `--print-role` has no error path echoing a role name. DEFERRED w/ rationale: shared-collector settings freeze until restart (cross-cuts MapDataCollector reload semantics); GATEWAY_ROLES/LORA_PROFILES derive-from-source (altitude — role gate now uses provision_role SSOT at run time); Option-B deployed-config migration for baked `true_origin_downlink_enabled=false` (moc1/moc2 dormant, documented in memory); legacy /tmp cache fallback (fleet already restarted past the window); `render_birc_numbers` regex dedup; heapq micro-opt on the cap sort. |
| 2026-07-09 | **FRONTIER WORKLIST Pri-1** — `src/utils/rns_init.py` (whole file, first adversarial pass) + the `open_reticulum()` seam: all 4 callers (rns_bridge pre-init, node_tracker, map collector, commands/rns.py), lab primitive callers, `test_rns_init.py` + `test_lab_common.py` pins, MA parity | frontier inline deep review, single-context (VolcanoAI multi-agent constraint — no fan-out); ~24 candidates walked against quoted code → 2 CONFIRMED fixed, rest refuted or residual | this session's fix commit (construct lock + reinitialise absorption in `open_reticulum`; vanished-listener-owner one-rescan in `check_rns_listener_owner`) + byte-identical MA port (parity in sync) | CONFIRMED FIXED: (1) check-then-construct race exported OSError("reinitialise") that 2 callers re-implemented catches for (chokepoint contract #2) — now `_CONSTRUCT_LOCK` + absorb-if-existing/re-raise-if-not, 4 new tests incl. 2-thread single-construct; (2) vanished pid between ss and /proc read as "malicious squatter" → fail-loud "kill <dead pid>" on ordinary rnsd restarts — now one fresh re-scan (teardown-gone passes, new-rnsd passes, live squatter still immediate, twice-unreadable still loud), 3 new tests + pinned test rationale updated. RESIDUALS (documented, not changed): `init_reticulum_with_watchdog` skips guards when config lacks instance_name (all lab callers write it — latent); `probe=False` bypasses require_listener (tests-only knob); `@rns/<name>` substring prefix-collision in presence scan (needs 2nd prefix-named instance; #82 lesson — don't "fix" socket-greps speculatively); silent configdir-mismatch singleton reuse; caller belt-and-braces reinitialise catches left in place (harmless). REFUTED: exit-124 no-fallback (deliberate freeze guard); `_rnsd_unit_enabled` Exception→False (documented fail-open); TimeoutError/OSError except order (correct — subclass first) |
| 2026-07-09 | **FRONTIER WORKLIST Pri-2** — `src/gateway/message_queue.py` + `message_routing.py` (whole files, first adversarial pass) + seam: `delivery_counters` DropReason taxonomy, `utils/classifier.py` bounce semantics, ack-flow callers (bridge_ack_mixin, bridge_send_mixin, meshtastic_broadcast_bridge), MA twin copies | frontier inline deep review, single-context (VolcanoAI constraint); ~23 candidates walked against quoted code → 3 CONFIRMED + 1 latent-inversion fixed, rest refuted or residual. **Twin-first check found MA carries all 4 shapes** — same fixes + tests ported (MA files are diverged copies, not parity-locked) | this session's fix commits, both repos | CONFIRMED FIXED: (1) `QUEUE_SHED` was in the closed DropReason taxonomy with ZERO recording sites — `_shed_overflow` deleted QUEUED messages with no DROPPED witness (lifecycle read QUEUED→nothing; honest_failure_modes #9); now records per shed row post-commit (`_dc.record` never raises by contract); (2) `mark_acked` SELECT-then-UPDATE race → two concurrent callers (LXMF callback vs overdue-ack sweep) both got the origin dict → double synthetic ACK, violating the idempotency contract 3 callers rely on — now atomic UPDATE-is-the-gate, 8-thread single-winner test; (3) `mark_delivered` unguarded UPDATE → double call double-counted stats + duplicate SENT — now `status != 'delivered'` guard; (4) routing `_classify_message` bounced branch `return category==QUEUE` — verified ALWAYS False today (classifier never emits QUEUE under action="queue") but would have bridged review-held messages if that changed → explicit `return False` (behavior-identical) + dead `direction_key` removed. RESIDUALS (documented, not changed): wall-clock timestamps throughout (persisted-state constraint; fleet-wide class, HST no-DST); legacy-rule recompile keyed on rule NAMES only (in-place pattern edit under same name = stale regex until restart — cross-cuts config-reload semantics); `get_stats` "delivered"/"failed" keys DB-shadowed while other keys are process counters (mixed semantics, longstanding); `_direction_allows` unknown-direction→allow vs `_has_matching_rule` unknown→never-match asymmetry (validator should own it); at-least-once redelivery after crash-mid-send (design); enqueue depth-check race (self-correcting ±1). REFUTED: dedup constant-hash class (already fixed, 3-shape branch); pacing break starving other destinations (per-destination loop); stats lock granularity |
| 2026-07-09 | **FRONTIER WORKLIST Pri-3 (security-first)** — `src/monitoring/mqtt_subscriber.py` + `_mqtt_message_decoder.py` + `packet_dissectors.py` + the `traffic_storage.py` dissector seam (whole files, first adversarial pass; hostile-input lens — nodeless mode parses the PUBLIC broker) + MA twin (older fork, pre-K1) | frontier inline deep review, single-context; ~28 candidates walked against quoted code; twin-first found MA carries every shape PLUS the pre-K1 numeric ghost-node bug; a THIRD twin (meshforge-maps, protobuf path) queued as new worklist row rather than blind-ported | this session's fix commits, both repos | CONFIRMED FIXED: (1) hostile-input class — unbounded/untyped attacker fields became node-dict keys, cached labels, and message-feed junk (64 KB payload cap × 10k node cap = memory/disk exhaustion; count-prune bounds COUNT not SIZE): node-key gate (canonicalize→require "!", ≤16 chars, `nodes_rejected` witness), label clamps+type-checks (`_clean_label`: str/numeric-enum only, containers rejected), text-message sanitation (dict text refused, 2 KB clamp, `_safe_*` on channel/snr/rssi/hop_start, msg-id 64); (2) `save_config` wrote broker credentials 0644 → chmod 0600; (3) `_on_message` swallow-at-debug had NO witness — processing could be 100% dead while messages_received climbed → `processing_errors` stat + 60s-throttled warning; (4) `traffic_storage` tree-rebuild called `_build_tree(packet, metadata)` but RNSDissector took 3 args → TypeError killed the WHOLE packet-list query on any stored RNS row (narrow except missed it) → default `raw_data=b""` + widened per-row except; (5) `RNSDissector.dissect(None,…)` crashed on eager `len(raw_data)` in a dict.get default; (6) `capture_packet` had no dissector containment (junk channel string killed the capture path) → per-packet degrade to UNKNOWN + `dissect_errors` witness; (7) client_id same-second collision → random suffix; (8) `_on_connect` stats write unlocked. MA port additionally canonicalizes numeric wire ids at `_ensure_node` (fixes MA's pre-K1 int-keyed ghost nodes; copied `node_num_to_id` verbatim). RESIDUALS (documented, not changed): `_connect` returns True on connect-timeout → reconnect loop records circuit-breaker success on ambiguous state (breaker largely decorative on this path; fleet-proven behavior — changing risks the #82 speculative-fix class); single topic-shape subscribe (#34 both-shapes fix is gateway-side); `_discover_relay_node` O(n) scan per relayed packet; `nodes_with_health_metrics` counts packets not nodes; DisplayFilter OR logic parsed-then-ignored (dead expr code); mqtt_decryptor seam not reviewed (separate module). Fix-introduced defects caught pre-commit: dropped `class` line in the decoder (py_compile), wrong config path in the MA-ported perms test |
| 2026-07-09 | **FRONTIER WORKLIST Pri-4** — `src/utils/service_check.py` (whole file, first adversarial pass) — the privileged-systemd SSOT; every service query/mutation fleet-wide + `_sudo_write` (unit-file/`/etc` writer) + MA twin | frontier inline deep review, single-context; ~16 candidates walked against quoted code → 2 CONFIRMED fixed; twin-first: MA lacked Finding D (already sudo-free) but shared Finding A | this session's fix commit + MA port (A only) | CONFIRMED FIXED: (1) **Issue #20 wrong-state class** — `check_service`'s read-only queries (`is-active`/`show`/`list-unit-files`) routed through `_sudo_cmd`, sudo-prefixing when non-root; on a box without passwordless sudo `sudo systemctl is-active` fails with empty stdout → read as "not active" → fell through to a (also-failing) sudo `list-unit-files` → a RUNNING service reported **NOT_INSTALLED**. New `_systemctl_query_argv` (read-only, never sudo) aligns `check_service` with the same-file sibling helpers (`is_service_enabled`/`is_service_masked`/`is_service_unit_installed`) that already avoid sudo; behaviour-identical on the fleet (passwordless) + as root, correct on the degraded box. MUTATIONS (start/stop/restart/enable/disable/mask/daemon-reload) KEEP `_systemctl_argv`/`_sudo_cmd`. (2) `_sudo_write` root path did `open('w')` (truncate-then-write) → a crash/ENOSPC mid-write left a half-written systemd unit that bricks the service on next daemon-reload (#60 class) → atomic mkstemp+fsync+`os.replace` (0644), temp cleaned on any failure. +5 tests MF / +2 MA. RESIDUALS (documented, not changed): `_sudo_write` NON-root sudo-tee path still non-atomic (tee truncates; atomic needs temp+`sudo mv` = 2 elevated calls — the established pattern, changing it risks the #82 speculative-fix class); transitional `activating`/`reloading` sub-states (is-active returns non-zero → classified not-running during a slow start — behaviour-change risk, systemctl-trust design); `_wait_for_tcp_ready(4403)` hardcodes the port while `check_service` uses `MESHTASTICD_PORT` (== 4403 today, cosmetic). REFUTED: SubState `split('=')[1]` (values never contain '='); exception-handler ordering (FileNotFoundError/TimeoutExpired are the right subclasses first); user-scope queries (already correct, `--user` needs no sudo) |
| 2026-07-09 | **FRONTIER WORKLIST Pri-final — THE GATES THEMSELVES** — `scripts/lint.py` (MF001-MF024) + regression-guard seam (`test_regression_guards.py`, `test_honest_signal_guards.py`) + MA twin `lint.py` | frontier inline deep review, single-context; "can a real violation slip the gate?" lens; ~12 candidates → 1 CONFIRMED (structural, affects 4 rules) fixed; twin-first: MA carries the identical defect | this session's fix commits, both repos | CONFIRMED FIXED: **the lookahead/lookback rules resolved a line's file position via `content.find(line)` — the FIRST textual occurrence.** Since lines like `time.sleep(1)` and `result = subprocess.run(` repeat verbatim across a file, a REAL violation on a LATER duplicate line was judged against an EARLIER twin's context and **silently passed the gate** — a false-negative in the enforcement layer every other guarantee rests on. Affected MF001 (Path.home import-fallback context), MF004 (subprocess-timeout lookahead), MF009 (Reticulum configdir lookahead), MF010 (daemon-loop enclosing-def lookback). Cure: `lint_file` threads each line's true running char offset into `_check_line(…, line_offset)`; the 4 sites use it; a `<0` fallback preserves the legacy path for any other caller. RED-FIRST VERIFIED both repos: 2 new tests (`TestLinterLineOffsetContext20260709`, MF010 byte-identical-sleep + MF004 identical-first-line) FAIL against the `content.find` behavior (`0==1`, violation missed) and PASS with the offset fix. Behaviour-identical on non-duplicate lines (full-repo lint stays exit 0). RESIDUALS (documented, not changed): the whole-file self-skip for `lint.py` (a real violation IN the linter isn't self-caught — accepted, self-reference); per-line rules can't see AST scope (a `time.sleep` in a nested non-loop def inside a `_loop` method is still judged by the outer def name — heuristic by design). REFUTED: MF002/MF003/MF006/MF007/MF008/MF019/MF020 are pure single-line matches (no position recompute, unaffected); the offset arithmetic is exact because `readlines()` preserves newlines and `content=''.join(lines)` |
| 2026-07-16 | **FRONTIER WORKLIST Pri-1 — Opus interregnum review** — MF `5083d56d..835528e0` + MA `417d57ad..a1f32f93` (07-13→07-16): manifest-lazy registry step 1 (`ab0fbb1d`: handler_registry `_LazyHandler`/`register_lazy`/`_materialize`, gen_capability_index shared walker + manifest emitter, pre-commit dual-artifact drift gate, 464-line test file), upshift-witness pre-push gate (`aef8177e`: review_provenance_check.py legs 1-3 + hook wiring), MF025 splits ×5 MF (`b28f1308` node_history, `1d2e18e4` drift→liveness+env, `3ef3a5b8` gateway→flow, `0514f220` map-collector config, `49a86ef4` rns_bridge events mixin) + ×2 MA (`341cb7f9` prometheus→metrics_http_handler, `33370b3e`), make_lazy_exports factory (`6b828995`/MA `f68c99a6`, twin byte-identical VERIFIED), cron_verdict flock fix (`2d34877d`/MA `a1f32f93`, twin-identical modulo path), local-brain eval attempts knob (`999fc0ff`), fleet_pull.sh (`09fbda87`), ntfy loopback retry-once (`db195a1d`), MA-only frozen-blackout positive-evidence fix (`bdad6e02`) + #74 settings port (`6e66f7c8`); convention-3 light pass over prior fix commits `7282dad4`/MA `12539da2`; spot-check of Fable mid-window `5e539616` (per the amendment). | frontier inline deep review, single-context (VolcanoAI no-fan-out constraint); ~30 candidates walked against quoted code; split fidelity via `--color-moved=zebra` non-moved-line extraction per split commit; LIVE direct-first import probes of all 6 new split modules (consumer-of-record, not wiring trace); `MetricsHTTPHandler` re-export exercised live; safe_call return-propagation seam verified in handler_protocol; gates re-run this pass: MF lint --all exit 0 + gen --check exit 0 + 110 targeted tests exit 0; MA lint exit 0 + 43 targeted tests exit 0 | this session's fix commit (leg-2 remediation message + honest-limit docstring + `TestLeg2BlockMessage`) — **1 CONFIRMED defect, found LIVE by the gate's own first firing, not by the read**: pushing this very row, leg 2 false-blocked under the ledger's stale `claude-opus-4-8` read (one-claim lag) — the docstring's "never false-blocks (a stale-frontier read is permissive)" was wrong in the OTHER direction (stale-NON-frontier false-blocks the first genuine frontier session after an interregnum, exactly the session most likely to stamp a frontier row), and the block message pointed only at `--no-verify` instead of the sanctioned rule-6 manual `record_claim` unblock (which was applied: claim `9378e62c8268`, then re-push). Detection semantics deliberately UNCHANGED (the lag is inherent to the chosen deterministic signal; weakening leg 2 to absorb it would reopen the fake-pass hole). Everything else in the range held up under adversarial read: splits are faithful code motion, new machinery is honest-failure-modes-conformant. | RESIDUALS (documented, not changed): **step-2 flip hazards for the manifest arc** — `register_lazy` raises ValueError on errored descriptors, so the future main.py manifest loop must decide crash-vs-skip-with-witness BEFORE the flip, and `_materialize`'s failure dialogs assume `_ctx.dialog` non-None (headless ctx AttributeErrors inside safe_call's except — dormant, nothing calls register_lazy yet); `_get_rns()` caches a FAILED RNS import until restart (deliberate per-tick-spam fix, network_topology); provenance gate leg-1 witness is satisfied by a SHA named anywhere in this file incl. queued rows (queueing IS the sanctioned response — by design) and REVIEW_CLAIM_PATTERNS can false-block a docs commit that merely discusses reviews (bias-to-precision documented; cost = one --no-verify); MA frozen check abstains indefinitely on an alternating None/stuck window (holey window never accuses — test-pinned latency tradeoff) and all-missing surfaces under KIND_FROZEN wording not its own kind (closed-enum); fleet_pull.sh hosts file must be one-host-per-line (inline comment would ride into ssh argv → UNREACHABLE); eval `latency_s` now spans all attempts (compound, fine as tell); ntfy retry doubles worst-case runtime to ~90s (within cron budget). REFUTED: MA metrics_http_handler circular import (prometheus_exporter side is function-local + PEP 562 `__getattr__`; direct-first import passes live); watchdog hub double-registration (explicit named imports, no star-import); leg-1 "queued-row-as-witness" as a bypass (queueing is the gate's own prescribed action). |
| 2026-07-09 | **FRONTIER WORKLIST (final row) — meshforge-maps THIRD subscriber twin** — `meshforge-maps/src/collectors/mqtt_subscriber.py` (whole file, first pass; protobuf + JSON ingestion). This repo runs on the internet-facing cloud-publisher box, parsing the PUBLIC broker. | frontier inline deep review, hostile-input lens, single-context; ~10 candidates → 1 CONFIRMED fixed | meshforge-maps `28542c6` (separate repo; not MF/MA parity-locked) | CONFIRMED FIXED: the protobuf handlers derive a bounded `!%08x` node id, but `_decode_json`'s fallback used an untrusted `sender` STRING verbatim as a node-dict KEY. Label caps bound VALUES + MAX_NODES bounds the node COUNT; neither bounds per-KEY SIZE → a crafted 64 KB `sender` from the public broker became a cached, served key (Pi OOM vector); a non-string `sender` (dict/list) also rode in to a downstream TypeError. Cure: `_key_ok` gate at the `MQTTNodeStore` write chokepoint (update_position/nodeinfo/telemetry/neighbors — "every path covered", same placement as the existing label caps) refusing non-strings + oversize keys with a `rejected_keys` witness surfaced in `get_stats.nodes_rejected`. +5 tests, RED-FIRST verified (oversize/non-string keys stored without the gate, refused with it). Deployed src-only to moc/moc1/moc2 (running collector keeps old code until next restart). RESIDUALS (documented, not changed): key gate bounds LENGTH+TYPE but does not force `!`-prefix (MF's `_ensure_node` does — kept looser here to avoid regressing a bare-hex JSON producer on this separately-deployed repo; length is the OOM-load-bearing bound); `_on_message` generic-except path logs at warning per-message with no throttle (log-spam under a systematic bug, not correctness — the `_decrypt_skipped` every-1000 is the silent-path witness). REFUTED: labels already capped at the store chokepoint (2026-07-05 maps audit); `_safe_int`/`_safe_float` already clamp every telemetry field; protobuf `from_node` is always formatted-hex (bounded) |
| 2026-07-16 | **FRONTIER WORKLIST item 4 — manifest-lazy registry STEP-2 FLIP review (design/hazard pass, NO code change)** — reviewed the planned flip of `src/launcher_tui/main.py:95` to manifest registration behind `MESHFORGE_TUI_EAGER=1` against the step-1 machinery (`handler_registry.py` `_LazyHandler`/`register_lazy`/`_materialize`/`get_handler`/`startup_all`, `handlers/manifest.py`, `main.py:336-354`). Supersedes the step-2 residuals in the 07-16 FRONTIER row. | frontier inline design review + 2 parallel Explore agents (handler `__init__`/`set_context` side-effects; ctx-dependent `menu_items()`) + live checks (19 manifest tests green, manifest lifecycle-flag audit, `isinstance(LifecycleHandler)` semantics, cross-handler `get_handler` call-site scan) | **NONE — review only; verdict BLOCKED with fixes handed to the executor.** Findings + blessed flip shape appended to `.claude/plans/manifest_lazy_handler_registry_design.md` ("Step-2 flip REVIEW"). | **VERDICT: do NOT flip until 2 BLOCKERS fixed (both missed by the design doc + `fc5be3b2` residuals).** BLOCKER-1: `startup_health` (on_startup-only → `isinstance(LifecycleHandler)` False → manifest `lifecycle: False` → registers LAZY) is called directly at `main.py:343` `get_handler("startup_health").on_startup()` → sentinel has no `on_startup` → **TUI startup AttributeError crash**; root = two divergent "must-be-eager" definitions (manifest both-hook isinstance vs main.py direct-call bypass); fix = widen the generator's `lifecycle` detection to on_startup-OR-on_shutdown (⚠️ keep the direct call — it's daemon-ungated unlike `startup_all`). BLOCKER-2: `get_handler()` is a plain dict lookup → cross-handler delegations (`mqtt.py:307` `_broker_menu()` hard-crash, `rns_interfaces.py:67` wrong-error, `startup_health.py:148` false-unavailable) hit unmaterialized sentinels; fix = `get_handler()` materializes on access. FINDING-3 (pre-existing, NOT flip-caused): `ai_tools.on_startup` (map auto-start) already never runs on main (missing on_shutdown → `startup_all` skips it). CLEARED: all 80 `menu_items()` static literals (AST-verified, no ctx-drift); `__init__`/`set_context` do no deferred work. Recorded hazards reconfirmed lower-sev: crash-vs-skip loop needs try/except+consolidated-witness (MF018); headless `_materialize` dialog DORMANT (interactive-only). |
| 2026-07-17 | **RNS 1.3.8 merge Phase-1 self-review** (feedback_review_your_own_fixes) — the self-applied merge resolution of upstream `1.3.8` into the RNS fork (`Nursedude/reticulum` branch `meshforge-138`, merge `6a90d2bb`), fleet-critical substrate about to be canaried: `RNS/Reticulum.py` (the 21-site `_rpc_recv` reconciliation + helper), the mf.4 logging re-port in `RNS/__init__.py`, and the auto-merged mf.3 detach bound (`Transport.py`) + #69 host-loss (`Transport.py`/`LocalInterface.py`/`BackboneInterface.py`). LXMF 1.0.1 excluded (byte-identical to upstream, no review needed). | 2 parallel adversarial code-reviewer agents (RPC reconciliation; mf.4/mf.3/#69 semantics), every finding source-verified against the merged tree, pure-1.3.8, and pre-merge fork before counting; diff-vs-upstream stitch audit; red-test-first on the one code fix | RNS fork `6dadb335` (F1 LOG_CALLBACK fix + test); eval-doc record `.claude/research/rns_138_merge_eval_2026_07_16.md` "PHASE 1 SELF-REVIEW" | **Merge is CLEAN — no stitch defect** (all 21 client recv sites bounded incl. new `is_blackholed`; server recv stays raw; `_rpc_recv` faithful; diff-clean vs pure 1.3.8 = only the 6 known patches; mf.3 detach byte-identical to pre-merge with upstream's new `deregister_listeners()` inside the bound; #69 `wanted_host`+listener check survive 1.3.8's `@rns/{name}` BackboneInterface binding). **1 code finding FIXED (F1, LOW/latent):** mf.4 re-port still invoked `logcall()` inside `logging_lock` → a LOG_CALLBACK handler that re-logs self-deadlocks the plain Lock (coverage narrowing vs mf.4's RLock); unreachable on the fleet (rnsd=LOG_FILE, no `src/` LOG_CALLBACK consumer) so not a canary blocker, but fixed structurally (dispatch outside the lock) to finish the re-port honestly — red-test-first `test_log_does_not_deadlock_when_callback_re_logs`. **2 OPERATIONAL hazards recorded for the roll runbook (not code defects):** [HIGH] RPC wire-format change (object-mode→msgpack) × #79 deploy-restart gap — a box where rnsd flips to 1.3.8 while a user-daemon client (nomadnet, meshforge-map) stays on old code times out EVERY RPC at 8s until manually restarted; roll gate = restart rnsd + ALL RNS clients together, confirm #79 hooks cover the user daemons. [LOW] `get_rpc_client()` connect still unbounded (upstream, pre-existing). Benign behavior change verified: 4 retention methods now swallow a wedge-TimeoutError to `False` (upstream's try/except design) — every consumer treats False as soft-fail, no wrong-state. |
| 2026-07-21 | **POST-07-19 WINDOW — full unreviewed range `5ca6ac33..77c66424` (77 commits)**: LXMF propagation arc (lab/lxmf_propagation_soak.py + fire script + soak probe), node-cache/announce-parser trilogy (node_models/node_tracker/rns_services), fleet-truth follow-ons incl. re-review of the 07-19 fix commit `5ca6ac33` itself (feedback_review_your_own_fixes), structural-dark burn-down probe batch (user/aredn/drift/liveness/claw/oracle rows), safe_import semantics flip `54a65060`, SSOT bump `72acf61e`. Also: QC-gate audit (honest_status FULL 6/6 PASS exit 0 pre-review @ 77c66424) + second-brain model-agnosticism audit (mini core clean; cadence pin fail-loud verified by design read; `claude_assistant.py` default model one generation stale — env-var seam, fail-safe). | Fable 5 frontier pass: 4 parallel adversarial code-reviewer agents (drill / node-cache / fleet-truth / burn-down), every counted finding source-verified at file:line; top findings re-verified inline by the orchestrator before fixing; red-test-first where the defect was pinnable (stale-coverage, oracle lockstep, wait_for_ci); MA twin ports run same session. | this commit chain (same push): fleet-truth+collector+safe_import+spool batch, oracle-classifier+probe-witness batch, node-cache batch, propagation-drill batch — MA twins: fleet_truth byte-identical + node-cache port. | **CONFIRMED+FIXED ~20**: [CRIT] fleet_truth stale watchdog `coverage{}` rendered as current green (the 07-19 fix covered signals[]+mini, missed the 3rd consumer — live-reproduced); [CRIT] oracle delivery classifier laundered `circuit_open`/`not_connected`/`no_lxmf_source` into the benign bucket AND shrank the tracked-ambiguity metric (now failure-set + producer-vocabulary lockstep test); [CRIT] propagation drill pull-leg transient read as DEFINITIVE store-and-forward failure (now tri-state: only a COMPLETED sync missing the message is False; `pull_link` stage = indeterminate); [CRIT] all-indeterminate absorbing state — silence leg never trips while envelopes publish (new sustained-indeterminate probe leg, ≥6 consecutive unproven fires page); [CRIT] drill INERT contract false (unconditional mkdir; probe now gates on gateway.json INTENT, fire script exits before any state write when unconfigured); node cache: `pki_status` TOFU/MITM baseline written-never-restored AND dropped by `_merge_node` (MITM detection structurally dead across restarts/merges — restored + routed through `update_pki_status`); loader dropped telemetry/hops/is_gateway/is_local/firmware/service_aspect+caps/position-ts/meshcore_* (field-complete round-trip test now pins the FULL to_dict shape); meshtastic/meshcore renames could never correct a stale cached name (self-reported flag now set on those legs + persisted); heuristic byte-smear names stripped of name-correction authority (`display_name_is_parsed` provenance); probe read cache keys the writer never emits (writer-derived-shape test added); `_save_parity_streak` silent-swallow witness; `mesh_to_rns_synth` paho attr-form false "not installed"; `_ci_cell` missing timed_out/startup_failure/action_required; wait_for_ci non-numeric arg → unbounded loop; claw probes' exception swallows now leave dispositions; drill child timeouts actually plumbed (F4); monotonic anchors in drill phases (F5); identity-file orphan prune (F6); worst-round pickers prefer definitive failure in all 3 copies (F7); spool alias ssh-argv/path validation + clock-backward clamp + worker-error IP masking + safe_import submodule non-ImportError guard; rns.txt prose contradicted its own pin. **RESIDUALS (documented, not changed)**: `is_gateway` one-way OR-ratchet (mixed-source views, flapping risk — deliberate); `probe_user_timer_unit_failing` slow-cadence (≳90min) blindness now stated in module docstring, cadence-aware window queued; waiver no-expiry/no-fire-path-witness (W5, queued); claw parser raise-through (W7 PLAUSIBLE, queued); stat-level EACCES→INERT class (W3 PLAUSIBLE, queued); pager second-distinct-claim-in-window no-edge residual; `_mask_ip_alias` "non-reversible" overclaim. **SECOND-BRAIN VERDICT**: deterministic core model-agnostic; model touchpoints are pinned-with-fail-loud-contract (cadence launcher, verified swap contract + tier-L fallback that can never forge the frontier verdict) or env-var seams (`MESHFORGE_ASSISTANT_MODEL` — default `claude-sonnet-4-6` likely stale, fail-safe to standalone); `review_provenance_check.py` frontier-prefix list is the one deliberate model-name coupling (documented, one-line update per frontier swap). |
| 2026-07-26 | **Pri-3 — second-brain taxonomy RATIFICATION, questions (a)(b)(d)** ((c) withdrawn by the author 2026-07-25, settled by code — not re-litigated). Scope: §1–§4 of `.claude/plans/second_brain_taxonomy_2026_07_26.md` (the argument half; §5 measurement spot-checked only), grounded against the T1 rule AS BUILT (`d11a141a` postdates the design): `detector_blind_any` seed entry + `_BLIND_DISPOSITIONS` read at source. | **Fable 5 frontier ratification**, adversarial: axis tested against incident classes beyond its three founding cases (#4 writer-no-reader → FALSIFIER via "what exercises the path"; #7 closed-enum → CHANNEL fail-dark; #5 deliberately outside — maintenance drift, not a lying mode); T1 attacked on meta-organ grounds and on its own UNIT axis (mini-tick streak resets on restart → blindness spanning a deploy under-counts → grace sizing is load-bearing). | this commit — RATIFICATION section appended to the design doc. **No code change, no design change required.** | **(a) RATIFIED + 2 annotations**: the axis is a lying-mode taxonomy for detectors, NOT a complete defect taxonomy (honest_failure_modes stays the write-time checklist; they compose); field assignment is not a partition (T2 diagnosed CHANNEL, cured FALSIFIER-shaped — composition, not error). Binding caveat: docstring fields are declarations (T4-exposed) — where mechanically checkable the field must be a TEST, default not option. **(b) RATIFIED**: not a meta-organ (one consumer of an existing per-tick score in an existing engine; the banned shape was a historian); the as-built 24 h grace SUPERSEDES the design's "order of days" (which would have been structurally near-mute against deploy cadence — the observed-tick streak resets on every mini restart, the accepted price of zero new machinery); `inert`-only exclusion correct CONDITIONAL on annotate-only — elevated to contract: ntfy promotion needs per-class review, and a chronically-blind-BY-DESIGN class gets fixed probe-side (reclassify to `inert`), never whitelisted in the rule; scoring `unknown` verified correct. **(d) RATIFIED**: T5 open = constraint adherence, not avoidance (its answer requires ADDING); any re-queue is an OPERATOR scope decision first; T5 falling outside the axis is evidence of honest boundaries, not failure. |
| 2026-07-26 | **Pri-1 Row 8 — `cross_gateway_dups_unsuppressed` BUILD vs ACCEPT ratification** (the queued frontier decision; the operator's 2026-07-19 ACCEPT reviewed at the rationed tier). Scope: the decision and its evidence base — `/fleet/dups` rollup re-fetched live, `probe_gateway_dup_degraded` + `probe_gateway_dual_homed_exposure` fire semantics read at source, mini live-rules wiring confirmed (`gateway_dup_degraded_any` present, human-only paging), the 07-19 inputs file + addendum. | **Fable 5 frontier ratification pass**, adversarial by construction: four attacks mounted on the ACCEPT (rate-decay via the growing unobservable population; static recipient-ownership as a "race-free" BUILD; receiver-side dedup middle ground; accept-goes-unwatched), each resolved against quoted live state or fleet incident history, not recalled claims. Inputs re-derived this session, superseding both the 07-19 file and the 07-25 row: `status ok`, `uncovered []`, 127+126 confirmed pairs, all dup counters 0, `dual_homed_recipients 3`, `unconfirmable_sent 15,936`. | this commit — verdict section appended to `.claude/research/cross_gateway_dup_design_inputs_2026_07_19.md`; eval case `oracle-cross-gateway-dup-accept-cost-asymmetry` (seed.jsonl, 34 lines valid); burndown row 8 annotated RATIFIED. **No code change — the deliverable is the decision record** (detector + leading indicator already shipped 07-19/07-20). | **VERDICT: ACCEPT RATIFIED — keep the detectors, do NOT build coordination.** The decisive ground is the time-invariant failure-cost asymmetry (dup = redundancy, visible; coordination misfire = silence, invisible — on emergency comms fail toward redundancy), NOT the rate. **The strongest BUILD counter was refuted with fleet-specific evidence**: static recipient-ownership (hash-assigned primary, heartbeat-gated failover) has no race window but relocates the loss mode into PARTIAL failure — active-but-wedged (#68/#72/#63), this fleet's dominant observed failure class — where the secondary suppresses while the primary delivers nothing; a heartbeat is a representation of delivery capability, not delivery (calibrated_claims #7 in protocol form). Receiver-side dedup refuted structurally (external clients, distinct LXMF source hashes, no join field). Accept-goes-unwatched refuted by live wiring verified this session. **Both populations stated, never averaged**: confirmable = 0 human dups ever (~27 d full 2/2 coverage, one infra fire ever, pre-classifier, deliberately unpaged); mesh = unknown-not-zero and growing (+12.9%/6 d) — the accept claims suppression costs more than the defect where we can see and cannot be validated where we can't, NOT that no mesh dups exist. **Revisit triggers named with first levers** (none is "design a protocol"): first human-class fire → re-decide with the instance in hand; material dual-homing growth → topology (single-home the recipient / RNS-leg-only scope where confirms exist); ACK consumption landing (#74 T2 step 4) → upgrades the evidence, not the direction. |
| 2026-07-26 | **Pri-2 — does an `indeterminate` disposition emit a false CLEARED?** ANSWERED **YES**, by reading `SignalTracker.update` rather than re-deriving the 18:22 journal. Scope: the signal-lifecycle layer every probe's output passes through (`watchdog_runner`), its two call sites, the state writer, and the Phase-2 restart decider. | Opus-class fix pass, red-test-first (13 tests red → implement → green, then 4 more added when self-review found the fix incomplete). Verified against the consumer of record: `--one-shot` run through the unit's own interpreter (`/usr/bin/python3 -m utils.watchdog_runner`, the real `ExecStart`), 57/57 coverage classes, exit 0, re-run after the module split. MA checked for the twin and has NEITHER `SignalTracker` nor the disposition/coverage concept — no port. | this commit — `src/utils/watchdog_tracker.py` (new, MF025 split), `watchdog_runner.py`, `tests/test_watchdog_unobserved_hold.py` (17), 3 existing tracker tests migrated. | **CONFIRMED, mechanism exact**: `update()` computed `newly_cleared = previous_keys - current_keys` and never received the disposition map, so a probe returning `None` because its channel FAILED was byte-identical to one returning `None` because all was well — the ~50 probes sharing the documented `indeterminate` self-guard could each emit a false CLEARED on going blind. **Cure**: `coverage` is now a REQUIRED keyword (honest_failure_modes #7 — a defaulted param would let call sites silently keep the wrong semantics; it duly broke all 3 existing tests), and only a POSITIVE observation (`clean`/`inert`/`active`, a small fail-closed allowlist) may clear. Everything else HOLDS. **Two findings beyond the queued question**: (1) the crashed-dispatch branch sets `signals=[]` + `coverage=None`, so ANY probe-dispatch exception previously cleared *every* signal on the box at once — same defect, widest blast radius, now holds everything; (2) **my own first cut was incomplete and self-review caught it** — I suppressed the CLEARED *log line* while `write_state` still serialised `signals` from the returned active list, so the signal kept vanishing from `watchdog.json` and mini/fleet_truth/`/fleet` would have read the recovery anyway. Fixing the symptom in the one place nobody consumes. Held signals are now RE-EMITTED carrying `extra.unobserved_hold=True`, preserving `first_seen` so a blind window cannot age-reset an hours-old wedge. **Two self-inflicted regressions caught before ship**: a per-tick WARNING for every held signal (a permanently-blind probe would have alert-stormed the journal — the exact volume problem the same day's alerting arc was cutting) → hold now reports as an EDGE like the other two transitions; and held signals reaching `decide_restarts` → new `observed_only()` seam, since **report-but-do-not-act** is the honest split (Phase 2 ships disabled, so this was latent — pinned anyway, a latent footgun armed by a fix is still armed). |
| 2026-07-26 | **SECOND-BRAIN TAXONOMY design pass** (Pri-3) — the ~8 organ families (58 probes / 57 signal classes, mini 67 rules, tier-L, claw, calibration ledger, cron-verdict layer, docs) against the prep artifact's six tensions. Plus the two open follow-ups the Pri-1 deletion pass queued into it (R2b claw-engine seed drift, R5 `tracer_peer_unreachable` fixed-or-blind). | **Opus-class** inline design pass, single-context. ⚠️ **TIER NOTE — this row does NOT claim the rationed top-tier pass.** `model_router --task-kind novel_design` recommends that tier *for this task kind*; I read that recommendation as a statement about the running session and stamped the row accordingly, which the upshift-witness gate correctly blocked. That is the design's own **T4 error — a representation read as a verification — committed while writing T4.** The design therefore wants ratification at the rationed tier; queued as its own row below. What IS defensible at Opus tier is §5, which is measurement, not judgment. Every §5 figure re-derived LIVE this session rather than recalled — coverage block off `/var/lib/meshforge/watchdog.json`, tracer fires off the state dir, claw rule ids off moc2, cron sweep across 7 boxes; one hypothesis (the 11 unversioned manager scripts duplicate existing probes) **REFUTED by reading their source** | this commit — design at `.claude/plans/second_brain_taxonomy_2026_07_26.md`. **Zero new organs, zero new files, zero new probes/signal classes** (the row's hard constraint). | **THE AXIS**: classify an organ by the falsifiability structure of its claim — **UNIT** (does it measure in the unit its phenomenon lives in?), **CHANNEL** (can the observation fail independently, and is that a third state?), **FALSIFIER** (what independent consumer exercises the same path, and does it share an ancestor?). Each field is derived from an incident, not invented; T2/T3/T4/T6 are instances of the three, not peers. **T1 ANSWERED — the score already exists and the prep's grep was looking for the wrong word**: `note_disposition` → `build_coverage` → `watchdog.json.coverage` is a complete per-class, per-tick, fail-dark record that all 58 probes already call. Reframe: *fire count measures the phenomenon, disposition measures the detector* — which is exactly why it satisfies the row's do-not-rank-by-fire-count constraint. It lacks only a TIME dimension (overwritten every tick; `/fleet` renders now). Live proof of cost found inside the spine: manager reads **38 clean / 15 inert / 4 indeterminate / 0 unknown**, and `kernel_reboot_pending` currently *cannot read the modules dir* — blind for an unknown duration, indistinguishable at every consumer from "no reboot pending". Proposed change is ONE mini rule (mini already ingests watchdog.json, already rotates history, and already has the correct debounce), not a coverage historian — the meta-organ the row forbids. **THE CONSOLIDATION**: six independent implementations of "debounce, confirm, re-alert, witness" (per-probe 2-tick / #78 cadence-confirm / mini `grace_s` / `cron_verdict_freshness.sh` REALERT_S / `fleet_offline_check.sh` REALERT_INTERVAL / rtun consec-counter) — honest_failure_modes #5 at ORGAN scale; two of the six were fixed separately this arc without either noticing the other four. **mini's `engine.py` grace is the reference implementation** (observed-tick, streak-reset, daemon-downtime-aware); R1 spent a commit re-deriving a weaker version of it. **R5 VERIFIED fixed-not-blind** (1015 retained fires, 8831 ok / 0 non-ok, newest 3 min old, 9/9 peers incl. moc5). **R2b CONFIRMED + sharper**: moc2's `collector` role maps to the *mini* seed, so the probe reports `clean` on `mini_dudeai_rules.json` while both claw rules files are read by nothing and no `claw` entry exists in `_ROLE_TO_MINI_SEED` — coverage claimed by ADJACENCY, the FALSIFIER field — but measured ID drift TODAY is **zero** (claw-01 7 ids = 5 seed + 2 legit box-local; claw-02 exact 5), so a structural gap with no live harm. **T6 MEASURED**: 11/11 manager cron organs UNVERSIONED (incl. `backup_rotate.sh` + `memory_git_sync.sh` — the two you need *after* losing the box — on the box that has hard-reset 8×; and `mini_honest_fire.sh`, an independent fail-closed auditor of mini, the T1 falsifier pattern already built and least durable), plus `power_capture.sh` on 5 boxes (md5 identical, no drift) and 2 one-offs. ⚠️ **Do NOT bulk-commit them** — MF014 operator values (ntfy topic, `~/` paths, box names); templating job, scoped not done. **NOT DONE (deliberate)**: the T1 rule is designed not built (live-alerting change, operator sign-off); T6 versioning; R2b fix (drift is zero, no blind ship); **T5 left open** — the only tension whose answer would require ADDING, and the row says consolidate. **COMPOSES WITH Pri-2** (`indeterminate` → false CLEARED?), which is now load-bearing: T1's score reads the same dispositions. |
| 2026-07-23 | **48-HOUR AUDIT (operator-requested QC)** — the full unreviewed 37-commit / ~8.4k-line window `0a85896d..c17c611c` (2026-07-21→23): second-brain arc WS-C/D/E/A (model_router + consumers, mini probes, parity byte-locks, eval cases), dude-claw WS-1/2/3 (claw-02 instance, multi-claw NOC, battery isolation) + deploy scripts (fleet_sync --memory-only, post-commit hook), witness arc (host_probe failover + fleet_truth fan-out), and the 07-21 fix commits re-reviewed per feedback_review_your_own_fixes (incl. the a0b4e8d3 review-fix commit itself). **Also ran the queued Pri-2 model_router adversarial design review** (frontier tier as required). | 5 parallel frontier code-reviewer agents (model_router design+code / witness / dude-claw+deploy / mini-probes+parity / 07-21 re-review) → ~45 findings incl. 1 CRITICAL + 8 major; each fix chunk self-verified + suite-verified; ground-truth battery before AND after (lint --all, full pytest, parity_check, CI check-runs) | MF `dac09029` (security), `17611b07` (deploy CRITICAL), `d85f1323` (claw identity), `78ee633f` (host_probe), `0fa21d9b` (model_router), `15e02ad7` (mini probes + adapter), `44b2e067` (07-21 chunk + MF025 propagation split), `722c5339` (scout pin update); MA ports `ea7ac54f`, `ec9955f3`, `65fc339c` (byte-locked twins, parity in sync) | **CRITICAL FIXED**: fleet_sync claw@ restart guard could never match an instance unit file → claw02 ran OLD code forever behind PASS no_unit (#79 re-opened, masked; verified live vs systemd getty@tty1 analog; guards now template-aware + instances enumerated, hardcode banned by test). **SECURITY**: operator LAN/AREDN IPs in tracked source (incl. an in-window docstring leak in host_probe_check.py; MF014 structurally blind to the class) → sanitized repo-wide + MF014 operator-local deny-patterns file (untracked) + 7 tests; residual: IPs remain in git history + 756e9f2b's commit message (unrewritable — burned). **MAJORS FIXED**: model_router F1 tier-filter (haiku rows would ground "tier-L competence" the moment the head-to-head runs) + F2 comparability floors (not_comparable status) + F3/F6 asserted-gate/founding-ts verdict integrity; mini dream-probe unreadable-deltas→clean (#60 class), #78 mutual-deferral unwired-cadence leg, byte-locked files carried MF adapter values (dual-stack MA attributed MF's HEAD to MA claims → _util APP_* adapter seam + parity shape-pin); propagation drill day-one-broken absorbing state → never_published escalation leg (MF025 fallout: propagation probes split to watchdog_probes_propagation.py); host_probe string-witness silently probed from the wrong subnet + blind-instrument failover block + dark-claw timeout stacking. Plus ~20 minors (claw %i instance default, one-discriminator pusher gate, instance rules-file promotion path, tick/mini no-timestamp honesty, merge-sweep favorite/is_local/relay completeness, activating≠FAILED spool cell, scout clock-step witness, ledger flock, orphan-verdict witness, launcher stderr witness, STATE_DIR pin, invariant test-pins). **REFUTED**: E-F10 claude_assistant top-level cache_control (documented automatic-caching param per current API docs); parity_drift fire on fleet_truth (port landed minutes later — deploy lag, not drift). **DESIGN FOLLOW-UPS (queued, not silently dropped)**: router taxonomy gaps (no cadence-ratification/code_review/memory-authoring kinds; capability_gap recommends the ceiling instead of defer/queue for frontier-shaped work; fleet==field ceiling vacuity masks env-detection degradation), ladder-walking escalation (consult api_small eval before assuming opus), verdict inputs beyond eval stability (cadence witness), fan-out alias-join witness (honest degrade, accepted), declared-secondaries registry (structural-dark class), battery_soak exit-code leg (text-loud accepted), crontab --gate literal vs DEFAULT_EVAL_GATE (operator-side), MESHFORGE_ROUTER_ENV knob name + _read_deployment_role("meshforge") in byte-locked model_router (works, MA-noisy — adapter candidates). |
| 2026-07-26 | **Pri-1 POST-07-23 WINDOW — full unreviewed range `c17c611c..HEAD`** (84 commits / 140 files / ~6.2k non-test code lines, re-derived at review time — supersedes the queue row's 75/12,240; the 5 commits the 07-24 scoped row reviewed excluded, its unreviewed fix commits `41f34b4b`+`4c840cb5` INCLUDED per convention 3). Plus: re-review of the prior session's fixes `2376e20c` (F1/F2) + `a196caed` (gate precision), and the queued **F3** design decision on `review_provenance_check.py` leg 3. | **Fable 5 frontier pass**: 4 parallel adversarial code-reviewers (host-probes / signal-lifecycle+fleet-truth / gateway+claw / scripts+mini+deploy) → ~44 findings (1 CRIT, 13 MAJOR, rest minor/nit); every acted-on major independently re-verified by the orchestrator against source (2 agent live repros + 1 live systemd drill quoted); 4 fixer agents with decided-design specs, red-first throughout (pre→post: 9F→15P, 23F→35P, 20F→28P, 32F→100P); every fix diff re-reviewed same session, the 9 riskiest hunks line-walked (feedback_review_your_own_fixes); MA twin ports same session. | this commit (MF) + MA twin-port commit (byte-locked fleet_truth/history/model_router/cadence_fallback + diverged-twin C4/C5 shapes) | **CONFIRMED+FIXED ~30 (+~80 tests)**: [CRIT] `meshforge-map.service` `${ip%.*}` eaten by systemd env-expansion → derived-CORS dead on arrival, live-drilled both ways → `$${ip%%.*}`. **STATE-FALLBACK CLASS** (= the 2376e20c re-review verdict: direction right, incomplete on 3 axes): a readable-but-STALE state file bypassed the new in-process fallback in `_load_parity_streak`+`_load_hist_state` (ro-remount/ENOSPC keep the old file readable; drill 0/10 fired) → memory-first, disk = restart-survival only; `probe_memory_cap_engaged` shipped with NO fallback (unwritable path → one historical kill re-fires FOREVER + ceiling debounce permanently silenced) → same pattern + #63 witness; `_save_parity_streak` witness upgraded, false "CANNOT reach threshold" text corrected. Fast cron that FAILs once then dies was NEVER flagged (unconfirmed branch skipped the silence leg) → stale-threshold check inside the branch. **Tracker restart dropped every HELD signal** (state was process-memory; deploy sweeps restart the watchdog #79) → rehydrate from prior `watchdog.json`, typed validation, first-adoption-wins, one-shot moved off the live path (`watchdog.oneshot.json`). **Partial-blind multi-subject probes falsely cleared unobserved subjects** (active stamped before noted-indeterminate) → coverage `partial:true` + tracker holds (repro-pinned). `extra.unobserved_hold` had ZERO consumers → fleet cell + dimmed chip + "last-observed, not current" tooltip. **Synthetic "[delivered]" emitted on a propagation hand-off** (state SENT ≠ recipient proof) → gated on DELIVERED; SENT resolves via timeout sweep (#16 posture — ack vocabulary has no honest hand-off kind and a novel kind would bypass `_SYNTH_ACK_CONTENT_PREFIXES`). **HEALTHY→DEAD on ONE transient failure** after a quiet week → two-evidence rule (age gate AND fail-streak ≥ threshold). **Frozen battery series served stale trend as current and kept soak `expected` suppressing a REAL drain** → tri-state `append_sample` (written/duplicate/unusable/io_error) + ERROR-once witness + stale-series gate (live-reading-only classify, `expected` forced off). **gen_fleet_hosts accepted ANY UDP reply** (fixed repo-public QID, no QR/RCODE check; SERVFAIL parsed as "authoritatively no A"; off-path poisoning of a fleet /etc/hosts write) → random QID + QR/RCODE validation, SERVFAIL=not-an-answer; **`--apply` overwrote a good current entry with the registry snapshot on one lost datagram** → per-name HOLD-current (source `held`, witnessed), NEGATIVE-only fallback, answered-empty doesn't stop the server loop, torn-block repair preserves operator lines, fsync-before-replace. model_router `capability_gap` fired on a session RUNNING the escalation target → `l_gap_unresolved` (gap only when the env ceiling clamped it). **F3 (gate design, frontier call)**: leg-3 boundary = newest commit ADDING a completed-table row (closing), so QUEUING debt no longer silences the nudge about it; no-closing-row fallback = oldest walked (over-fires, never goes quiet); red-first + live boundary resolves past the queue commit. Minors: `_prior_verdict_statuses` ISO validation (garbage line seeded a prior FAIL), disk-path boot-mismatch streak reset, propagation held-reason honesty, host-module blanket guards (one raise killed the whole tick's signals), `cleanup_stale` witness derives from UPDATE-affected rows (RETURNING ≥3.35 + per-row fallback — a concurrently-delivered row was witnessed as DROPPED), `_resolve_heard` unparseable→`(None, False)` + absent `lastHeard`≠live-packet, backward-ts series rows dropped not re-sorted (code now matches its own docstring), rtun DEAD counted only on observed failure + honest observed/unobserved log + 64KB cap, lint sudo-home (deny-patterns file silently OFF under sudo) + tab-split-first, history flock LOCK_NB+bounded retry+witness, promote_seed_rules OSError-widened + backup-suffix skip + instance-list witness, selftest unmapped-pkg → named WARN never guessed FAIL, ntfy ack GET→`http` POST action (prefetch-forgeable receipt closed), cadence retrieval-FAILED ≠ absence, 86400/TRIAGE_FRESH_S test-pin, `timeout 60` on all 3 remote promote calls, `_fetch_peer_claw` cap+1 read + distinct warning, future-stamped tick → skew-unobservable not fresh (mini+claw blocks), fleet_snapshot cadence-fallback one-shot warning + `cadence_source` field. **REFUTED**: "four signal classes never note dispositions" — a grep-pattern artifact (multi-line calls + `cls`-variable); **my own confirming grep repeated the reviewer's method — a same-method double-check is not verification** (feedback_verify_the_verification, live instance); AST audit 57/57 covered; AST-based adoption-gate test added so the false zero cannot recur. **RESIDUALS (documented, not changed)**: repo `scripts/meshforge-map.service` is NOT the live unit — `/etc/systemd/system` copy predates both the MF014 IP fix and the D1 fix, both land only on the next unit install (operator action); propagation `no_result_since` forward-NTP inflation (fleet-wide persisted-wall-clock class); prop-state one-file-two-schemas (mutually-exclusive legs today, collision shape noted in-code); spool `activating` genuinely ambiguous at a point-in-time read (wording now names both readings; probe-side owns crashloop paging); rtun HEARTBEAT alive-vs-healthy conflation (consumer redesign, commented); `dream_ratification_stalled` deliberate-retirement leg + `local_brain_regressed` ts-less newest-record (PLAUSIBLE, queued); selftest §1/§4 invoker-interpreter residual (warning-severity by design); leg-3 scoped-closing-row + annotation-edit limits (documented in module docstring); D10 unmapped-but-genuinely-missing core dep now WARNs (named, prompts the mapping) rather than FAILs from a guessed name. |
| 2026-07-28 | **Operator-requested adversarial review of the past-24h session window `3afda33a~1..HEAD`** (11 commits: honest_status fleet-list/suite-leg hardening, `pytest_tmp_prune.sh`, /fleet/dups role-evidence, 4 shell harnesses), explicitly flagged for over-engineering. | **Fable 5 frontier pass** (`/code-review`, 8 finder angles in a background fork): ~40 candidates deduped + adversarially verified to 10 survivors; CONFIRMED items quoted live checks (`systemctl is-active` rc, `${ninternal:+}` expansion reproduced, both LOCK_TIMEOUT copies read as 259200, `fleet_pull.sh` parse compared). Meta-pattern: the window's fixes repeatedly RE-COMMITTED the class they cited — broken-mapped-to-absent inside the check of record (failed watchdog unit and git-erroring box both shrank denominators to PASS), a session-bus proxy swapped in for a self-confirming signal, and the anti-drift fix hand-copying the SSOT chain it existed to unify. | **`f7811c02`** (all 10 findings; fixes verified by full suite 9854 passed/1 skipped exit 0, lint exit 0, live root-uid `operator_cron_wired`=True on the manager, `honest_status --quick` end-to-end 9 boxes). ⚠️ **SHA corrected 2026-07-29**: this row originally cited `bfc1eba7`, which is **not an ancestor of `main`** — the commit was amended and the content landed as `f7811c02` (`git diff bfc1eba7 f7811c02` = this provenance line only). A pass computing coverage from the old SHA would have found nothing. Cite reachable SHAs; ancestry is not coverage, but an unreachable SHA is not evidence either. | **Fixed 10/10.** Notables: honest_status watchdog leg now splits LoadState not-found (absent organ) from loaded+not-active (FAIL — the #82 class read GREEN before); SHA leg keeps a repo-present git failure in the denominator as UNKNOWN; crashed suite (rc!=0, no summary) stays FAIL; `^INTERNALERROR` anchored + numeric guard; `scripts/lib/fleet_hosts.sh` is the ONE resolver (honest_status + fleet_pull source it — HOME-unset skips user tiers, `#` comments stripped identically); `operator_cron_wired` scans the whole spool (no min-UID bus heuristic), direct first-party imports, 300s monotonic cache; dups fallback names the status instead of asserting "<2 gateways"; prune spoken-FAIL exits 0 (exit 1 reserved for verdict-unrecordable), runtime `_pytest` derivation deleted (pinned constant + existing drift pin). **Residuals (documented, not changed)**: token-in-crontab coupling remains the role evidence — `fleet_roles.yaml` declares SERVICES, not crons, so there is no role-SSOT leg to consult; the wrapper-script caveat (keep `# fleet_dup_collector` on the crontab line) is now in both docstrings. ~10 other in-tree copies of the fleet_hosts chain (fleet_status/fleet_sync/mini rollup/dup_collector) not yet converged onto the lib — only the two per-repo-tier consumers were in scope. MA twin ports pending (honest_status is UNTRACKED-diverged tier; check which way each fix travels). |
| 2026-07-29 | **Ultra cloud review of the post-07-28 window** — target computed as `f7811c02~1..f51f5106` (13 commits, 33 non-test files), NOT the naive `65132236..f51f5106`: `65132236` is exactly `3afda33a~1`, the start of the 07-28 pass's own window, so 11 of those 24 commits were already covered. Delivered through the tool's real interface via a throwaway replay branch `review/post-0728-window` (13 commits + the scoping doc replayed onto `0e11cfae` as new SHAs, so `merge-base` with `origin/main` = the range start; tree verified byte-identical to `c43f42bb`). Scoped in advance by `.claude/plans/review_scope_2026_07_29.md` (risk ranking, per-file questions, 2 pre-identified candidates, 5 do-not-flag residuals, author's declared conflict on `f51f5106`). | **Ultra multi-agent cloud pass** (54 files, +4,674/-329): **10 findings — 2 normal, 8 nit. ALL 10 CONFIRMED against the code before any fix** (each verified by reading the named lines, not accepted on report). | THIS COMMIT (`tests/test_review_fixes_2026_07_29.py`, 18 guards; full suite 9996 passed / 1 skipped exit 0, lint exit 0, parity in sync) | **Fixed 10/10, with 1 PRESCRIPTION REJECTED.** The two `normal`s were the real ones: (1) `claw_telemetry._watch_verdicts` called `logger.warning` in its except handler and `logger` was never defined in the module — the WITNESS raised NameError from inside the handler, escaped, killed `build_tick` and (since `claw_metrics_push` only guards NatsError) would have taken the capture cron down. The handler written to keep the tick alive was the one thing that could kill it; now uses the package's own `log_warning`. (2) `fleet_offline_check.sh` ran `set_state ... alerted=1` UNCONDITIONALLY after the `ntfy_push`-or-`echo` fallthrough, so a first page lost to a brief ntfy.sh dip was never retried — the next tick skips the first-alert branch and STILL-DOWN waits out the full interval: a REAL outage silent for 1 h (2 h quiet tier), inside the monitor whose 06-17 hardening promised a page can no longer vanish. Delivery now gates the state on BOTH branches. Nits fixed: `SIGNAL_CLASSES` listed `claw_uplink_node_moved` TWICE (set-equality gate is blind to multiplicity; `merge_coverage` double-counted, so every `/fleet` cell read total=59 over 58 keys — gate now checks count too); the uplink probe's debounce tick noted `clean` while its own reason said drift was seen (and `clean` is in the tracker's `_CLEAR_PERMITTING_DISPOSITIONS`, so a debouncing tick could clear a held signal) → `indeterminate`; only `moved[0]` was emitted so additional drifted uplinks vanished exactly when a router reboot moves every lease → folds all movers like its sibling; the selfheal `flock` fell through on TIMEOUT via an or-`true` guard → refuses loud; `Optional` unimported in the firewall rewriter (latent under PEP 563) → imported; shell resolver's `tr` lacked `\r` so a CRLF `fleet_hosts` gave shell and Python DIFFERENT fleets → fixed; uplink tests read the host's real `/etc/nftables.conf` → pinned. **REJECTED prescription (bug_003)**: the review said delete the guarded `mini_dudeai` import in `watchdog_probes_claw_watch`. Its DIAGNOSIS was right (the author's "it's test-pinned" claim was imprecise), but deleting the guard breaks the module on a box without the mini package and contradicts the deliberate convention in `watchdog_probes_mini.py` (4 instances, each commented "mini package absent in some contexts"). Fix instead makes the fallback OBSERVABLE: `_VERDICT_CONSTANTS_FROM_FALLBACK` + a test asserting it is False wherever mini imports. **⚠️ TWO OF MY OWN GUARDS WERE VACUOUS and only the red drill caught it**: `test_build_tick_survives_a_failing_gate` passed `"watched:"` as the lora payload, which `parse_lora_stats` rejects, so the patched gate was NEVER CALLED (fixed: real firmware wire format + a call counter + a fixture-still-works guard); and the CRLF parity test used `subprocess(text=True)`, whose universal-newline translation ERASED the `\r` it existed to detect — the harness destroying its own evidence, the 07-25 self-confirming-checker lesson in test form (fixed: `text=False`, assert on bytes). 13 of 18 guards now fail on the unfixed code; the other 5 are meta-guards or paired with a source-level pin. **Residual**: `fleet_offline_check.sh`'s RECOVERED branch has the same unconditional-`set_state` shape but a lost recovery page self-corrects on the next tick, so it was left alone deliberately. |
| 2026-07-31 | **Adversarial pass on the post-07-29 window `e5a76bcc..73b4768c`** — pytest-rc trust arc, claw RF-watch segment gate, cron evidence capture, peer-RF probe (~3,873 diff lines, every hunk read by the line-scan finder) | **Fable 5 `/code-review` background fork**: multi-angle finder sweep (Angle A line-by-line + Angle G altitude ran as dedicated agents; C/other angles inline) → candidates deduped + verified by the coordinator against the live tree → **10 CONFIRMED findings**, ranked (full JSON + below-cut minors in the fork's final report). Top of rank: `calibration_reverify.sh` missing `[ -x ]` guard can mint false 'broke' ledger verdicts (rc 126/127 → code_exit=1); CI bare-rc false-green at the highest-authority gate; claw segment gate reads only an env var nothing sets while its template says copy-to-a-path-no-code-reads; peer-RF journal needle zero-padded vs unpadded `%x` logs; cron_verdict excerpt poisons harness_audit's whole-line greps. | `957fee5b` (finding 2 FIXED: CI 'Run tests' step now judged by `scripts/pytest_verdict.sh --log --rc` with rc captured under GitHub's `bash -e`; flap/green/red simulated locally 1/0/1; `TestCIPytestVerdictContract` pins classifier call + rc capture, guard verified to bite by reverting the fix) | **Findings 2 (`957fee5b`), 1 (`1007fee5`+`36a6a438`), 7 and 10 (`e02a227e`), 4, 5 and 6 (peer-RF trio, `f2b4bcee`), 3 and 9 (claw pair, `d895e331`), 8 + ultra nit 2 (ledger batch, this push + MA twin port) FIXED — **ALL 10 LOCAL FINDINGS AND BOTH ULTRA FINDINGS CLOSED.** Ledger batch: producer travels in a new marker `instrument` field (honest_status + calibration_reverify writers updated, both repos), `summary` stays a message; summary-only markers attributed as `unattributed marker (…)`, never dressed as a tool name; `record_annotation` drops reserved keys from `extra` (forgery pin proves `{"kind": "verdict", "outcome": "held"}` can no longer mint a hand-written verdict). Byte-locked source+test ported to MA same session, parity in sync, MA suite 37 passed.** Claw pair: finding 3 — `_load_segments` now defaults to the template's advertised path (`~/.config/meshforge/claw_watch_segments.json`; live moc2 verified BEFORE the change: its env sets the var explicitly to that same path, so production behavior unchanged), plus the aggravator (nodes declared with no claw segment anywhere = declared-but-unusable, loud); finding 9 — segment-config errors are RETURNED not raised: watch leg withheld (never judged ungated — that restores the 07-30 false-silent), battery/liveness captured, tick persisted with a `segment_config_error` witness, run exits FAIL at the END naming the cause. 9 new pins; autouse fixture pins `DEFAULT_SEGMENTS_PATH` off the real box for the whole module (ambient-state rule). Peer-RF trio: finding 4 verified against the LIVE journal before fixing (`from=0x2ecc800` — 7-hex unpadded on this box) — needle now strips padding + lookahead-bounds the match so a 7-hex needle can't prefix-match a longer originator; finding 5 — clock-backward now travels as `parse_error` (blindness) not `never` (silence-qualifying), and the old unit test that PINNED the broken shape asserts the opposite + a new end-to-end pin at the verdict layer; finding 6 — state file gets the streak's memory-first pattern (no repeated 9-h re-seed) and write failures are witnessed on every emission. Revert drill: 5 pins fail on the old module. Also: the finding-7 poisoning tests were killed on CI runners by `set -u` + the `/opt/meshforge` default repo path (run 30672255064) — now pinned to the checkout via `MESHFORGE_REPO`; diagnosis proven locally with a nonexistent repo path. Batch also closed ultra nit 1 (vacuous prune test → `CRON_VERDICT_TS` injection, exact-count + newest-survive asserts). Finding 7 fixed at the CONSUMERS (harness_audit both legs, avatar-seed restart gate, mf5_soak_watch counter — structural field-2/field-3 parsing, never whole-line grep); pins drive the real harness_audit.sh via `CRON_VERDICT_LOG`, column-positional asserts (two of my substring asserts were vacuous against the old code and were caught by the revert drill). (Ranked set preserved in the fork output, task a79f048929b3e4e80.) Finding 1 repro'd live before fixing: unguarded script exits **0** (cron reads OK — fail-dark) while minting `broke` with an empty instrument string; guard = classifier rc ∉ {0,1,2} → infra exit 3, no verdict written; 3 sandbox-repo tests (`test_calibration_reverify_infra.py`), red-verified 2 failed on the unguarded script. **Follow-up from finding 2's first live CI run**: the classifier itself miscounted pytest's 8-char-padded log-level lines as verdict lines (203 in a 10000-passed run) → single-space discriminator fix `18af17df`, verified against the real 30,930-line CI artifact both ways, CI green (run 30670384186). **Ultra cloud review LANDED** (same arc, branch `pytest-rc-review`): **2 findings, both nit, no overlap-conflict with this set** — vacuous `test_prunes_to_keep_limit` (1s timestamp granularity collapses 6 captures to 1, prune loop never exercised; adjacent to local finding 10) and `record_annotation` `rec.update(extra)` can override reserved keys incl. `kind`/`outcome` (hand-written-verdict hole, convention-guarded only). Both queued with 3–10. **Live-drill addendum (recorded 07-31 evening, reconstructed from the verdict log — the drill itself left no provenance note)**: harness_audit's FAIL leg was live-drilled 08:00 HST same morning — `core.hooksPath` set to the self-describing `.githooks-drill-absent`, audit caught it (13 PASS 1 FAIL), the FAIL verdict named its cause + preserved `cron_out/harness_audit.fail-2026-07-31T18:00:12Z.out`, mini's `cron_verdict_stale` paged 44 s later (08:00:56), restore + OK re-verified 08:02:53 (verdict-log lines 17:59:51Z/18:00:12Z/18:02:53Z) — the FAIL path of the structural field-2/field-3 parsing + cause-naming (`331ad4e3`) proven end-to-end through the real cron consumer chain. |
| 2026-07-31 | **Re-review of the same-day fix window `957fee5b..8c7a3a20` + MA twin port `c119b717`** (feedback_review_your_own_fixes: 8 self-applied fix commits, self- and CI-verified but read by no independent pass) | **Fable 5 background fork**, 4 lenses run directly (line-by-line correctness / incomplete-cure sweep / fix-introduced regressions / test honesty); every candidate verified against the live tree, one by LIVE DRILL → **2 CONFIRMED + 3 PLAUSIBLE**. Checked-and-cleared list recorded in the fork output (task aea5bf3e608b78f8c) — incl.: no code parses verdict `detail` strings (instrument format display-safe); MA's `fleet_aggregator` verdict parser already structural + ts-validated (MA not excerpt-poisonable); poisoning tests' column indices correct for both leg-name shapes. | this push (MF) + MA port commit | **CONFIRMED-1 (top)**: MA's `cron_verdict.sh` was byte-identical to MF PRE-fix — finding 10's prune glob lived on in the twin, PROVEN by live drill (seeded `sync.extra` capture reaped by a churning `sync` through MA's real script); `e02a227e` never travelled because cron_verdict is untracked-diverged tier and the day's port covered only the ledger → ported now (script hunks + retention-test fixes; the poisoning test class deliberately NOT ported — MA has no harness_audit.sh, note left in the test file). **CONFIRMED-2**: my finding-7 rewrite of the avatar-seed gate passed VACUOUSLY on an empty verdict line (awk over zero input exits 0) — restart still blocked by the date check but with a misleading STALE page; explicit `[ -z ]` guard added. **PLAUSIBLE fixed**: `!00000000` peer strips to needle '0' and matches `from=0x0` local-inject lines (permanent false HEARD) → rejected at load_peer_config validation + pin. **PLAUSIBLE documented, not changed**: prune regex escape covers only the sanctioned charset (a hand-wired name with other ERE metachars accumulates its own captures — degraded direction preserves evidence); template-copied-with-placeholders files at the new default path now activate the gate (intended semantics; placeholder nodes inert, template carries claw_segment). |
| 2026-08-03 | **NOT A CODE REVIEW — logged because the fix commit `1334a84c` (+ MA twin `fc7ccfaa`) claims verification and the gate correctly demanded a witness.** Scope is ONE string: the `_SYSTEM_PROMPT` ratifier rubric in `src/mini_dudeai/cadence_fallback.py` (byte-locked MF↔MA). Nothing else in the range was read adversarially, and **this range must still be treated as unreviewed by the next pass.** | **Controlled empirical A/B, not a review.** `triage-ratifiable-decision-tell` + `triage-ratifiable-documented-lint-rule` × OLD/NEW prompt × 3 reps, sequential on an idle box, same corpus throughout, only the prompt varying; conditions verified by marker-presence before each block and the working-tree version restored via `trap … EXIT INT TERM`. Retrieval checked separately with the real `retrieve_context` (tier R, no LLM). Plus a same-cases Ollama-vs-Haiku comparison (24 cases, separate ledger, no cursor). | `1334a84c` (MF) + `fc7ccfaa` (MA) | **CAUSAL**: OLD 1/3 passing and needing all 3 attempts even then; NEW 3/3, mostly try 1/3. **REFUTED — my own untested assertion**: I had claimed the lint-rule case's 1251s/try-3/3 was box load rather than my change; measured try 1/3 in ALL SIX reps across both conditions (63–162s), so load confirmed, not asserted. **NOT a retrieval gap** (a hypothesis worth recording as killed): `[Errno 24]` verified present inside the 700-char clamp in both root sets, so the model had the evidence and withheld anyway — the defect was prompt balance. Precision held (`triage-mixed-ratifiable-and-wrong` still try 1/3). **RESIDUALS**: (a) Ollama FAILED `triage-four-mixed` by `TimeoutError` on the largest-backlog case — an operational fragility that scales with the thing tier-L exists to handle, NOT a wrong answer, and unaddressed; (b) Haiku failed `triage-ratifiable-calibration-discipline`, marking `pass_at_1_is_not_reliable` as `looks-rejectable`; (c) accuracy tie at 23/24 each with ~10× wall-clock and 4.7 GB + 3.6 cores of local cost for Ollama — the tier-ladder decision is OPEN and deliberately not taken on a 24-case, n=1 sample; (d) these eval cases were authored alongside the Ollama tier, so it is arguably home turf. |
| 2026-08-06 | **THE REVIEW-PROVENANCE HARNESS ITSELF (operator-requested reliability audit)** — this ledger file, `scripts/review_provenance_check.py` (all 3 legs + parsers), the pre-push wiring, and the MA twins of both. | **Fable 5 frontier inline pass, evidence-first**: the file re-parsed through the gate's own `parse_completed_rows`; every hex token swept for `main`-ancestry (147 MF, 18 MA); `~/upshift_witness.log` + calibration ledger read; `git log -S` forensics dating each damage; the legs' live-firing history QUOTED rather than re-drilled (leg-1 blocks 08-03, leg-2 block 07-31, leg-3 advisories ×15 on 08-05 — all three legs proven live by the witness log, evidence from outside this session's authorship); red-first tests (10 red → implement → 64 green MF, 63 MA). | this commit (MF) + MA twin commit same day (gate + tests ported, MA ledger SHAs corrected) | **CONFIRMED+FIXED 5 ledger defects**: (1) a 2026-07-25 line-merge (`b56b06af`) overwrote the SECOND-BRAIN TAXONOMY row's leading cells — 36 recorded passes, 35 parseable rows, invisible for 12 days; restored verbatim from `6768ca09`. (2) literal pipe characters in the 07-29 row's prose (quoted shell) truncated its fix/residual cells — reworded. (3) cited-but-unreachable fix SHAs: MF `d8d2c247`, MA `0afc8add` + `60437c38` — with the known `bfc1eba7`, FOUR instances of ONE mechanism, now named in convention 4: amending the provenance row INTO the fix commit changes the SHA the row just cited; cure = cite "this commit" for the carrying commit. (4) both queued Pri-1 rows missing their Pri cell (2 cells vs 3). (5) 13 blank lines splitting the GFM tables — most rows rendered as plain text, not table rows. **GATE HARDENED**: new structure leg — a NEW malformed table row (merged line, stray pipe, wrong cell count) BLOCKS the push with a reword remediation, pre-existing damage warns, and the live file is pinned well-formed by test; leg 3 now counts `scripts/` + `.githooks/` too (a src-only counter was blind to the enforcement layer where BOTH recent Pri-1 arcs lived); an internal-error pass-open now leaves a witness-log line (was fail-dark); an unresolvable leg-3 boundary warns instead of silently skipping. **TWIN DRIFT FOUND+CLOSED**: MA's gate was a pre-07-16 generation — no F3 closing-boundary fix, no ISO-date false-block guard, no negation awareness; those fixes never travelled. MF's gate + test file ported wholesale (the MF-only taxonomy-row pin adapted out). **VERIFIED WORKING, not changed**: all three legs fire live (witness log); the current 1288-line leg-3 debt is 100% covered by the queued GUARD-SPINE row — 0 src+scripts lines outside queued ranges since the 08-03 boundary, so the repeating advisory is by-design over-fire on queued-but-unreviewed debt, not a coverage gap. **REFUTED**: "Ultra cloud pass" mechanism text not matching FRONTIER_TIER_MARKERS is not a leg-2 hole — the ultra pass runs in the cloud with external task-id evidence, the local session merely records it. **RESIDUAL (documented)**: leg 2 reads only the Mechanism cell; whole-line frontier scanning rejected (scope/residual cells legitimately discuss the frontier worklist — false-block cost); hex-only English words can pose as SHA tokens in leg 1's witness scan (harmless without a real-SHA prefix collision). |
| 2026-08-06 | **meshing_around_meshforge + meshforge-maps — reliability, security, MF-port-gap audit (operator-requested)**. Both repos frozen since 07-23 while MF landed the 07-19→08-06 arcs. Audited: every unreviewed commit since each repo's last pass (client `a791448..bf64662`, maps `28542c6..843a3d5`) plus each repo checked against the post-freeze MF hardening classes (pytest-rc trust, inert-checker, backup-overwrite, subscriber witness). | **Fable 5 frontier inline pass, drill-first**: both repos' own checkers DRILLED with planted violations before being trusted (client config-atomicity lint fired exit 1 on a planted non-atomic write; readme_stats fired exit 1 on a corrupted sentinel, restored exit 0); pytest-rc surfaces read at source in healthchecks, CI workflows, and hooks; both suites then run end-to-end through the newly ported classifier; revert drill on the maps fixes (4 of 5 new tests red on the stashed pre-fix tree). | client `6695730`, maps `6cc0d50` (both landed beneath this row — convention 4) | **CONFIRMED+FIXED 4**: (1) client healthcheck's test leg piped pytest through tail, so the gate tested TAIL's exit code — and finding (2) proved the consequence live: (2) `test_message_reception_from_live_broker` carries an internal 30 s wait equal to healthcheck's `--timeout=30` harness budget, so a reachable-but-quiet broker killed the WHOLE suite with no summary — the old gate printed "Tests passed" over that crash; the new classifier's FIRST run caught it (FAIL "suite crashed before reporting"). Wait now 15 s; suite 1126 passed exit 0. (3) maps `_on_message` generic-except counted processing BUGS into `_decrypt_skipped`, the benign encrypted-traffic bucket — 100%-dead processing indistinguishable from normal skips in get_stats (the 07-09 third-twin pass ported only the key gate; this was its documented residual, now sharper and closed): separate `processing_errors` stat + 1/min-throttled warning. (4) maps `create_backup` overwrote an existing destination in place (the 08-05 destroyed-backup class): O_CREAT-O_EXCL atomic reserve, loud refusal, failed backup removes its own partial file. **PORTED**: `scripts/pytest_verdict.sh` verbatim to both repos, wired into both healthchecks and all three CI pytest steps (rc alone measured flapping to 0 on failed runs, MF 2026-07-28; the flap only loses failures toward zero). **REFUTED**: both CIs' pytest-pipe-to-tee is NOT an rc hole — both steps set pipefail before the pipe; maps POST backup endpoint is rate-limited + api-key-gated (default-open only when no key configured, the documented LAN-trust posture) and 5-backup rotation bounds disk. **VERIFIED CLEAN**: the four unreviewed commits (readme_stats twins byte-identical, well-designed, drilled alive; maps 07-19 healthcheck rc-capture correct); row 26's "OPEN" items are STALE — the client's 2nd/3rd passes (same day, `2af1db3`..`80fefce`) closed S4, the TUI render-DoS, and the config-broker fallback. **RESIDUAL (documented, not changed)**: client CI has no per-test harness timeout at all (a hung test holds the CI job to GitHub's own limit — adding one needs the internal-deadline sweep first, queue-worthy); maps verdict line echoes pytest's decoration bar (cosmetic); DEPLOY — maps runs on moc/moc1/moc2 and pull is manual; the running collector keeps pre-fix code until its next restart (left to the operator — no restart performed). |
| 2026-08-06 | **FRONTIER WORKLIST Pri-2 — the detector-blindness contract (queued row: can a HOLD outlive its observer?)** — `watchdog_tracker.py` hold semantics, `probe_kernel_reboot_pending`, `scripts/honest_status.sh` watchdog leg, mini's `detector_blind_any` ownership question, MA twin question. | **Fable 5 frontier ratification, evidence-first**: every mechanical leg checked against LIVE state before judging — moc5's real watchdog.json read this session (zero held signals; `kernel_reboot_pending` disposition `clean` while `ProtectKernelModules` is STILL `yes`, i.e. the 07-27 sandbox-surviving `/boot/vmlinuz` leg observing honestly); the gate's held-blind tier read at source; the one missing piece closed with 4 harness pins in `test_honest_status_boxes.sh` §14, sabotage-drilled (neutered `_held` → 4/4 FAIL, restored → ALL PASS). | this commit | **VERDICT: RATIFIED as-built — the hold stays UNBOUNDED, and that is correct.** The contract has three legs with three distinct owners: (1) the TRACKER holds forever and re-emits with `extra.unobserved_hold` — only a positive observation may clear; bounding the hold would be the false-recovery in slow motion. (2) Chronic-blindness ESCALATION belongs to mini's `detector_blind_any` (24 h grace, taxonomy-ratified) — so the row's "what is N?" answers N=24h AND IT ALREADY EXISTS; a tracker-side bound would have been the SEVENTH independent debounce implementation (the taxonomy's consolidation finding forbids exactly this). (3) CONSUMERS must file a hold as last-known/UNKNOWN, never live — honest_status tiers held-only below degraded into UNKNOWN (never WARN/FAIL on its own, never a pass), fleet_truth dims + tooltips. **The row's premises were partly STALE** — two of its three mechanical legs were already cured by later sessions: the probe grew the sandbox-surviving `/boot` leg 2026-07-27 (hardening preserved — do NOT flip ProtectKernelModules), honest_status grew the held-blind tier the same arc; MA has NO SignalTracker (file verified absent) — nothing to port. All 4 `detector_blind_any` classes the row names have since been addressed at source (kernel 07-27; delivery_confirmation_stall + mqtt_root_drift 08-05 structural fixes; rns instance-name class 08-05). **NEW this pass**: the gate tier was prose-only — 4 pins added, including the ambient-state trap caught while writing them (this box's own live degraded signal leaked into the fixture verdicts until self was pinned to the clean fixture). **RESIDUAL**: in a mixed live+held box the held signal is visible only as the sigdesc class-suffix, with no separate summary count (accepted — the count line appears when held stands alone, which is the case that was lying). |
| 2026-08-06 | **GUARD-SPINE + BACKUP-CHOKEPOINT ARC ultra review (queued Pri-1 row)** — MF `8c550c72..4ef7c5ac` delivered per the row's recipe as replay branch `review/guard-spine-arc` (4 cherry-picks on base `8c550c72`, tree verified byte-identical to `4ef7c5ac`); 16 files +1218/-101 (wider than the row's +874 — the docs commit rode the range). MA twin range checked during adjudication. | **Ultra multi-agent cloud pass** (session 01AoNGkHNsJmZREAt4xRMULJ) → 1 finding; **Fable 5 adjudication in-session**: the finding verified against all four claimed code sites before acting; fix red-first (2 tests FAIL unfixed → pass fixed) in BOTH repos. | this commit (MF) + MA twin commit same day | **CONFIRMED+FIXED (the one finding, filed nit, adjudicated low-probability/HIGH-consequence): create_backup built `BackupMetadata` BEFORE `reserve_backup_path` could step aside, so a same-second second backup wrote `<id>-2.json` whose on-disk metadata still said `<id>` — list_backups then showed two backups under ONE id, and restore/delete resolve `<id>.json` first, so selecting the SECOND backup silently operated on the FIRST file — a wrong-artifact DELETE inside the very module the 08-05 arc shipped to prevent that class.** My own 08-05 inline comment named the hazard and cured only the RETURN value; the on-disk label kept the stale id — a half-fixed hazard my same-day re-review missed because the consumers (list/restore/delete) sat outside the arc's diff. Cure: identity follows the FILE (`metadata.backup_id = backup_file.stem` post-reservation), both repos, + `test_device_backup_identity.py` (2 tests, red-first) both repos. **THE FIVE PRE-BRIEFED JUDGMENT ITEMS — all survived external attack with zero findings**: (a) `_strip_trailing_comment` (the author-flagged attack target) drew no stripping-too-much input; (b) no removed per-guard filter was load-bearing beyond test exclusion; (c) no drill↔contract mismatch; (d) no objection to the `TestConfigPathContract` deletion; (e) no objection to retiring the historical replay. Adjudication verdict: absence-of-finding from one external pass is corroboration, not proof — but stacked on the local drills, the revert drills, and both suites, the arc's self-assessment stands with its confidence upgraded from self-verified to externally-attacked. The known-mechanical list was honored (nothing re-litigated). Replay branch kept on origin for provenance. |
| 2026-08-09 | **EGRESS-GUARD ARC COLD REVIEW (the queued Pri-1 row below, run same day it was queued)** — MF `f3418d26..b79170b1` replayed as branch `review/egress-guard-arc-cold-2026-08-09` (tree verified fully contained in main plus this audit file; branch deleted after), whole arc: `tx_guard.py`, the rns_init attach backstop, e2e harness plus conftest tripwire, `honest_status.sh` skew leg. | **Fable 5 /code-review background fork** — parallel finders then adversarial verify (~47 candidates deduped, 2 legs REFUTED and dropped), several findings live-drilled against the installed meshtastic 2.7.9 parser and with planted violations; fixes applied in-session by the parent, red-first wherever a test pinned the old behavior; full suite re-run at close. | `b7857718` + `2c5e5fba` + `66368c0b` + `b7332bd4` + `b816a728` (all landed beneath this row) | **10 CONFIRMED, 10 FIXED. Three of the queue row's five judgment calls drew blood**: (a) the arming predicate — an EMPTY env value read as an explicit disarm and silently switched the whole stack off under pytest, and the env-armed live drill SEVERED the RNS attach (silent RNS outage with a log blaming pytest) while a refusal killed three daemon threads mid-bookkeeping (cure: empty falls through to pytest detection with a loud disarm witness; attach backstop scoped to actual-pytest; sanctioned deliberate-catch pattern at periodic/fallback sites); (d) the self-authored sweeps — the MeshCore companion radio (MA's PRIMARY type) sat entirely outside the architecture (new coarse gate + attach backstop + function-level sweep), the CLI guard missed the equals-form and argparse abbreviations, the sweeps accepted guard-after-transmit ordering and a comment-substring exemption and missed four interface TX methods, and adding the missing `--request-telemetry` flag immediately exposed TWO unguarded production CLI sites (both now guarded); plus the queue row's own known-MEASURED normalize_target collapse OVERTURNED — collapsing unknowns to the most-allowlisted target is fail-open in both directions (cure: UNRESOLVED token refused unconditionally; malformed allowlist entries raise). Also: the send_message CLI-fallback guard referenced an undefined name (NameError on every production fallback send — deleted as redundant with the runner guard, which now receives the real host); module-lifetime e2e allowlist of the incident's exact endpoints narrowed to the prewarm window; the patch-drain registry no longer discards higher-scoped restores or swallows failures; the skew leg now judges each unit against its OWN repo, enumerates both systemd scopes disclosing user-scope blindness, and treats non-numeric timestamps as unknown. **REFUTED by verify**: the skew-leg HT guard candidate and the sendHeartbeat omission. **RESIDUALS (documented, not done)**: queue judgment calls (b) connect_ex-vs-connect split and (e) process-global allowlist concurrency were NOT attacked this pass and stay open; (c) the open_reticulum None-reader hunt did not run; four pure-cleanup findings cut at the 10-finding cap; **MA PORT PENDING** — tx_guard.py is byte-identical-by-design and MeshAnchor still carries the PRE-fix guard, and the new MeshCore gate covers MA's primary radio: land there next. VERIFIED: full suite 10618 passed exit 0 after all five fix commits. |
| 2026-08-10 | **EGRESS-GUARD residual legs b, c, e (the Pri-2 row queued yesterday, run next day)** — the three judgment calls the cold review did not attack, across BOTH repos: the connect_ex/tripwire seam, every open_reticulum caller, and the process-global allowlist's concurrency semantics. | **Fable 5 pass, two parallel very-thorough read-only enumeration agents (one per existence question) with every actionable claim re-verified at source by the orchestrator before acting; leg e drilled inline RED-FIRST (the corruption reproduced on the pre-fix code exactly as predicted, then fixed, then green); the new chokepoint-evasion guard proven red against MA's real pre-fix line at git HEAD and green on the fixed tree; full suites re-run both repos.** | this commit (MF) + MA twin commit same day | **Leg (b) REFUTED with evidence**: ~120 connect_ex sites enumerated across both repos, every one a true probe (socket closed, never written, never escapes); all real TX libraries (TCPInterface, urlopen to the radio HTTP port, paho) connect via connect/create_connection and stay tripwire-visible, incl. asyncio and AF_INET6. Three secondary findings FIXED: the probe_connect docstring promised permitted-but-RECORDED but no recorder existed (the permitted branch was unfalsifiable at runtime — probe ledger + tripwire witness added, both repos); the tripwire's own drill left an unclosed connected PhoneAPI socket on a live box (closed + witness asserted); one CLI site lacked the runner guard (guarded, both repos). **Leg (c) 3 CONFIRMED, 3 FIXED**: MeshAnchor's CLI read the chokepoint's degraded None as literally "no peers this cycle" and rendered a wedged rnsd as a healthy quiet network (honest_failure_modes number 1 verbatim, stated in its own comment; MF's twin already raised — ported, incl. require_listener); MF's map collector captured the degrade bool, DISCARDED it, and classified the resulting empty path_table as the green-scored no_data (now unreachable with the wedge named); MeshAnchor's node_tracker smuggled RNS.Reticulum as a bare callable into call_boundary — a raw attach with no wedge probe, no listener preflight, no pytest backstop, invisible to MF019 AND the chokepoint regex because the site has no call syntax. Routed through the chokepoint + a NEW guard leg in both repos closes the evasion shape itself. **Leg (e) CONFIRMED + FIXED**: overlapping non-nested allow_targets lifetimes corrupted in both directions (A-exit revoked still-open B; B-exit resurrected closed A) — replaced snapshot/restore with per-context grant registration; the SAME class found and fixed in the RNS and MeshCore coarse gates (saved-boolean restore → depth counters); the teardown wipe hatch now beats a leaked grant, pinned. **RESIDUALS (documented, not done)**: sendto/UDP is outside the tripwire (no radio-port UDP exists in either tree; meshtasticd's API is TCP-only, so recorded rather than machineried); one MF lifecycle test's incidental RNS coverage went to zero under the attach backstop (asserted nothing about RNS before or after); MA still has no test_rns_init.py — its chokepoint decision logic is pinned only by parity; the queue-worker refusal-kill under an armed drill remains queued. |
| 2026-08-11 | **TONIGHT'S INSTRUMENT CHANGES — the queued frontier pass, all 4 ranges**: `3eca3b5b` via-verdict (scripts/fleet_offline_check.sh + watchdog_probes_liveness.py), `8d4c746e`/MA `e72a9a03` brief/rollup active-resolved split, `6cd26cb9` honest_status three-repo skew dispatch + its class pin, runbook-zero §7 acceptance contract | Fable 5 frontier inline pass, single-context (footprint doctrine — no fan-out on the Pi); each range walked against quoted live code along its queued steer; every fix drilled in the failing direction (red-first against the pre-fix code / planted violation), and the old class-pin hole PROVEN live before tightening | this session's fix commit (MF) + MA twin (byte-identical brief.py + MA-side rollup pin) | **CONFIRMED+FIXED 4 (+1 hardening)**: (F1 — the range-2 steer answered YES) `_split_escalations_by_activity` demoted a pair whose `currently_active` FIELD was renamed/dropped/nulled — `bool(None)` → False — flipping every matched escalation to "resolved", the hiding direction, fleet-wide at once, while its own docstring promised demotion only on a positive False; absent-or-None is now unknown → stays ACTIVE (red-first: 1 failed on pre-fix code, 1 passed with fix; MA port + MA-side pin). (F2 — the range-1 steer's "wrong demotion silences a real box-down page", found one level up) a broken `via` DECLARATION (typo'd alias, missing ssh config) is indistinguishable from "path down" at outage time, so every REAL outage of that box would page the demoted UNOBSERVABLE claim forever, unwitnessed (the 08-05 detector-keyed-to-the-wrong-name class); free cross-check added: a direct-reachable tick PROVES the path through the hop, so a failing via probe on that tick logs `VIACONF-SUSPECT` naming the coming misreport — witness only, no page, drilled both directions (2 failed pre-fix, 29 passed post). (F3) the skew-leg class pin's `HT[A-Z]{2}` regexes had two holes: a longer var name (`HTMAPS`) escaped "defined" entirely while `H=\$HTMAPS` substring-polluted "used" as a false `HTMA`, and "used" accepted any text containing `H=\$HTxx` (alias assignment, comment) as consumption; planted-violation drill PROVED the old pin passes an unwired aliased repo (defined=used=HTMA HTMF HTMM with HTMAPS live in the file); now `[A-Z]+` maximal-munch + "used" requires the case-arm shape `\) H=\$HT…`, and the harness takes `HS_SKEW_SCRIPT` so the pin stays drillable. (F4) fleet_offline_check had no lock: a total-outage tick (every ssh timing out + ntfy 3×12s retries) outlasts the */5 cadence and overlapped runs interleave the fixed-name `$STATE.tmp` (hfm #8; pre-existing, widened ~15s/box by the via probe) — `flock -w` on `$STATE.lock`, loud exit-75 refusal lands as a cron_verdict FAIL, drilled locked-out + uncontended. (Hardening) `set_state`'s `${7:-down}` default meant a future missing-verdict bug silently wrote the STRONGER claim — the exact laundering the probe refuses; now `${7:?}` fail-loud. **§7 SIXTH CHECK (range 4) — two answers**: TODAY a deaf radio (wrong region/preset, dead PA, disconnected antenna) passes the entire list while dead at its purpose, because "message arrives" was deferred in both trials and as written need not transit THIS box's RF on its segment — §7 now requires the box's own radio, segment-aware; and the CLASS-IN-WAITING is reboot survival: every acceptance line interrogates a running system with inherited state, and the fleet already carries boot-triggered destroyers that pass all of §7 until the first power cycle (cloud-init hosts-wipe §1.5, missing linger §1.4, wrong `*.target.wants`) — §7 now ends with power-cycle + re-run the list + verify NTP-synchronized before trusting any freshness-based check (hfm #6). **RESIDUALS (documented, not changed)**: shell recovery wording reads a corrupted/unknown stored verdict as "was down" (stronger claim; corrupted-row-only — the probe, the consumer that matters, already refuses the laundering); re-page duration attributes the whole outage to the CURRENT verdict after a mid-outage flap (current-observation-wins, deliberate); `IFS=';'` read would absorb a future 6th BOXCONF field into `via` (trailing-capture); probe `extra.max_down_min` mixes unobservable durations; the class pin remains source-grep — a wrong GLOB on a future arm, or a comment shaped `) H=\$HTXX`, still passes; behavioral drill stays the real check; F2's witness is LOG-ONLY (fleet_alerts.log, operator-greppable — verified no machine consumer reads that file) — probe-visible wiring needs an 8th state field and is deferred with rationale (footprint doctrine: no new machinery for a rare config typo; the line names its own consequence and recurs every tick while true). **REFUTED (attacked, held)**: verdict promotion/demotion under dependency flap (current-tick observation wins in both directions, test-pinned incl. threshold-crossing sequences); the 6-field back-compat read (`cut -f7` → empty → "down" — exactly what those rows meant; old+new writers coexist coherently); via probe deliberately not forcing FLEETKEY/StrictHostKeyChecking (would test a path nobody uses, fail-closed into false "path down"); rollup's per-escalation splitter call (O(n·m) on single-digit n); `_last_seen` keyed on the dedup tuple (always present for members of esc_pairs). |
| 2026-08-11 | **follow-on from the row above — the bare-warmstart check (operator-requested)**: `src/mini_dudeai/{_util,warmstart,rollup}.py` + both fleet presets, MF and MA | fix from the check's own finding (no new review pass); red-first drilled on the MA side (all 5 divergence tests FAIL against the pre-fix src via stash, pass post), the remote breadth cmd executed through a real shell against an MA-shaped fake home, live-drilled on the replica | this session's fix commits (MF + MA twin) | **CONFIRMED: the 07-23 adapter pass carried unit/repo/preset NAMES through the `_util` seam but not PATHS.** Byte-locked `warmstart._default_paths()` and both rollups' basename constants kept `$HOME/mini_dudeai_*` baked in while the `meshanchor_fleet` preset writes `~/.local/share/meshanchor/mini/{brief.md,state.json,history.jsonl}` — measured live: bare warmstart on the replica reported "mini has not run here" beside a daemon ticking 30 s away, and a healthy MA fleet would have rolled up as `no_state` on every box. Neither MA module had a wired consumer yet, which is why it sat invisible (an instrument that has only ever produced one outcome). Cure: `APP_{MINI_SUBDIR,BRIEF,STATE,HISTORY}_RELPATH` + `app_artifact_paths()` joined the adapter seam; the byte-locked warmstart, both rollups (remote `cat` legs incl. claw sibling twins, local joins, error literals), and both fleet presets (the writers) resolve through it; MA's `ma_mini_dir` delegates to `APP_MINI_SUBDIR` so `fleet_watchdog`'s test-pinned location agreement rides the same constant. MF values byte-coincide with the old hardcodes (behavior-identical, suite green). Pins: MA `test_mini_artifact_paths.py` (the DIVERGENCE side — the only side that can catch a re-hardcode, stated in the MF test's own docstring), MF wiring tests (warmstart↔adapter, collect_local default, remote-cmd relpaths) + preset source tripwire both repos' shape. ALSO FOUND+FIXED during the replica live drill: (a) the CLI `--brief` default was a THIRD independent path convention — `dirname(state)/mini_dudeai_brief.md`, ignoring `engine.brief_path` — invisible on MF (values coincide), an orphan writer on MA (the drill's own manual `--brief` created the orphan, then removed); now `_brief_out_path()` prefers the engine's wired path, sibling fallback only for brief-less engines, pinned both repos. (b) NOT a bug, ruled out with the writer's docstring: the replica's `brief.md` mtime frozen at 08-06 is the SD-wear guard skipping unchanged-content writes on a quiet box — the warmstart banner's freshness comes from state, which is the honest signal. RESIDUALS: MA has no claw-tick writer yet — the sibling-of-state aim in its remote leg is convention-setting, unexercised; the preset tripwire is a source grep (limits stated in it; behavior pins carry the weight); the `_generated` stamp on a wear-guarded brief reads as staleness but means last CONTENT change (documented in write_brief; the banner carries freshness). |
| 2026-08-14 | **TUI HARDENING ARC self-review (same session)** — MF `4c5bc1f6..7534dd9a`, the six audit-driven fix batches: `backend.py` escape/cancel + checklist + gauge deletion, FakeDialog parity, startup honesty (S1/W1/W10/W3), `_rns_repair.py` C1/C2 witness, NOC-panel-into-menu-body + ANSI, parallel network scan. Arc itself born from a 3-explorer audit (architecture W1-W18, handler sweep B/C/D/E/G, tests/UX/MF018) recorded in `~/.claude/plans/tui-audit-features-elegant-dream.md`. | **Fable 5 /code-review background fork (high)** — 8 finder angles, near-duplicates deduped (4 clusters), every survivor verified against live source by the reviewer AND re-verified by the parent before fixing; the top finding reproduced live (real `check_port` TypeError). Fixes applied in-session, red-shaped tests added per finding; full suite re-run at close. | `78f81d3a` (landed beneath this row) | **10 findings, all closed.** (F1, the blood-draw) the new `_parallel_port_scan` called `check_port(ip, port)` against the real `check_port(port, host=..., timeout=...)` — the scan could NEVER find a device, the batch's own tests mocked the phantom shape and stayed green (FakeDialog-parity T1 at function level), and the pre-arc serial loop carried the same latent bug, meaning the nmap-less scan has been broken since birth; cure = keyword args + a ZERO-MOCK end-to-end test (real listener on 127.0.0.42, real check_port). (F2) the C1 fix covered only the second-chance branch — the pre-flight sibling ran the same confirmed destructive write with print-only witness; both now share `_confirm_disable_interfaces` and a source pin allows exactly one `disable_interfaces_in_config` call site (the 08-09 grep-every-branch tell, caught by review instead of a 12-day-blind box this time). (F3) `menu()` auto-fit only ever shrank — the flagship NOC panel clipped inside the fixed 22-row box even on tall terminals; the box now grows to content. (F4) Escape at the main menu was logged as a dialog FAILURE and 3 presses killed the TUI with a false ERROR; Escape now offers "Exit MeshForge?" and the retry budget counts only `DialogError`. (F7, the class behind F4) the Batch-1 `-1` sentinel was honored only by `menu()` — `yesno()` answered "No" on a dead subprocess, checklist parse failure read as Cancel; input primitives now RAISE `DialogError` (a dead dialog can never fabricate a user answer), display primitives log witnesses. (F5) the backgrounded update check's `git fetch` could prompt on /dev/tty OVER the live menu — killed at the `_git` chokepoint (GIT_TERMINAL_PROMPT=0, ssh BatchMode, stdin=DEVNULL). (F6) startup_health idempotence guard (reviewer's double-call claim REFUTED — no `on_shutdown` means startup_all skips it — but the guard makes adding one a non-event). (F8) both isatty guards survive `sys.stdin=None`/closed (the 26k-restart class reintroducible by its own mirror). (F9) ANSI rejection extended to ALL FakeDialog primitives + central strip-with-witness in `DialogBackend._run`. (F10) USAGE.md's dead `--debug` section replaced. **RESIDUALS (documented, queued in the plan file)**: Q1-Q7 worklist (dead-code purge incl. W4 profile-gating decision, journalctl/E-series dedup, MF018 39-string sweep, B2-B10 hang paths, W8/W11/W15-W18 consistency, main.py coverage, IA reorg); the F8 piped-stdin-but-real-tty case conservatively skips the wizard (reachable via Configuration menu); FakeDialog does not emulate DialogError semantics (parity test pins method surface only). VERIFIED: lint exit 0, full suite 10912 passed exit 0, CI green on `7534dd9a` (4/4), live drills = no-whiptail launch exit 1 honest + real-whiptail pty menu (panel text rendered, single-Escape return in 1.9s). |

| 2026-08-30 | **dude-claw remote reboot + firmware-version field** — MF `b81d6508..06d2ab50` (`scripts/claw_set_watch_ids.py`, `src/mini_dudeai/claw_telemetry.py`, `tests/test_claw_telemetry.py`, `.claude/plans/dudeclaw_remote_reboot_2026_08_30.md`) + fork `wireclaw-dudeclaw` `a80b795..dffd3c0` (`src/tools.cpp`) | **NO adversarial pass ran — this row exists to say so.** Opus session: build + **LIVE HARDWARE drills**. A frontier pass WAS queued here for the config-rollback boot path, then **WITHDRAWN the same session** when the operator's evidence (physical access to every claw = minutes, not a building trip) removed its premise; the rollback itself was then judged DO-NOT-BUILD rather than reviewed. What replaced review was hardware: all 3 claws flashed and verified (`Hash of data verified` + version pollable on each), reboot tool drilled on 2 boards (`sw-restart`, uptime 60->13 / 42->15), claw-03's ST receive path re-proven post-flash by forcing a moc3 TX (`heard 0 -> 1 pkts`, -61 dBm, crc_err 0). Parser built against a CAPTURED live reply, not an assumed shape; 2 of 3 new tests revert-drilled (break `_RE_VERSION` -> 1 fails; break `_FIELD` -> that test + 4 pre-existing fail). | `b81d6508`, `978448b6`, `06d2ab50`; fork `a80b795`, `dffd3c0` | ⚠️ **Drill 2 (the reboot tool's refusal path) is UNDRILLED and cannot be done remotely** — `config_set` refuses `wifi_ssid` by design, so blanking it needs the portal on the claw's own /28 with a human present. That is the load-bearing guard and it stays BELIEVED. ⚠️ Caught in-session, both mine: I compiled `env:esp32-s3` all session when the claws run `esp32-s3-heltec-v4` — flashing it would have stripped OLED/VBAT/BLE/anomaly **and `lora_ears`**, i.e. the RF watch itself; and a bare `esptool read_mac` left claw-01 in the bootloader ~90 s (recover with `--no-stub --after hard_reset`). Board->claw identity established by DISRUPTION, never inference — subnet reflects AREDN AP association, not location. OTA (Part 2 of the plan doc) is SPEC'D AND UNBUILT; its gate 1 (measure the flash chip — 4 MB declared vs Heltec V4's likely 8 MB) is untested. 3rd new test (None on old firmware) is a documentation pin, not a drilled guard. |

## Known NEVER-reviewed (as of 2026-07-05)

- Everything predating the 2026-07-01 pass that isn't in a range above
  (the bulk of the codebase predates adversarial review; field soak +
  tests are its evidence base instead).
- `20d0b643` (revert of durable r2m reverse-routing) — a revert, not a
  review target.

## Frontier worklist — queued upshift ranges (added 2026-07-09, model-advisor companion)

> The model advisor's upshift path lands HERE: when a session judges work
> review-shaped but isn't running on a frontier-class model, it appends a row
> instead of faking the pass. Next frontier session: pick from the top.
> Remove a row only by running the pass (its results become a row in the
> table above).

| Pri | Scope | Why it's frontier-shaped |
|-----|-------|--------------------------|
| — | ~~**2026-08-30 dude-claw arc — adversarial pass** (Pri-1)~~ **DONE 2026-08-30 (same day)** — ran as the completed-table row above (MF `978883ea..b9d6751c` + fork `4f1172d..2d21e0d`; 3 confirmed defects fixed at `ee7b8007`, all in the PSK bridge exactly where the warrant said to start; 3 firmware findings queued in that row's residuals — F1 truncation sentinel, F2 (0,0) false-direct, F3 wifi_password presence — for the next flash cycle). | Removed by running the pass, per convention. The empirical warrant held: the uncaught rate was indeed non-zero, and every defect was the honest-failure class the queue predicted. |
| — | ~~**Opus interregnum review** (Pri-1)~~ **DONE 2026-07-16** — ran as the completed-table row above (MF `5083d56d..835528e0` + MA `417d57ad..a1f32f93`, 0 confirmed defects). | Removed by running the pass, per convention. |
| — | ~~**meshing_around_meshforge FIRST adversarial pass** (Pri-1)~~ **DONE 2026-07-16** — ran as the completed-table row above (client `4fc5eea`, 5 confirmed defects fixed of ~35 findings, suite 1028 green). Residual worklist for the next pass lives in the client repo's `docs/reviews/2026-07-16-first-adversarial-pass.md` (top item: TUI Rich-markup render-DoS). | Removed by running the pass, per convention. |
| — | ~~**version_consistency_check 07-09 fix-port** (Pri-2)~~ **DONE 2026-07-16 same session** — MF's current file turned out to be the byte-portable superset (its `CANDIDATE_SSOTS` already names both apps' SSOT paths; the queued "keep DEFAULT_SSOT" adaptation was unnecessary), so the port is verbatim: maps `e9e2953` + client `8304e5d`, both TWIN-IDENTICAL to MF, both checks run live exit 0 with the new CHANGELOG leg active (SSOT 0.7.4-beta / 0.6.0). | Removed by doing the work. |
| — | ~~**Fleet-truth honesty invariants** (queued 07-18/19 by the NOC arc)~~ **DONE 2026-07-19** — ran as the completed-table row above (this commit + MA `c16a240e`). | Removed by running the pass, per convention. |
| — | ~~**Row 8 `cross_gateway_dups_unsuppressed` BUILD vs ACCEPT** (Pri-1)~~ **DONE 2026-07-26 — ACCEPT RATIFIED at frontier tier**; ran as the completed-table row above. Verdict + adversarial pass + revisit triggers recorded in `.claude/research/cross_gateway_dup_design_inputs_2026_07_19.md` (VERDICT section); eval case `oracle-cross-gateway-dup-accept-cost-asymmetry`. The original queued text below is retained for provenance. ✅ ~~**READY — no blocker, inputs re-derived 2026-07-25 22:05 HST.**~~ The old "sequencing-blocked behind the RNS 1.3.8 roll" note is RETIRED: that roll completed **2026-07-19** (`50213071`, SSOT `72acf61e`, all 7 boxes at the pin). **LIVE numbers (use these, not the 07-19 file's):** `/fleet/dups` `status: ok`, `covered_hosts [moc, moc3]`, **`uncovered []`** (full 2/2 gateway coverage), moc 127 confirmed pairs / moc3 126, ages 55 s and 18 s. **`fleet_human_duplicate_pairs: 0`** · `fleet_duplicate_pairs: 0` · `fleet_infra_duplicate_pairs: 0` · `rns_unconfirmed_pairs: 0` · `fleet_duplicates: []`. `dual_homed_recipients: 3` (all 3 infra hashes known). **The bound: `unconfirmable_sent` 14,117 (07-19) → 15,936 (today), +12.9%** — the mesh leg that cannot be dup-checked AT ALL keeps growing, so "0 observed" will never become "0 exists". `untracked_events: 588`. **So the ACCEPT case strengthened** — still 0 human dups after ~27 days of full coverage, one fire ever (2026-06-29, infra-class, pre-classifier) — while the unobservable population it cannot speak for grew. That tension IS the decision. **Row 8 `cross_gateway_dups_unsuppressed` — decide BUILD vs ACCEPT before designing** (queued 2026-07-19 by the burn-down session). Inputs are gathered and measured: `.claude/research/cross_gateway_dup_design_inputs_2026_07_19.md`. Headline: **0 human cross-gateway dups have EVER been observed** (one fire ever, 2026-06-29, infra-class recipient, pre-classifier), detector currently fully covered (2/2 gateways, `uncovered []`, 196 confirmed pairs) — but bounded by `unconfirmable_sent: 14117`, the mesh leg that cannot be dup-checked at all. | Distributed coordination (which gateway yields, race windows, idempotency, partition/split-brain) is the highest-risk design in the backlog, and the evidence says it may be unnecessary — a row-3-style accept. That build-vs-accept call, against a partly-unobservable population, is exactly the ambiguous-evidence judgement frontier tier is rationed for. **Do not open with a coordination design.** ~~Also sequencing-blocked behind the RNS 1.3.8 roll (touches meshforge-gateway).~~ **Blocker RETIRED 2026-07-25** — the roll completed 2026-07-19; see the re-derived inputs in the scope cell, which supersede the 07-19 research file's numbers. |
| — | ~~**`model_router.py` adversarial design review** (Pri-2)~~ **DONE 2026-07-23** — ran as the 48-hour-audit row above (frontier tier; questions (a)-(d) answered, 2 majors + verdict-integrity fixes landed `0fa21d9b` — ⚠️ SHA corrected 2026-08-06: originally cited `d8d2c247`, an amended pre-image NOT reachable from `main` (tree-identical, `git diff` empty; the bfc1eba7 class from the 07-28 row) — design follow-ups queued in that row). Original ask: (queued 2026-07-22 by the WS-E build session, authored on Opus). The router (`src/mini_dudeai/model_router.py`, 21 tests) turns `model_advisor.md`'s tiering into a re-derivable `route(task_kind, env) -> Recommendation` grounded in the eval + calibration ledgers and the capability gradient. It is **advisory-only** (never blocks), so shipping it Opus-designed is safe; but the DESIGN wants a frontier eye. Review targets: (a) is the `TASK_TIER` / `ENV_CEILING` taxonomy right, or does it miss task kinds / mis-clamp an env? (b) the eval-fail-on-qth "escalate to opus" jump — correct, or should it consult the eval for the higher tier too? (c) the self-scoring plan — verdicts re-derived from the eval ledger for L/api_small routings, `open` otherwise — before the routing ledger accretes rows a future pass must trust. (d) the consumers NOT yet wired (warm brief / cadence launcher / haiku promotion) — design them here. | Novel-arc harness design touching the reliability/observability spine (the model_advisor.md "novel-design" upshift tell, applied to itself). The taxonomy + capability-gradient calls are judgement the frontier tier is rationed for; a shallow Opus self-review that blesses it is worse than this honest queue entry. |
| — | ~~**Does an `indeterminate` probe disposition emit a false CLEARED?** (Pri-2)~~ **DONE 2026-07-26 — ANSWERED YES**; ran as the completed-table row above. Confirmed by reading `SignalTracker.update`, not retroactively from the journal: the tracker diffed on signal presence alone and never saw the disposition map. Cured at the layer that owns the transition (only a positive observation may clear; `coverage` now a required keyword), plus the crashed-dispatch branch that cleared every signal at once. Original ask (queued 2026-07-24 by the 18:22 self-inflicted-outage triage). Observed, unexplained: `rules_seed_drift` logged `signal CLEARED` at 18:22:24 — six seconds after systemd-oomd killed 49 processes in `session-2.scope`, and on a box where the drift was REAL and uninterrupted (seed gained `host_memory_pressure_any` at `e8e9a62d` 14:16; live rules file untouched since 07-23 09:44, confirmed by mtime + rule count, and the drift re-fired next boot). Candidate mechanism, NOT confirmed: `probe_rules_seed_drift` (`src/utils/watchdog_probes_mini.py:295`) has four `note_disposition(..., "indeterminate"); return None` paths (unresolvable role, unreadable seed, unreadable/corrupt live rules), and `None` is indistinguishable from "healthy" at the layer that emits transitions — so an observation channel that failed under memory pressure would clear a still-true signal. Ruled out already: a seed-content change (git `-S` shows the id entered once and stayed). Not confirmable retroactively — dispositions are per-tick and never logged, which is itself part of the finding. | Textbook honest_failure_modes #2 ("never emit a recovery from data missing because the observation channel failed") + #9 (every swallow leaves a probe-visible witness), but the fix is NOT local to this probe: `return None` is the deliberate, documented self-guard contract shared by ~50 probes, so the question is whether the WATCHDOG layer must distinguish absent-signal from cleared-signal, and whether dispositions need to reach the log. That is a spine-wide contract call across every probe consumer — ambiguous-evidence, high-blast-radius design work, and exactly what a shallow single-probe patch would paper over. Hand the next pass the 18:22 journal window on boot `c9e16aae`. |
| — | ~~**SECOND-BRAIN TAXONOMY** (Pri-3)~~ **DONE 2026-07-26** — ran as the completed-table row above; design at `.claude/plans/second_brain_taxonomy_2026_07_26.md` (axis = UNIT/CHANNEL/FALSIFIER; T1 answered by the coverage map that already exists; consolidation = six debounce implementations onto mini's reference semantics; R5 + R2b + T6 measured; **T5 deliberately left open** — re-queue it separately if wanted, it is the one part that would require adding). Original ask (queued 2026-07-26 by the soak-close/DNS session, at operator request: *"there's been a lot of second brain work done — which in itself redefined the meaning of second brain for the meshforge domain"*). **Prep artifact is COMPLETE and measured — do NOT re-derive it: `.claude/plans/second_brain_taxonomy_prep_2026_07_26.md`** (full organ inventory: 58 probes / 14 modules / 57 signal classes, mini 67 live rules + 146 dreams judged, tier-L 61 eval cases, 78 calibration-ledger events, plus consumers and constraints). **Headline finding**: score-keeping is INVERTED — the model-driven organs (me, tier-L, mini's dreams) all carry measured track records, while the 58 deterministic detectors that actually watch the domain carry NONE (`grep -rn "false_positive|precision|probe_score" src/utils/watchdog_probe*.py` -> zero). This session produced the proof of cost: the owrt1 rtun watchdog ran at ~7% false-positive rate for days and bounced a HEALTHY tunnel 4 times — the guard's false positives causing the outage it exists to prevent — and nothing noticed. Six tensions carried in the artifact (T1 who scores the scorers / T2 always-loaded docs have a size guard but no STALENESS guard, which already cost a real decision / T3 layers that agree by construction / T4 a rule is not a check / T5 who owns "while you're away" / T6 unversioned organs on the boxes they guard). ⚠️ **SEQUENCING: run the Pri-1 SIGNAL-YIELD DELETION PASS FIRST** — it is the same judgment at a lower altitude and is operator-framed as *run before adding anything new*; its per-signal reasoning becomes this taxonomy's evidence. ⚠️ **Hard constraint: the output must be CONSOLIDATION, not addition** (footprint rule — never add machinery to watch the machinery); if the pass ends with more organs than it started, it went wrong, and T1's answer must NOT be a meta-organ. ⚠️ **Do not rank detectors by fire count** — the Pri-1 row already proves fire count is a representation of value, not value (37 never-fired classes are dominated by wedge guards fixed AT SOURCE, where zero fires IS the fix working). | Novel-architecture design across ~8 organ families with no existing taxonomy, ambiguous evidence (is a silent detector dead weight or a working seatbelt?), and a blast radius spanning the alerting contract ~50 probes share plus both memory stores. The prep was deliberately built on an Opus session per `model_advisor.md` so the rationed pass spends itself on judgment, not discovery — start at §2. |
| — | ~~**SIGNAL-YIELD DELETION PASS** (Pri-1)~~ **DONE 2026-07-26** — ran at operator request; full results `.claude/plans/signal_yield_deletion_pass_2026_07_26.md`. **The deletion question answered EMPTY: nothing was deleted.** The 'fired exactly once' list had zero deletion candidates (all 7 incident-born, each catching what no other class would). The alerting burden was never dead detectors — it was a debounce in the wrong unit, a manual step missing from deploy, and an inherent lead-repo workflow. All three actionable items shipped the same day: **R1** cadence-aware confirmation for `cron_verdict_stale` (fast crons must fail twice, slower ones and @reboot fire on sight, so the added delay is bounded at one hour instead of one day; confirmation read from the verdict log, no new state file; an unconfirmed failure notes `indeterminate`, never clean) — `~30%` of escalation volume; **R2** role-seed promotion folded into all three deploy legs (`d025a4e7`) — `~15%`; R3 `parity_drift` judged inherent, no change. Corrections recorded in-row at the time: the original trio missed `tracer_peer_unreachable` (3rd largest, not deploy lag, silent since 07-10), 68 escalations come from box-local rules outside every seed, and the top three were 3 conditions FLAPPING rather than 99 alerts. **Open follow-ups queued into Pri-3**: R2b (the dude-claw engine is a SECOND rule engine whose seed drift NO probe watches — invisible rather than noisy, the structural-dark shape), R4 (separate `annotate` from `ntfy` before any future yield cut), R5 (confirm whether `tracer_peer_unreachable` was fixed or went blind). | Removed by running the pass, per convention. |
| — | ~~**RATIFY the second-brain taxonomy design** (Pri-3)~~ **DONE 2026-07-26 — RATIFIED at frontier tier, questions (a)(b)(d)**; ran as the completed-table row above. RATIFICATION section appended to the design doc; as-built 24 h grace supersedes the design text; two conditions elevated to contract. No design or code change required. Original queued text retained below for provenance. **RATIFY the second-brain taxonomy design** (queued 2026-07-26 by the Opus-class session that wrote it — see the completed row above). Read `.claude/plans/second_brain_taxonomy_2026_07_26.md`. **Do NOT re-derive §5** — it is live measurement (coverage dispositions, tracer fires, claw rule ids, the 7-box cron sweep) and is quoted with evidence. **Judge §1–§4**, which are argument: (a) is UNIT/CHANNEL/FALSIFIER the right axis, or does collapsing T2/T3/T4/T6 into instances of it lose something the prep's six tensions were holding separately? (b) T1 proposes ONE mini rule over the existing coverage block instead of a coverage historian — is that genuinely not a meta-organ, and is `inert`-must-not-be-scored the only exclusion? (c) ~~§3's mini-grace claim~~ **WITHDRAWN 2026-07-25 — the author found it WRONG by reading the code, before any ratification pass ran.** R1 counts **cron runs** (the unit its phenomenon lives in — R1's own insight, and this document's UNIT axis applied correctly); mini counts observed **mini ticks**. Converging R1 onto mini's semantics would have reintroduced the exact wrong-unit defect R1 fixed, and R1's log-on-disk state is *more* restart-durable than mini's in-memory streak. §3 is corrected in place: the consolidation is NOT "converge on one implementation" but "each debouncer declares the unit its phenomenon lives in" — share the DISCIPLINE, not the code. **Do not re-litigate this; the correction is settled by code, not judgment.** What remains for the ratifying pass is (a), (b) and (d) only — i.e. the AXIS itself. (d) T5 was left OPEN deliberately (its answer would require adding); confirm that was right rather than avoidance. | The design pass itself was frontier-shaped — novel architecture across ~8 organ families, ambiguous evidence (is a silent detector dead weight or a working seatbelt?), blast radius across the alerting contract ~50 probes share. It got an Opus-class author because the session that picked up the queue read the router's tier RECOMMENDATION as a statement about itself. The measurement half is sound and reusable; the judgment half is exactly what the rationed tier exists for, and a shallow self-ratification would be worse than this entry. |
| — | ~~**T5 — who owns "while you're away"?** (Pri-3 follow-on)~~ **RESOLVED 2026-07-26, NOT at frontier tier and it did not need it.** Re-queued to the operator as a scope decision per (d)'s condition; operator chose "widen the existing channel" (PROPOSE-only). The premise then proved **stale at source**: T5's "1 of ~50 signal classes watches for available-but-unadopted capability" is the 07-20 sweep's *pre-fix* count — there were already **two** shape-C classes (`watchdog_probe_core.py:60-61`) plus the shape-A companion, both wired into mini's propose channel in both seeds. So the channel was already widened; the gap was that the sweep which found them was a **one-off**. Verdict + the shape-C discipline: `second_brain_taxonomy_2026_07_26.md` (T5 section); the recurrence now lives as a PERIODIC item in `.claude/prompts/mini_cadence.md`. **Zero new organs, probes, files or runtime cost.** ⚠️ Two carries: a native *dream* detector for this is unsafe (pure `(state, history)`, no `note_disposition`, so `[]` reads as "nothing unadopted" — the Pri-2 defect one layer up); and the stale count survived a **frontier ratification pass**, which restated it as fact — T4 committed inside the document that names T4. | **Explicitly NOT frontier-shaped** — the operator half was a scope decision (theirs), and the rest was reading `SIGNAL_CLASSES` and two probe docstrings. Recorded here because the row was queued here, not as an upshift claim. The lesson from `9cadd372` held again: a queued frontier item can turn out to be unread code rather than judgment — check before spending the window. |
| — | ~~**POST-07-23 UNREVIEWED WINDOW** (Pri-1)~~ **DONE 2026-07-26 — ran as the completed-table row above** (window had grown to 84 commits; ~30 confirmed findings fixed incl. 1 CRIT, F3 leg-3 boundary redesigned, MA twins ported). Original queued text retained below for provenance. **POST-07-23 UNREVIEWED WINDOW — `c17c611c..HEAD`, 75 commits / 12,240 lines / 114 files (~9k lines of CODE).** Computed 2026-07-26; method + harness in `.claude/plans/audit_gap_2026_07_26.md`. Risk-ranked entry points, highest first: `src/utils/watchdog_probes_host.py` (1011 lines, ALL-NEW, pages), shared `watchdog_probe_core._load/_save_parity_streak` (+5 consumer probe modules), `scripts/gen_fleet_hosts.py` (399 — its self-consuming-checker fix is itself unreviewed), `src/gateway/meshtastic_broadcast_bridge.py` (212, new bridge), `src/utils/watchdog_tracker.py` (195, signal lifecycle — adjacent to the 07-26 `indeterminate`-CLEARED answer), `src/mini_dudeai/model_router.py` (883), `scripts/selftest.py` (184, 2 defects found 07-24), `src/gateway/message_queue.py` (165), `src/utils/claw_battery.py` (136), `src/utils/fleet_snapshot.py` (1294). **1 of 88 code files reviewed so far** (probes_host, this session) — 2 confirmed findings, both FIXED here, and those fixes are IN SCOPE for this pass (`feedback_review_your_own_fixes`). ⚠️ **Do not repeat my gap-computation error**: "ancestor of a ledger SHA" is NOT coverage, because most rows are scope-limited (row 14 = `src/gateway/*` only, row 21 = maps). Only BLANKET full-window rows give blanket coverage; the newest is `0a85896d..c17c611c`. | Adversarial sweep of a 9k-line window with no operator in the loop per step. Queued rather than run: an Opus session measurably hit the representation-vs-thing class twice in the same sitting (wrong-channel "unflashable" conclusion; the ancestry-as-coverage error above), and a shallow pass that BLESSES 87 files destroys exactly the signal this ledger exists to preserve. |
| — | ~~**DETECTOR-BLINDNESS CONTRACT — can a HOLD outlive its observer? (Pri-2, queued 2026-07-27)**~~ **DONE 2026-08-06 — RATIFIED as-built + test-pinned; ran as the completed-table row above** (hold unbounded; N=24h already owned by mini's detector_blind_any; consumers file holds as UNKNOWN — now 4 sabotage-drilled pins). Original queued text retained for provenance. Live instance: `probe_kernel_reboot_pending` on moc5 reported `degraded` **after** the condition was cured (rebooted 07-27, `/var/run/reboot-required` absent, running kernel == newest installed). Not a probe bug — the unit hosting it sets `ProtectKernelModules=yes`, and I proved the consequence **in the service's own sandbox**, not by reading the unit: `systemd-run --property=ProtectKernelModules=yes ls /lib/modules` → `modules_visible=0` vs `36` unsandboxed. So `newest_installed: null`, the probe cannot evaluate either leg, and `watchdog_tracker` correctly HOLDS the last-known verdict with `extra.unobserved_hold=true`, `debounce_streak: 347`. **The hold design is sound and deliberate** (`watchdog_tracker.py:38-46` — held signals must be REPORTED, since silence would be a false recovery, but must never drive an ACTION; tests in `test_watchdog_unobserved_hold.py` + `test_watchdog_lifecycle_fixes_2026_07_26.py`). ⚠️ **I twice guessed wrong here — check before re-deriving**: (1) "writer with no reader" is FALSE, `unobserved_hold` IS consumed by `watchdog_tracker.py:45` and `fleet_truth.py:571`; (2) the flag is genuinely absent on moc5, so this is not a re-set-after-boot case. **THE ACTUAL QUESTION (the judgment call): the hold contract assumes observability RETURNS.** Under a permanent sandbox it never does, so a "last known" becomes an unfalsifiable permanent assertion. Decide: should a hold be BOUNDED (after N unobserved ticks escalate the class to `detector_blind`/INDETERMINATE instead of continuing to assert the held severity), and what is N? Scope is a CATEGORY, not one probe — mini's warm brief listed **four** live `detector_blind_any` classes this session (`delivery_confirmation_stall`, `kernel_reboot_pending`, `mqtt_root_drift`, `rns_shared_instance_unresponsive`); audit which are structurally vs transiently blind. **Known-mechanical, do NOT spend the window on these**: (a) un-blind the probe (`ReadOnlyPaths=/lib/modules` or drop `ProtectKernelModules`) — verify in-sandbox, not by reading the unit; (b) `scripts/honest_status.sh` contains **zero** references to `unobserved_hold` (`grep -c` = 0), so the gate of record buckets a blind hold and a live observation identically as "1 degraded" — by the project's own tiering an unobservable is UNKNOWN, not degraded. (c) MeshAnchor parity port. | Frontier-shaped **only for the hold-vs-INDETERMINATE contract**, which sits on the honesty spine (`honest_failure_modes` #2 vs #9: "absence of evidence is not evidence of absence" pulls toward holding forever; "every swallow leaves a witness a PROBE can see" pulls toward escalating). Getting it wrong in either direction is costly: unbound holds train the operator to ignore a permanently-degraded line (alarm fatigue on a false signal), while clearing on blindness is the exact false-recovery the hold was built to prevent. Legs (a)/(b)/(c) are mechanical and pre-diagnosed above — per the T5/`9cadd372` lesson, a future pass should NOT re-read this code to rediscover them. |
| Pri-1 | **PYTEST-RC TRUST ARC — MF `3b3e78ef..b8d11e47` (7 commits, 22 files, +1450/-80) + MA `afef62e2..ed20c931` (5 commits)** (Pri-1, queued 2026-07-31). **Recipe:** `/code-review ultra` takes a PR/branch/nothing, NEVER a commit range — branch at the range start, cherry-pick onto it (new SHAs), pass the branch BY NAME, stay on `main` ([[project_review_pass_2026_07_29]]). **What it is:** pytest's exit code was measured 2026-07-28 to flap to 0 on a failed full-suite run (~50%, thread-join race at interpreter shutdown; loses failures only TOWARD zero). The cure had reached `honest_status.sh` inline and no sibling. This arc extracted it to `scripts/pytest_verdict.sh`, routed all 10 invocation sites through `scripts/lib/pytest_checked.sh`, converged the gate of record onto it, and dealt with the fallout in the calibration ledger. **Risk-ranked entry points:** `scripts/pytest_verdict.sh` (NEW — single point of trust for every gate in the repo), `scripts/lib/pytest_checked.sh` (NEW — fails CLOSED; both git hooks depend on it), `scripts/honest_status.sh` suite leg (the gate of record, now delegating), `src/mini_dudeai/calibration_ledger.py` (byte-locked MF↔MA; new `annotation` event kind + `fold` change), `scripts/annotate_ledger_provenance.py` (NEW — wrote 21 rows to LIVE operator data), `scripts/calibration_reverify.sh` (writes ledger verdicts), `scripts/cron_verdict.sh` (evidence capture, `331ad4e3`). ⚠️ **Also changed but NOT in git: the crontab on all 9 boxes** (`scripts/cron_capture_wire.py --apply`, 55 crons) — no test covers a live crontab. | **The judgment calls, in the order I'd spend the window:** (a) **The classifier is now the single point of trust for every gate, and its corpus is MINE.** Find an input where it returns PASS on a red suite, or UNKNOWN-blocks a healthy one. This is exactly the shape that scored precision 1.00 on my own 6 cases and 0.33 on 80 labels I didn't write ([[feedback_verify_the_verification]]) — self-authored corpora are the known weak spot, and here the corpus defines what "green" means repo-wide. (b) **Fail-closed blast radius:** a missing/unrunnable classifier now BLOCKS commits and pushes. Right call, or a bricking scenario during bisect, fresh clone, or partial checkout? (c) **The 21 annotations rest on a wall-clock heuristic** — a 04:40-06:00 LOCAL window standing in for "the 04:45 cron", on an RTC-less fleet where wall-clock is explicitly forgeable (honest_failure_modes #6: NTP steps, fake-hwclock). DST is unhandled. Is 21 the right set, and is landing-time attribution defensible at all, or should the annotation say less? (d) **Is "verdict stands, evidence qualified" the right disposition** for those 21, or should they have been reopened/re-derived? That is a judgment about the integrity of my own track record and is precisely what a self-review cannot settle. (e) **Ledger growth:** the annotation note is ~700 chars × 21 ≈ 15 KB in one pass; `append_jsonl` rotation drops OLDEST events first, which would silently drop old CLAIMS and move the historical ratio. Cap is 2 MB, file is 67 KB — headroom now, but is the payload too fat as a pattern? **Known-mechanical — do NOT re-derive these, they are verified:** classifier corpus 20/20 + both cures sabotage-reverted; wrapper 8/8 incl. fail-closed; suite-leg 17/17 through the converged gate incl. the missing-classifier leg; `calibration_drift` probe reads only `state["broke"]` and is unaffected by annotations (checked); ledger rotation headroom (checked); the `--collect-only` leg is deliberately excluded and that was verified against real collect-only output, not assumed; MA has no `calibration_reverify.sh`, so no unguarded consumer there. |
| Pri-1 | ~~**GUARD-SPINE + BACKUP-CHOKEPOINT ARC**~~ **DONE 2026-08-06 — ultra pass run + Fable-adjudicated; ran as the completed-table row above** (1 finding confirmed+fixed both repos: backup identity followed the pre-reservation label; all five judgment items survived attack). Original queued text retained: **MF `8c550c72..4ef7c5ac` (4 commits, 10 files, +874/-73) + MA `d6b57e5a..5f1b13e2` (4 commits, 10 files, +1067/-78)** (Pri-1, queued 2026-08-05). **Recipe:** `/code-review ultra` takes a PR/branch/nothing, NEVER a commit range — branch at the range start, cherry-pick onto it, pass the branch BY NAME, stay on `main`. **What it is:** Issue #29's Layer-2 regression guards were found INERT — `SRC_DIR` was `<repo>/tests/../src`, so every scanned path contained `/tests/` and each guard's own `/tests/` skip discarded every match. Dates to the file's FIRST commit (`c378be1a`, 2026-02-23): 8 contracts enforced nothing for 5.5 months / 891 commits while passing every run. Separately, a real backup was DESTROYED during an authorised cleanup (generic `~/delivery_db_backup_<date>/` reused across two sister repos in one shared home; `sqlite3 .backup()` overwrote in place, unrecoverable), producing the `utils/backup_paths.py` chokepoint. **Risk-ranked entry points:** `tests/test_regression_guards.py` (both repos — the scanner all 8 contracts depend on), `src/utils/backup_paths.py` (NEW, both repos, byte-identical), `scripts/guard_drill.py` (NEW), the 5 routed backup writers (`commands/rns.py`, `commands/device_backup.py`, `commands/fleet_backup.py` MF-only, `utils/device_backup.py`), `src/core/orchestrator.py` (install path switched pip3→pipx + routed through `utils.pip_install`), MA `tests/conftest.py` (3 session-scoped autouse gates). | **The judgment calls, in the order I'd spend the window:** (a) **`_strip_trailing_comment` is a hand-rolled quote state machine that now gates EVERY scanner guard in both repos.** It is per-line, so it knows nothing of triple-quoted strings, f-string nesting or line continuations. Find an input where it strips too much and a REAL violation goes invisible — that would re-introduce silent inertness in a new form, inside the very file whose inertness this arc fixed. I reasoned the mis-parse modes fail toward NOT stripping (safe) but did not prove it; treat my safety argument as the thing to attack. (b) **I removed the per-guard `test_`/`/tests/` filters (8 in MF, 10 in MA) and replaced them with one `_is_test_path`.** Verify no guard depended on that filter for a reason OTHER than test exclusion. Both suites pass — but "passes" is precisely the evidence this arc proved worthless. (c) **`scripts/guard_drill.py` reports 8/8 FIRE, and I authored all 8 drill snippets.** That proves those 8 strings trip those 8 guards, NOT that the contracts are correctly specified — the known self-authored-corpus weak spot ([[feedback_verify_the_verification]]). One drill case already tested a contract that did not exist. Attack the mapping between each drill and the contract's real intent. (d) **`TestServiceCheckContract` numeric budget → named `SINGLE_LINE_ALLOW`, and `TestConfigPathContract::test_no_hardcoded_reticulum_paths_in_code` DELETED** (it collected violations then `pass`ed — no assert, no print). Judge the deletion: is losing a 47-hit advisory worse than counting a no-op as coverage? (e) **The claim "nothing survived the 5.5-month window"** rests on all guards passing against the current tree plus a worktree run against the deployed SHA. Guards only check what they check — is that inference sound enough to have RETIRED the historical replay (decision recorded in the session notes)? **Known-mechanical / MEASURED — do NOT re-derive:** `_candidate('.hidden')` → `'-2.hidden'`, a dotfile loses its name and gains a leading `-` (measured; safety invariant HOLDS — distinct path, original intact — so it is a wart, not a hole, and is unfixed on purpose rather than shipped hastily at session end); `reserve_backup_dir` accepts an EMPTY existing dir under `refuse` (deliberate, but the emptiness check is not atomic with the caller's write — TOCTOU worth a verdict); `_MAX_UNIQUE_ATTEMPTS` exhaustion raises `BackupDestinationExists`, semantically "existence" for what is really exhaustion; watchdog config `[]` IS honored (parser uses `isinstance(x, list)`, not truthiness — verified); `_operator_home_for_root()` correctly returns the operator home under a root service, NOT `/root` (verified live on meshanchor-server); guards 54 MF / 43 MA, suites 10367 / 6179, lint 0, CI 4/4 both, `honest_status.sh` exit 0. |
| — | ~~**RF + RNS/LXMF EGRESS GUARD ARC** (Pri-1)~~ **DONE 2026-08-09 same day** — ran as the completed-table row above (Fable 5 /code-review background fork, not the cloud ultra recipe — ultra quota was exhausted; 10 confirmed findings fixed, full suite green). Judgment calls (b) connect_ex split, (c) open_reticulum None-readers and (e) allowlist concurrency were NOT attacked — re-queued as the Pri-2 row below. | Removed by running the pass, per convention. |
| — | ~~**EGRESS-GUARD residual legs (b, c, e)** (Pri-2)~~ **DONE 2026-08-10 next day** — ran as the completed-table row above: leg b refuted with evidence (plus 3 secondary fixes incl. the missing probe witness), leg c 3 confirmed fixed (incl. an MF019 chokepoint EVASION in MeshAnchor, guard leg added both repos), leg e confirmed and fixed red-first (grant registry + depth counters). | Removed by running the pass, per convention. |
| Pri-3 | **RNS-fork `known_destinations` / announce-retention root-cause** (queued 2026-08-12 by the overdue-ledger triage; durable twin = deferred_work `rns-known-destinations-retention`, re-gated to 2026-11-15). The 2026-06-29 moc1 incident: hourly re-announce refreshes peers' PATH table but NOT their identity store, so a ~12h identity TTL evicts a re-announcing peer → `Identity.recall` None → echo tx skipped → false `tracer_peer_unreachable` wedge, asymmetric. Full recipe + tells in memory `tracer_peer_unreachable_identity_eviction_2026_06_29`. **Verified quiet 2026-08-12**: 0 `Identity.recall returned None` in 30d of echo journals on moc1 AND VolcanoAI; recent tracer wedges are all the <1h cold-start class. Stopgap (restart the lost peer's echo) works; watchdog re-flags recurrence independently. | Fork-internals judgment on the announce/identity-retention contract — changing retention semantics in a hard fork that must stay wire-compatible is exactly the ambiguous-evidence, high-blast-radius work the rationed tier exists for, and with no live recurrence there is no urgency to spend a smaller session guessing at it. |
| — | _(add new upshift rows here as sessions surface review-shaped work)_ | |

## Conventions for future passes

1. When a review pass closes, add its row HERE in the same commit as the
   fixes ("review provenance rides the fix commit").
2. Hand every new pass the residual/refuted pointers of overlapping prior
   rows — re-flagging a documented residual wastes a verification cycle.
3. A fix commit is unreviewed code: re-review it before push
   (feedback_review_your_own_fixes), and record the re-review in the row.
4. Cite SHAs **reachable from `main`** — an amended/rebased pre-image is not
   evidence (`git merge-base --is-ancestor <sha> main` before recording).
   Four live instances across MF+MA (`bfc1eba7`, `d8d2c247`, MA `0afc8add`,
   MA `60437c38`), all caught by later audits — and all four share ONE
   mechanism: convention 1 ("provenance rides the fix commit") means the row
   cites the fix SHA and is then **amended into that commit, changing the SHA
   it just cited**. So: write `this commit` for the carrying commit; cite
   literal SHAs only for commits already landed beneath you.
5. **One row per line, 5 cells, no literal `|` in prose** (reword quoted
   shell like an or-true guard). The gate's parser is line- and pipe-based:
   a merged line hides a whole pass (the 07-25/26 line-merge made the
   taxonomy row invisible for 12 days), and a stray pipe truncates every
   cell after it. `review_provenance_check.py` now blocks a NEW malformed
   row at push and warns on pre-existing ones.
6. **This file is PUBLIC and its history is permanent — record the RISK, not
   the RECIPE.** It has lived in a public repo since 2026-07-05, and moving it
   later would NOT unpublish it: `git rm` drops it from the tip while every
   past version stays in the public history, in forks, and in the clone on
   every fleet box. Un-publishing would mean rewriting history and breaking
   those clones, so the only lever that actually exists is what gets written
   NEXT. Keep what gives a reviewer their bearings: the entry points, why a
   defect there would be costly, what has already been measured. Leave out
   what mainly helps someone reach a degraded state on live hardware — named
   hosts or interpreters that are exposed, enumerated bypass paths, a
   step-by-step to a silent outage. The steer survives the redaction: on
   2026-08-09 the row said "attack the arming predicate, a false ARM is a
   silent mesh outage" AND enumerated the eight production interpreters where
   `pytest` is importable; the first clause alone points a reviewer the same
   way. ⚠️ This deliberately cuts against the project's habit of publishing
   its own failure modes (`persistent_issues.md`, `honest_failure_modes.md`) —
   those describe defects already CURED, while this file describes soft spots
   still OPEN by design, which is the whole point of a queue. Companion to
   MF014/MF015, which keep operator-specific values and LAN IPs out of
   published docs for the same reason.

## 2026-08-11 — tonight's INSTRUMENT changes, queued for a frontier pass

> **CLOSED same day** — ran as the 2026-08-11 row in the table above
> (Fable 5 inline pass): 4 confirmed fixed (splitter schema-drift flip,
> unwitnessed via-declaration demotion, class-pin regex holes, missing
> flock) + the §7 sixth-check question answered twice (deferred RF end
> today; reboot survival as the class in waiting). Kept per convention —
> the steers below are the review spec the row's findings answer to.

**Why queued rather than self-reviewed**: I shipped three defects in my own
acceptance logic in one evening (ownership unrecorded, a crashlooping radio
read as healthy, dead timers invisible), and a fourth — TLS material deleted
then logged as "legitimately absent" — was found by the operator from a
browser. A reviewer whose blind spots differ from mine is exactly what these
ranges need; a self-review would re-apply the model that produced them.

Ranges, most-suspect first:

1. `scripts/fleet_offline_check.sh` + `src/utils/watchdog_probes_liveness.py`
   (`3eca3b5b`) — the `via` dependency verdict. **Steer**: attack the
   promotion/demotion path when a dependency flaps mid-outage, and the
   back-compat read of a 6-field row while a 7th is being written. A wrong
   demotion here silences a real box-down page.
2. `src/mini_dudeai/brief.py` + `rollup.py` (`8d4c746e`, MA port `e72a9a03`) —
   the active/resolved split. **Steer**: the conservative default (unknown
   pair stays ACTIVE) is asserted in two places; confirm a state-schema drift
   cannot flip it to the hiding direction.
3. `scripts/honest_status.sh` skew leg (`6cd26cb9`) — three-repo dispatch.
   **Steer**: the class pin compares DEFINED vs USED repo vars by regex; a
   repo wired through an alias or an indirect var would satisfy the pin while
   still being judged against the wrong HEAD.
4. The capture/restore scripts are NOT in-repo (they live in the session
   scratchpad, deliberately) — but `.claude/plans/pi_box_runbook_zero.md`
   §4/§5/§7 encodes their contract. **Steer**: §7's acceptance list is now
   five checks long because four defects walked past earlier versions; ask
   what a SIXTH would be, i.e. what still passes all five while the box is
   dead. That question is the one I could not answer for myself all evening.

---

## 2026-08-12 — the inert/indeterminate arc + 4 spin-off fixes (queued for a frontier adversarial pass)

> **CONSUMED 2026-08-12** by the frontier pass recorded in the completed-review
> table (row dated 2026-08-12; fix commit `a5d5c377`). Steers 1, 2, 4 each
> found a real defect; steer 6's five re-derived clean. Kept below verbatim as
> the review's input record. The follow-on re-review of `a5d5c377` itself ran
> the same day (row above) — its fix commit closes this arc.

Author: Opus 5 session, ~9h. Range: MeshForge `1f30cc8e..9840d006` (8 commits),
MeshAnchor `de4bf822..decf2c9c` (2). All gates green (`honest_status.sh` exit 0
at 9840d006, CI green both repos, MF 10,760 tests / MA 6,382). Green gates are
the START of this review, not its conclusion — every check that passed is one I
wrote or one whose narrowness I chose.

**Where I am least confident, in descending order. These are steers, not a
verdict — each is a place my own reasoning is the only thing holding the claim
up.**

1. **`32a6998e` — the seven `absent -> inert` conversions.** The load-bearing
   argument is "nothing is hidden, because `service_inactive` owns
   expected-active-but-missing; `systemctl is-active` answers `inactive` for a
   nonexistent unit (verified)." I verified the systemctl half. I did NOT
   verify the other half per box: that each converted unit actually appears in
   some box's `services_expected_active`. **Steer**: find a box+unit pair where
   the unit is ABSENT and NOT expected-active. On that pair, my change moves a
   class from `indeterminate` to `inert` and, if `service_inactive` never
   judges it either, NOBODY owns it — which is the exact defect this arc was
   fixing, re-created one layer over. `fd_exhaustion` / `phoneapi_tcp_leak`
   are runner-gated on the map being expected-active; `main_thread_wedge` is
   driven by `services_wedge_check`, which is `[]` on meshanchor-server.

2. **`4bb20f1f` — CODE = `src/ requirements/ templates/`, and `scripts/` is
   deliberately excluded.** My justification: scripts are exec'd fresh per
   invocation, so no resident unit can be stale on one, and no `src/` module
   imports `scripts/` (checked). **Steer**: a long-lived unit whose ExecStart
   IS a `scripts/` entry point would be stale on that script while my leg calls
   it "behind on prose only" — a strictly quieter answer than before. Enumerate
   ExecStart lines fleet-wide and look for a `scripts/` path on a `Type=simple`
   daemon. If one exists, the path class is wrong, and the fail-safe does not
   help because the code-head resolves fine.

3. **`fcbd4cc0` — `tests/test_rns_api_surface.py` SKIPS when RNS is
   unimportable, which is CI.** I said so out loud in the file, but say it
   again adversarially: this gate is inert in the one place that runs on every
   push. It enforces only on the fleet and on dev boxes. **Steer**: is a gate
   that never runs in CI worth its maintenance, or should the fork be in the
   CI dep set so the class it guards (a fork-merge rename hiding in a swallowed
   except) is actually caught before deploy rather than after?

4. **`fcbd4cc0` — `exit_handler()` is shared-instance-safe.** I concluded that
   from reading five lines of the fork (`_should_run = False`, `void_queues()`,
   `persist_data()` guarded by `is_connected_to_shared_instance`). **Steer**:
   `void_queues()` on a client of a SHARED rnsd — whose queues, and can a
   gateway shutdown discard anything the shared instance still owns? I did not
   read `void_queues`.

5. **`05847b94` — I DECLINED to make the propagation probe live** (duplicate
   owner, per-tick RNS traffic, weaker evidence than the hourly soak that
   already exists). That is a judgement call made by the same session that had
   just misread the probe. **Steer**: attack the decision, not the f-string.
   Is 18h (3 announce periods) genuinely the floor, or is there a cheap
   escalation — active probe ONLY when passive evidence crosses some fraction
   of the window — that gets faster detection without a 30s-loop cost?

6. **The RNS API audit's 5 "false positives."** I triaged them by eye
   (docstring, hasattr-guarded, cmdline string literal, `Protocol.RNS.value`,
   docstring). **Steer**: re-derive independently; if any is real, it is a live
   AttributeError sitting in a swallowed except right now.

7. **Everything I verified with a throwaway one-liner.** Seven of my own
   measurement commands were WRONG tonight (unused awk cutoff, lines counted as
   events, unsorted `find | head`, `pgrep` self-match x2, a verdict grepping
   "passed" inside "2 failed, 3 passed", an `until` loop satisfied by the
   OUTGOING process's write). All were caught by re-reading my own command, not
   by any tool — so assume the ones I did NOT re-read are the survivors.
   **Steer**: re-run the live claims from the commit messages rather than
   trusting the quoted output.

Fleet context the review will want: ~30 units restarted and verified by
function this session; `meshanchor-server` has NO meshtasticd (that box is the
whole reason the arc exists) and its USER journal is dark; `meshforge-echo`,
`meshforge-lxmd`, `meshanchor-echo`, `meshanchor-mini-dudeai` are USER units
while map/gateway/fleet-collector/fleet-watchdog are SYSTEM on the same boxes.

---

## QUEUED 2026-08-31 (Opus 5) — `scripts/rotate_session_notes.sh` adversarial pass

> **CLOSED 2026-08-31 by the Fable 5 pass** — see the 2026-08-31 rotate_session_notes table row at the top. Fix commit `7086ec4f`. Brief kept below for the record.

**Target**: `scripts/rotate_session_notes.sh` and
`tests/test_rotate_session_notes.py`, both @ **`5d83306f`** — review THAT sha,
not the `d5d78277` where the script first landed; it gained a whole delete
path afterward. 33 tests. Pushed; NOT deployed to the fleet (drift held
deliberately until the moc2 F2 capture lands ~2026-09-01 16:15Z), so every
box is still running code without this file.

**Run it**:
```bash
git -C /opt/meshforge show 5d83306f:scripts/rotate_session_notes.sh
python3 -m pytest tests/test_rotate_session_notes.py -q     # 33, ~2s
shellcheck scripts/rotate_session_notes.sh                  # clean at this sha
```
The tests show the sandbox pattern: a fixture notes file under `tmp_path`,
`HOME` redirected so backups and lock files land in the sandbox. Copy it —
do not improvise a harness against real paths.

**What it does**: splits a markdown session-notes handoff doc on `^## `
headings and moves older sections into a half-year archive file, to keep the
file under the 80KB gate at `harness_audit.sh:156`. Human-invoked, dry-run by
default, `--apply` mutates.

### ⚠️ Before you run anything

`~/.claude/plans/gateway-session-notes-<host>.md` is **live operator data** —
years of handoff context, not a fixture. Never run `--apply` against the
default path. Every experiment goes through `--notes` pointed at a copy, with
`HOME` redirected to a sandbox (the script writes backups and lock files under
`$HOME/.local/state/meshforge/`). The test file shows the pattern.

### Task frame — deliberately NOT a doctrine check

Review this as **bash, parser and filesystem correctness on a file-mutating
tool**. Do *not* frame it as "does this comply with CLAUDE.md / the rules
files." That frame is the correlated half: I wrote the code with those same
`@`-included rules in context, so we would both be checking the same list, and
the defect is by definition outside it. Evidence that the frame matters — both
real defects I found on my own came from asking "what does a line-range
splitter get wrong?", and the one rule I *violated* (`honest_failure_modes` #8,
concurrent writers) was loaded in my context every single turn and I still
shipped a file-mutating tool without a lock until a second pass caught it.

**Deliverable**: concrete failure scenarios — *input → wrong output or data
loss* — not verdicts. "This looks fine" and "this is well structured" are both
worthless here. If you cannot construct the input, say the concern is
unverified rather than upgrading it.

### The invariants (what "correct" means — check the property, not my story)

1. No byte of the original file is ever lost: content ends up in the new notes
   or in the archive, never neither.
2. A section marked live is never rotated, regardless of its position.
3. A failed or refused run leaves both files exactly as they were.
4. A backup is never overwritten.
5. Two concurrent `--apply` runs never interleave.
6. Post-apply verification cannot report success when content was lost.

### Already found and fixed — do not spend the pass rediscovering these

Stated bare, no reasoning, so they don't anchor you:
- `## ` inside a ``` fence was parsed as a section boundary.
- Unbalanced fence silently mis-split; now refused by line number.
- Post-apply check asked "is this heading in the archive?", which passes
  vacuously when the heading was already there.
- No `flock`; two `--apply` runs could interleave.
- Symlinked notes/archive would be replaced by a regular file.

### Steers — where I think I am most likely still wrong

1. **Untested input shapes.** I never exercised: CRLF line endings; a file with
   no trailing newline; a file whose *first* line is `## ` (`pre_lines=0`); a
   heading as the final line with no body; headings containing tabs (the index
   is TSV — a tab in a heading would corrupt the field split). Any of these
   could mis-slice line ranges.
2. **`ls -1t` picks the archive by MTIME.** "Newest existing archive" is keyed
   on modification time, so `touch`ing an old archive silently redirects where
   content lands. mtime is a weak key for "which archive is current" — is there
   an input where this appends to the wrong half-year file?
3. **Pipeline subshells.** The kept/rotated section extraction runs
   `... | while IFS='-' read -r a b; do sed -n "${a},${b}p"; done`. `set -e`
   does not reliably propagate out of that subshell. If `sed` fails mid-loop,
   does anything catch it before `mv`? I believe the byte assertion does —
   verify rather than accept that.
4. **`LC_ALL=C` is exported process-wide** so `awk length()` counts bytes. It
   also affects every `grep -cxF` against headings that contain emoji. Confirm
   byte-mode matching is actually what happens there.
5. **`SESSION_NOTES_STICKY_RE` is interpolated into awk via `-v`.** Not code
   injection, but a malformed regex from the environment — what does it do?
   And a too-broad regex silently makes everything sticky, i.e. the tool
   quietly stops working while reporting success.
6. **Backup pruning — FIXED after this brief was written, and now the highest
   risk surface in the file.** Retention was absent; `--keep-backups N`
   (default 10) now prunes per notes-basename, keyed on the ISO timestamp in
   the FILENAME sorted lexically (never mtime), only after a clean
   verification, matching an exact `<stem>.????-??-??T??:??:??Z.md.bak` shape,
   best-effort so a prune failure cannot misreport a good rotation.
   **This turned a write-only tool into one that DELETES, inside the directory
   whose only job is recovery — review it harder than the rotation itself.**
   Ask specifically: can any input make the glob select a file it did not
   write? Can the notes stem and the archive stem (one is a PREFIX of the
   other) evict each other? Does a clock that went backwards — RTC-less Pis,
   this fleet's standing hazard — produce a filename that sorts as OLD and
   gets pruned while being the newest real backup? I believe not, and I have
   not constructed that input.
7. **Review the TESTS as code, not as evidence.** A test that cannot fail is a
   defect (Issue #29 Layer 2 was inert for 891 commits). I mutation-drilled
   eight guards — fence awareness, fence imbalance, flock, symlink, count-delta,
   byte assertions, backup `O_EXCL`, sticky rule — and all eight turn the suite
   red when removed. That leaves every *other* assertion in the file unpinned.
   The count-delta check was green under mutation until I added fault injection,
   so assume siblings of that shape remain.
8. **Challenge the design, not just the code.** Fair game: whether size-based
   rotation is the right trigger at all; whether "newest N by position" is a
   sane staleness proxy for a file that is not strictly chronological; whether
   this should exist rather than the audit leg simply warning earlier.

### What is already mutation-pinned (so you know what is NOT)

13 guards have been proven to turn the suite red when removed: fence
awareness, fence imbalance, flock, symlink guard, count-delta check, byte
assertions, backup `O_EXCL`, sticky rule, prune-pattern narrowness, prune sort
order, `--keep-backups 0`, prune-only-after-clean-verification, and
own-backup-undeletable. **Every OTHER assertion in that test file is
unpinned** — treat those as unverified scaffolding, not as evidence.

⚠️ **Two of those drills first reported a false pass.** The mutation never
applied — `||` in the matched text collided with the `sed s|...|` delimiter —
and the harness happily printed "0 failed", which reads exactly like a guard
holding. The drill now fails loud on an unchanged script ("ANCHOR MISS"). If
you run your own mutation drills here, assert the file CHANGED before
believing the result: a drill that cannot miss is the same defect class as a
guard that cannot fail.

### Three questions I posed and did not answer

Recorded as unverified rather than dressed up as cleared:
1. Can any input make the prune glob select a file the tool did not write?
2. Can the notes bucket and the archive bucket evict each other, given one
   stem is a strict prefix of the other?
3. Can a backward clock still produce a prunable filename for a backup that
   is actually current? (The run's own backup is now excluded by identity,
   but every OTHER recent backup is still ordered by a forgeable clock.)

### Provenance caveat

This file has already been through one self-review pass that found and fixed
four defects. That means the cheap findings are gone and the survivors are
specifically the ones that a careful read *by me* does not surface — so a pass
that returns "looks good" is more likely to be a correlated miss than a clean
bill.

---

## ~~QUEUED~~ CLOSED 2026-09-01 (Fable 5.1 — see the completed-table row) — field-eComm + duty-cycle DESIGN pass (not a code review)

> **Closed the same day it was queued.** Decided positions + drills live in the seed's `⚖️ Frontier design pass — DECIDED 2026-09-01` section. Queue text kept verbatim below.

**Target**: the ⚖️ design decisions batched in
`.claude/plans/field_ecomm_and_dutycycle_fleet.md` (the seed is Opus-ready for
build work; ONLY the ⚖️ items are frontier-shaped). Queued at the 2026-09-01
session close by the Fable session that closed the rotate_session_notes pass —
per the model_advisor rule, so the next rationed frontier pass lands here
instead of being burned on ops.

**The three batched decisions** (seed lines ~36/41/57):
1. **Detached = ZERO fleet dependencies** — what the kit must self-derive
   (time via chrony orphan, identity, DNS) and what it must refuse to need.
2. **REJOIN semantics** — when Starlink returns, artifacts flow OUT only;
   design the one-way valve and what "caught up" means observably.
3. **DORMANT as a DECLARED power posture** — a fleet-level state, not a
   per-box hack; how it is declared, observed, and distinguished from a
   fault (unobservable ≠ dark ≠ dormant — the #74 creed extended to power).

**Frame**: adversarial DESIGN review — attack each decision with concrete
failure scenarios (storm timeline, clock-forgeable Pis, tunnel-only boxes,
the two-preset RF split), not compliance prose. Deliverables: decided
positions written into the seed + the drills that would prove each one in
the virtual fleet BEFORE hardware. Context that must be loaded: storm-prep
notes (`.claude/research/storm_prep_power_starlink_2026_08_27.md`), Lala
forensics (zero-byte state-file class), NTP-island work, and the Starlink
cutover ~2-week window flagged 2026-08-27 — the cutover clock makes decision
1 and 3 time-sensitive.

---

## ~~QUEUED~~ CLOSED 2026-09-01 (Fable 5.1 — see the top completed-table row) — mesh-oracle policy split: WHO may query a gateway

> **Closed the same day by the frontier row at the top of the table.** Kept verbatim below as the queue's record; the live evidence section was the right frame — the two-hop path had already fired 74 times.

**Target**: item (b) of deferred task `mesh-oracle-followups` — and ONLY (b).
Queued per the model_advisor rule: it is a trust-boundary DESIGN decision with
a transitivity question, which is frontier-shaped. The other two items in that
task are explicitly NOT for the rationed pass — see "Do not spend the pass on"
below.

### The split, read live 2026-09-01 (not from the task text)

    moc   ORACLE_ALLOWLIST=!dd8d894b,!b29fa244,!896b1917   (3 nodes)
          ORACLE_RNS_ALLOWLIST=f0a6899b...,3ac3d1e3...     (2 explicit hashes)
          no channel gate, no explicit cooldown

    moc3  ORACLE_CHANNELS=meshforge                        (channel gate only)
          ORACLE_RNS_ALLOWLIST=*                           (ANSWER ALL over RNS)
          ORACLE_PHONEAPI_TAP=1 · ORACLE_COOLDOWN_S=10
          no node allowlist at all

Both are drop-ins under `meshforge-gateway.service.d/`. Note `from_env` is
fail-closed on an empty allowlist (`responder.py:246`), so neither box is
accidentally open — moc3's `*` is a deliberate, written wildcard.

### The question to decide

Is the split INTENDED — moc3 as the open channel-gated responder, moc as the
strict one — or is it drift that should be aligned? Two sub-questions the
decision has to survive:

1. **Transitivity.** A relayed query reaches moc3 with the RELAYING GATEWAY as
   the RNS sender, not the originator. moc's RNS allowlist already trusts two
   specific hashes; moc3 trusts `*`. So gateway-to-gateway trust plus a
   wildcard composes into "anything that can reach any gateway can query moc3",
   which is not a property either box's config states on its own. Attack it
   with a concrete two-hop path, not prose.
2. **What the channel gate is actually worth.** `responder.py:139-146` answers
   when the node is allowlisted OR the inbound channel is whitelisted — the
   legs are ADDITIVE, not intersected. On moc3 the node leg is empty, so
   `meshforge`-channel membership is the entire Meshtastic-side authorization.
   Decide whether channel membership is an authorization primitive or just a
   routing label.

**Deliverable**: an operator-ratified position recorded in the task's `check`
field, plus whichever drop-in edits it implies. A decision that says "the split
is intended" is a fine outcome — but it should be WRITTEN, so the next reader
does not re-derive it from two disagreeing drop-ins.

### Do not spend the pass on

- **(a) LEGIBILITY — DONE 2026-09-01**, as day-work, not frontier. `fleet:` ->
  `nodes:` at the producer (`intents.py`), both branches (count and `?`), plus
  the two prose references (`snapshot.py`, `meshtastic_handler.py`) and the
  test assertions. Drilled by planting the old label: 4 test_oracle failures,
  so the assertions are load-bearing rather than vacuous. Historical records
  were deliberately NOT rewritten — the 2026-06-22 substack post and the
  2026-06-21 arc plan quote real output from those dates, and editing a
  transcript to match today's code falsifies the record.
  ⚠️ **MeshAnchor carries the identical defect** at its `oracle/intents.py`
  lines 103-104. The file is a DIVERGED twin (differing md5, absent from
  `parity_check.py`'s set), so this is a port to make in MA's own tree, not a
  byte-locked edit — see [[reference_repo_twin_map]] tier 3.
- **(c) WITNESS — ALREADY CLOSED, verified live 2026-09-01.** The task text
  says the audit log "was never built". That is STALE. `log_fn=_log` is wired
  at all five call sites (rns_events, meshtastic, phoneapi_tap, mqtt, meshcore)
  and the log is WRITING: moc 10 records (last 2026-08-29T17:19:04Z, the RNS
  drill), moc3 108 records (last 2026-08-13T07:05:36Z). The runbook's
  `~/mesh_oracle_log.jsonl` was wrong, not missing — the bare home is
  `ProtectHome=read-only` under the gateway sandbox and `append_jsonl` swallowed
  the OSError (#60), so `oracle_log_path()` was moved to
  `~/.local/share/meshforge/mesh_oracle_log.jsonl` (`snapshot.py:237-248`).
  Checking the path the runbook NAMED would have confirmed the gap that no
  longer exists; the consumer-of-record is the data dir.

### ⚡ LIVE EVIDENCE for (b) — the wildcard demonstrated, 2026-09-01

Not a hypothetical. Drilled on moc3 the same day this entry was queued, via
`scripts/oracle_rns_drill.py` (the sender-side drill, run ON the box):

    T0                2026-09-01T16:55:08Z — drill identity DID NOT EXIST
                      (`~/.config/meshforge/oracle_drill_identity`: No such file)
    drill source      5f1e34a77eb2b8e879a00fdc63dc53c3  (minted after T0)
    oracle dest       f68c2f56cb61527b6c9ad603b9a5009a
    RESULT            sent=5 replies=5, exit 0
    audit log         108 -> 113 records, all `delivered: true`,
                      mtime 2026-09-01T16:58:07Z (> T0)

**A keypair created seconds earlier, allowlisted nowhere, on no channel, asked
moc3 for fleet status and got authoritative answers** — including
`whatsup` -> `active: detector_blind_any`, i.e. the box volunteering its own
degraded-detector state to an unknown sender. That is `MESHFORGE_ORACLE_RNS_ALLOWLIST=*`
working exactly as written (`responder.py:146`: `if self._answer_all or
node_key in self._allowlist` — the wildcard short-circuits before the
allowlist is ever consulted).

So sub-question 1 no longer needs a constructed two-hop path to motivate it:
the ONE-hop case is already open to anyone who can reach moc3 over RNS. The
transitivity question is what that composes to when a relaying gateway is the
apparent sender — strictly worse than this, not better.

**Frame this for the pass as a decision, not a defect.** The wildcard is
deliberate and written, the gateway is a public-facing responder by intent,
and every answer it gave is arguably public fleet-health data. The pass should
decide whether that intent still holds now that it is demonstrated rather than
theoretical — and if it does, say so in writing so this evidence stops reading
as an unreviewed finding.

**Bonus data point for (a)**: moc3's reply renders `fleet:?`, not a number —
its `directory_total` is None, so the mislabeled field degrades to `?` there
while moc shows a count. The rename should be checked against BOTH shapes.
