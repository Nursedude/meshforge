# Core dependencies for the installer UI
click>=8.0.0

# Meshtastic Python library (required for radio communication)
# Note: CLI (meshtastic command) can also be installed via pipx for user isolation
meshtastic>=2.3.0
rich>=13.0.0
pyyaml>=6.0
requests>=2.31.0

# System utilities
psutil>=5.9.0
distro>=1.8.0

# Configuration
python-dotenv>=1.0.0

# TUI Framework (for SSH/headless access)
# Install with: pip install textual
textual>=0.45.0

# Web UI Framework
flask>=2.3.0

# GTK4 dependencies (for graphical UI) - install via apt:
# sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 libadwaita-1-0 gir1.2-adw-1
# PyGObject is not listed here as it must be installed via system packages
