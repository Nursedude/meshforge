# MeshForge Monitoring Dependencies (optional, enhances all profiles)

# System monitoring
psutil>=7.2.2

# WebSocket support (real-time message broadcast)
websockets>=17.1

# Prometheus metrics endpoint (/metrics on the map server).
# Optional: when not installed, /metrics returns 503 with a clear
# message — does not break the rest of the service. Pinned via the
# >= floor only because the API surface we use (Counter, Gauge,
# generate_latest, CONTENT_TYPE_LATEST) has been stable since 0.10.
prometheus_client>=0.26.0
